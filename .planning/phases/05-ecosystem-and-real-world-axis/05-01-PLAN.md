---
phase: 05-ecosystem-and-real-world-axis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ch11-json-http/App.fsproj
  - examples/ch11-json-http/package.json
  - examples/ch11-json-http/vite.config.js
  - examples/ch11-json-http/index.html
  - examples/ch11-json-http/src/App.fs
  - examples/ch11-json-http/package-lock.json
  - src/ch11-json-http/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `cd examples/ch11-json-http && dotnet tool restore && npm install && npm run build` and it exits 0, producing examples/ch11-json-http/src/App.fs.js (SC-1, SC-4)"
    - "examples/ch11-json-http/src/App.fs fetches JSON from a real public API (https://jsonplaceholder.typicode.com/todos/1) via Fable.Fetch, decodes it with a MANUAL Thoth.Json decoder into an F# record, and renders the decoded data in the DOM (SC-1)"
    - "The decoder is a MANUAL Decode.object / get.Required.Field decoder — NOT Decode.Auto (Auto uses reflection and breaks under Ch.13 --noReflection; manual is also clearer pedagogy)"
    - "The fetch+decode runs inside an Elmish async Cmd: update uses Cmd.OfAsync.either to call an Async<string> fetch fn, dispatching FetchSucceeded on success and FetchFailed on error (NOT Cmd.OfAsync.perform — errors must be handled, RESEARCH Pitfall 7)"
    - "Decoding uses Decode.fromString todoDecoder json : Result<'T,string> with explicit Ok/Error pattern matching (NOT Decode.unsafeFromString)"
    - "Thoth.Fetch is NOT used (it pins Thoth.Json >=6 → version conflict with 10.5.1); Fable.Fetch 2.7.0 + Thoth.Json 10.5.1 are used as separate packages"
    - "CORS behavior with jsonplaceholder is RESOLVED BY BUILD/inspection (RESEARCH Open Q #2): plain `fetch url []` is used first; if a CORS RequestProperties.Mode is needed it is added and documented — the build still exits 0 regardless"
    - "src/ch11-json-http/index.md follows the authoring template in Korean (개념→예제→실행하기→핵심 포인트), pulls ALL F# code via {{#include}} anchors, uses PLAIN `>` blockquotes (no [!NOTE]), no mermaid fence, wraps F# attributes/flags in backticks, and states the cross-chapter link that manual decoders (not Decode.Auto) are why Ch.13's --noReflection build works; mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch11-json-http/src/App.fs"
      provides: "Elmish app: Todo record + manual todoDecoder, fetchTodo Async<string>, update with Cmd.OfAsync.either fetch+decode, Feliz view rendering Loading/Loaded/Failed, withReactSynchronous mount; per-concept ANCHORs"
      contains: "Decode.object"
    - path: "examples/ch11-json-http/App.fsproj"
      provides: "net10.0 fsproj, RootNamespace Ch11JsonHttp, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0 + Fable.Elmish 5.0.2 + Fable.Elmish.React 5.0.1 + Fable.Fetch 2.7.0 + Feliz 3.3.3 + Thoth.Json 10.5.1"
      contains: "Thoth.Json"
    - path: "examples/ch11-json-http/package.json"
      provides: "dev/build scripts (--verbose), react@^18.3 + react-dom@^18.3 in dependencies, vite ^6 in devDependencies"
      contains: "react-dom"
    - path: "examples/ch11-json-http/index.html"
      provides: "<div id=\"root\"></div> mount node + <script type=module src=./src/App.fs.js>"
      contains: "id=\"root\""
    - path: "src/ch11-json-http/index.md"
      provides: "Full Ch.11 prose: JSON 디코딩 concept (Thoth.Json compositional decoders, manual vs Auto), HTTP concept (Fable.Fetch), async Cmd bridge (Cmd.OfAsync.either), cross-link to --noReflection"
      min_lines: 70
  key_links:
    - from: "src/ch11-json-http/index.md"
      to: "examples/ch11-json-http/src/App.fs"
      via: "{{#include ...:decoder}} (and model/fetch/update/view/program anchors)"
      pattern: "#include.*ch11-json-http/src/App.fs:"
    - from: "examples/ch11-json-http/src/App.fs update"
      to: "HTTP fetch → decode → dispatch Msg"
      via: "Cmd.OfAsync.either fetchTodo url FetchSucceeded (fun ex -> FetchFailed ex.Message)"
      pattern: "Cmd\\.OfAsync\\.either"
    - from: "examples/ch11-json-http/src/App.fs FetchSucceeded"
      to: "F# Todo record"
      via: "Decode.fromString todoDecoder json with Ok/Error match"
      pattern: "Decode\\.fromString"
    - from: "examples/ch11-json-http/src/App.fs fetchTodo"
      to: "real HTTP response body"
      via: "open Fetch; fetch url [] |> Async.AwaitPromise; response.text()"
      pattern: "fetch .*\\[\\]"
    - from: "examples/ch11-json-http/index.html"
      to: "examples/ch11-json-http/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js> + div#root"
      pattern: "src/App\\.fs\\.js"
---

<objective>
Create the standalone examples/ch11-json-http project and write Ch.11 (JSON과 HTTP), which teaches real-world data: fetch JSON from a public HTTP API with Fable.Fetch, decode it with a MANUAL Thoth.Json decoder into an F# record, and render the result — all wired through an Elmish async Cmd (Cmd.OfAsync.either) that bridges the HTTP/decode workflow into the MVU loop established in Ch.8.

Purpose: Satisfies REAL-01 (Thoth.Json + fetch HTTP) and Phase 5 Success Criteria 1 (decode JSON + real HTTP request → display response) and 4 (npm run build exit 0). Establishes the manual-decoder pattern that makes Ch.13's `--noReflection` production build work (Decode.Auto would break it). (REAL-01)
Output: A runnable examples/ch11-json-http that builds with `npm run build` (exit 0); an App.fs with a Todo record, a manual todoDecoder, an Async fetch fn, an Elmish update using Cmd.OfAsync.either (fetch+decode), a Feliz view rendering Loading/Loaded/Failed states; and a complete Korean Ch.11 chapter with code via {{#include}} anchors only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ecosystem-and-real-world-axis/05-RESEARCH.md
@.planning/phases/04-elmish-and-ui-axis/04-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

This is a wave 1 plan (parallel with 05-03 Ch.13; disjoint files). It establishes the Thoth.Json + Fable.Fetch + Elmish async-bridge pattern. It reuses the React 18 + Feliz + Fable.Elmish stack verified in Ch.8 (04-01-SUMMARY).

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 1 scaffolds; Task 2 builds, resolves the CORS open question, and inspects output; Task 3 writes prose quoting verified behavior, then runs mdbook build. Do not reorder or parallelize.

Copy the verified runnable example pattern from Ch.8 (examples/ch08-elmish/) — fsproj at example ROOT, `<Compile Include="src/App.fs" />`, Fable emits src/App.fs.js adjacent to source, index.html loads ./src/App.fs.js, vite.config.js ignores **/*.fs / **/*.fsproj / **/obj/**, NO @vitejs/plugin-react. The shared dotnet tool manifest lives at repo-root `.config/dotnet-tools.json` (Fable 5.3.0 pinned). Name = "ch11-json-http", RootNamespace Ch11JsonHttp.

Established stack (Ch.8, 04-01-SUMMARY — reuse exactly): react/react-dom in `dependencies` (NOT devDependencies — bundler treats devDeps as externals → "Cannot find module 'react'"); react@^18.3 resolves to 18.3.1 (Feliz 3.3.3 does not support React 19); Fable.Elmish 5.0.2 + Fable.Elmish.React 5.0.1 + Feliz 3.3.3 build cleanly; Async.Sleep / Async.AwaitPromise compile fine in Fable 5.3.0; Program.withReactSynchronous "root" mount with matching index.html div#root.

NEW for Ch.11 (vs Ch.8):
- Add Fable.Fetch 2.7.0 and Thoth.Json 10.5.1 PackageReferences.
- Do NOT add Thoth.Fetch (RESEARCH Pitfall 1: it pins Thoth.Json >=6, conflicting with 10.5.1).
- The async Cmd is Cmd.OfAsync.either (NOT perform) so network/decoding errors are handled (RESEARCH Pitfall 7).

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — PLAIN markdown `>` blockquotes only (STATE.md [01-01]).
- mermaid is UNAVAILABLE — no ```mermaid fence; use ASCII/prose inside a plain ```text fence if a diagram is wanted.
- Wrap F# attributes and CLI flags in backticks in prose (e.g. `--noReflection`, `Decode.Auto`) so mdbook does not parse them as HTML (STATE.md [02-03]).
- ALL code via {{#include}} anchors. Include path from src/ch11-json-http/index.md is `../../examples/ch11-json-http/src/App.fs:anchor`.
- `--verbose` required in dev script (Fable #3631 deadlock, STATE.md [Init]).
- Versions (pin EXACTLY, RESEARCH Standard Stack): Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Fable.Fetch 2.7.0, Feliz 3.3.3, Thoth.Json 10.5.1, net10.0, Vite ^6, react@^18.3, react-dom@^18.3.
- `.gitignore` already covers fable_modules/, **/*.fs.js, dist/, node_modules/ — do NOT commit generated files; DO commit package-lock.json.

Thoth.Json + Fable.Fetch + Elmish facts (RESEARCH Pattern 1 + Code Examples + Pitfalls 1/2/6/7/11):
- Manual decoder: `Decode.object (fun get -> { Field = get.Required.Field "key" Decode.int; ... })` returns `Decoder<'T>`. Primitives: `Decode.int`, `Decode.string`, `Decode.bool`, `Decode.float`. Optional: `get.Optional.Field "key" dec` → `'T option`.
- Run a decoder: `Decode.fromString decoder jsonString : Result<'T,string>` — pattern match Ok/Error. Do NOT use `Decode.unsafeFromString` (throws) and do NOT use `Decode.Auto` (reflection → breaks --noReflection in Ch.13).
- Fable.Fetch: `open Fetch` (module is `Fetch`, NOT `Fable.Fetch` — Pitfall 11; do not name any local module Fetch). `fetch url [] : JS.Promise<Response>`. `response.text() : JS.Promise<string>`. Bridge to F# async with `|> Async.AwaitPromise`. Do NOT use `response.json<'T>()` (untyped cast, no validation — Pitfall 6); always get text then Decode.fromString.
- Async fetch fn: `let fetchTodo (url:string) : Async<string> = async { let! r = fetch url [] |> Async.AwaitPromise in let! t = r.text() |> Async.AwaitPromise in return t }`.
- Elmish async Cmd: `Cmd.OfAsync.either fetchTodo url FetchSucceeded (fun ex -> FetchFailed ex.Message)` — runs async, dispatches success Msg OR error Msg (capital OfAsync; Cmd.OfAsync.perform would swallow errors — Pitfall 7).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch11-json-http example + App.fs (manual Thoth decoder + Fable.Fetch + Elmish async Cmd)</name>
  <files>examples/ch11-json-http/App.fsproj, examples/ch11-json-http/package.json, examples/ch11-json-http/vite.config.js, examples/ch11-json-http/index.html, examples/ch11-json-http/src/App.fs</files>
  <action>
Create the example directory mirroring Ch.8's verified layout (examples/ch08-elmish/).

`App.fsproj`: net10.0, RootNamespace Ch11JsonHttp, `<Compile Include="src/App.fs" />`, PackageReferences: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Fable.Fetch 2.7.0, Feliz 3.3.3, Thoth.Json 10.5.1. Do NOT add Thoth.Fetch.

`package.json`: name "ch11-json-http", private, version 0.1.0, type module, scripts dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`. `dependencies`: { "react": "^18.3", "react-dom": "^18.3" }. `devDependencies`: { "vite": "^6.0.0" }.

`vite.config.js`: identical to Ch.8 (defineConfig, server.watch.ignored = ["**/*.fs", "**/*.fsproj", "**/obj/**"]). NO @vitejs/plugin-react.

`index.html`: lang ko, charset UTF-8, viewport meta, title "Ch11 - JSON과 HTTP", body has `<div id="root"></div>` and `<script type="module" src="./src/App.fs.js"></script>`.

`src/App.fs` — Elmish app that fetches + decodes + renders. ANCHORs (each marking a teachable unit): `model`, `decoder`, `fetch`, `update`, `view`, `program`. No `[<EntryPoint>]`. Use this verified skeleton (RESEARCH Pattern 1):
```fsharp
module App

open Elmish
open Elmish.React
open Feliz
open Thoth.Json
open Fetch

// ANCHOR: model
type Todo =
    { Id: int
      UserId: int
      Title: string
      Completed: bool }

type Model =
    | Loading
    | Loaded of Todo
    | Failed of string

type Msg =
    | FetchStarted
    | FetchSucceeded of string   // 응답 본문(raw JSON 문자열)
    | FetchFailed of string

let init () : Model * Cmd<Msg> =
    Loading, Cmd.ofMsg FetchStarted
// ANCHOR_END: model

// ANCHOR: decoder
// 수동 디코더(manual decoder): Decode.Auto는 리플렉션을 쓰므로 --noReflection(13장)과 충돌.
let todoDecoder : Decoder<Todo> =
    Decode.object (fun get ->
        { Id        = get.Required.Field "id"        Decode.int
          UserId    = get.Required.Field "userId"    Decode.int
          Title     = get.Required.Field "title"     Decode.string
          Completed = get.Required.Field "completed" Decode.bool })
// ANCHOR_END: decoder

// ANCHOR: fetch
// HTTP 요청을 F# Async로: fetch는 JS Promise를 반환 → Async.AwaitPromise로 변환.
let fetchTodo (url: string) : Async<string> =
    async {
        let! response = fetch url [] |> Async.AwaitPromise
        let! text = response.text() |> Async.AwaitPromise
        return text
    }
// ANCHOR_END: fetch

// ANCHOR: update
let url = "https://jsonplaceholder.typicode.com/todos/1"

let update (msg: Msg) (model: Model) : Model * Cmd<Msg> =
    match msg with
    | FetchStarted ->
        Loading,
        Cmd.OfAsync.either
            fetchTodo
            url
            FetchSucceeded
            (fun ex -> FetchFailed ex.Message)
    | FetchSucceeded json ->
        match Decode.fromString todoDecoder json with
        | Ok todo   -> Loaded todo, Cmd.none
        | Error err -> Failed (sprintf "디코딩 오류: %s" err), Cmd.none
    | FetchFailed err ->
        Failed (sprintf "HTTP 오류: %s" err), Cmd.none
// ANCHOR_END: update

// ANCHOR: view
let view (model: Model) (dispatch: Msg -> unit) : ReactElement =
    Html.div [
        prop.children [
            Html.h1 [ prop.text "할 일 (jsonplaceholder)" ]
            match model with
            | Loading ->
                Html.p [ prop.text "로딩 중..." ]
            | Loaded todo ->
                Html.div [
                    prop.children [
                        Html.p [ prop.text (sprintf "#%d: %s" todo.Id todo.Title) ]
                        Html.p [ prop.text (if todo.Completed then "완료" else "미완료") ]
                    ]
                ]
            | Failed err ->
                Html.p [
                    prop.style [ style.color "red" ]
                    prop.text err
                ]
        ]
    ]
// ANCHOR_END: view

// ANCHOR: program
Program.mkProgram init update view
|> Program.withReactSynchronous "root"
|> Program.run
// ANCHOR_END: program
```
Keep focused on JSON decode + HTTP fetch + Elmish async bridge. Use a MANUAL decoder only (no Decode.Auto). Use Cmd.OfAsync.either (not perform). Do NOT add Thoth.Fetch.
  </action>
  <verify>
`test -f examples/ch11-json-http/App.fsproj && test -f examples/ch11-json-http/src/App.fs && test -f examples/ch11-json-http/index.html && test -f examples/ch11-json-http/package.json && test -f examples/ch11-json-http/vite.config.js`. Anchors present: `for a in model decoder fetch update view program; do grep -q "ANCHOR: $a" examples/ch11-json-http/src/App.fs || exit 1; done`. Manual decoder (not Auto): `grep -q 'Decode.object' examples/ch11-json-http/src/App.fs && grep -q 'Decode.fromString' examples/ch11-json-http/src/App.fs && ! grep -q 'Decode.Auto' examples/ch11-json-http/src/App.fs`. No unsafe decode: `! grep -q 'unsafeFromString' examples/ch11-json-http/src/App.fs`. Real HTTP via Fable.Fetch: `grep -q 'open Fetch' examples/ch11-json-http/src/App.fs && grep -q 'jsonplaceholder.typicode.com' examples/ch11-json-http/src/App.fs && grep -q 'AwaitPromise' examples/ch11-json-http/src/App.fs`. Error-handling Cmd: `grep -q 'Cmd.OfAsync.either' examples/ch11-json-http/src/App.fs`. Packages pinned + no Thoth.Fetch: `grep -q 'Thoth.Json' examples/ch11-json-http/App.fsproj && grep -q 'Fable.Fetch' examples/ch11-json-http/App.fsproj && ! grep -q 'Thoth.Fetch' examples/ch11-json-http/App.fsproj`. Mount: `grep -q 'withReactSynchronous' examples/ch11-json-http/src/App.fs && grep -q 'id="root"' examples/ch11-json-http/index.html`. react in dependencies: `grep -q '"react"' examples/ch11-json-http/package.json && grep -q '"react-dom"' examples/ch11-json-http/package.json`. `grep -q -- '--verbose' examples/ch11-json-http/package.json`. `! grep -q 'EntryPoint' examples/ch11-json-http/src/App.fs`.
  </verify>
  <done>examples/ch11-json-http scaffolded; App.fs has a Todo record + manual todoDecoder (Decode.object/get.Required.Field), an Async fetchTodo using Fable.Fetch (open Fetch, fetch url [], response.text(), AwaitPromise), an Elmish update with Cmd.OfAsync.either (fetch + Decode.fromString with Ok/Error), a Feliz view rendering Loading/Loaded/Failed, and withReactSynchronous "root" mount; no Decode.Auto, no unsafeFromString, no Thoth.Fetch, no EntryPoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build, RESOLVE CORS open question, inspect output</name>
  <files>examples/ch11-json-http/App.fsproj, examples/ch11-json-http/src/App.fs (only if a CORS RequestProperties fix is needed), examples/ch11-json-http/package-lock.json, examples/ch11-json-http/ (build + inspect)</files>
  <action>
From `examples/ch11-json-http/`:
1. `dotnet tool restore` (exit 0 — resolves Fable 5.3.0 from repo-root .config/dotnet-tools.json), `npm install` (exit 0 — installs react@^18.3, react-dom@^18.3, vite). This generates package-lock.json.
2. `npm run build` (`dotnet fable --run npx vite build`) — must exit 0. Fix any compile errors until exit 0. In particular, confirm NuGet resolved Thoth.Json to 10.5.1 (NOT an old 6.x pulled by a stray dep — RESEARCH Pitfall 1); if a version conflict appears, ensure Thoth.Fetch is absent and re-restore.

OPEN QUESTION #2 — CORS with jsonplaceholder (resolve by build/inspection, do NOT assume):
3. The skeleton uses plain `fetch url []` (no RequestProperties). The build does not exercise the network, so the build exits 0 regardless of CORS. jsonplaceholder.typicode.com sends `Access-Control-Allow-Origin: *`, so plain `fetch url []` is expected to work in the browser. KEEP `fetch url []` as the default. RECORD for Task 3 + SUMMARY: the default is plain fetch; if a reader hits a CORS error, adding `[ RequestProperties.Mode RequestMode.Cors ]` is the documented remedy (this is a browser-runtime concern, human-verify, NOT a build failure). Do NOT add the Mode property unless a compile error forces it.

INSPECT `examples/ch11-json-http/src/App.fs.js` and record for Task 3:
- Confirm the file exists and the Elmish program is emitted (mkProgram / withReactSynchronous present).
- Confirm the Thoth decoder and fetch call are present in output (e.g. a `Decode`/`fromString`-derived call and a `fetch(`/`text()` call).
- Confirm the resolved react/react-dom version from package-lock.json (expected 18.3.1, consistent with Ch.8).

Write findings for Task 3 + SUMMARY. Do NOT commit generated files (fable_modules/, *.fs.js, dist/, node_modules/); DO leave package-lock.json staged for commit.
  </action>
  <verify>
Final `cd examples/ch11-json-http && npm run build` exits 0. `test -f examples/ch11-json-http/src/App.fs.js && test -f examples/ch11-json-http/package-lock.json`. Thoth.Json 10.5.1 resolved (no 6.x conflict): build exit 0 confirms; `grep -q '10.5.1' examples/ch11-json-http/App.fsproj`. App.fs.js emitted with fetch + decode: `grep -q 'fetch' examples/ch11-json-http/src/App.fs.js`. CORS outcome RECORDED (plain fetch kept, or Mode added with reason) and react version captured from package-lock.json: `grep -q 'react' examples/ch11-json-http/package-lock.json`. Findings recorded for Task 3.
  </verify>
  <done>Example builds (npm run build exit 0); Thoth.Json resolved to 10.5.1 with no Thoth.Fetch conflict; the CORS open question (Open Q #2) is resolved — plain `fetch url []` kept as default, the RequestMode.Cors remedy documented as a browser-runtime note; package-lock.json committed; App.fs.js inspected (Elmish program + fetch + Thoth decode present).</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.11 chapter prose (JSON 디코딩 + HTTP + async Cmd) and verify render</name>
  <files>src/ch11-json-http/index.md</files>
  <action>
Fully replace the stub `src/ch11-json-http/index.md` following the authoring template. H1 `# 11장. JSON과 HTTP (JSON and HTTP)`, then a PLAIN `>` blockquote (NOT [!NOTE]) pointing to examples/ch11-json-http + run command (`cd examples/ch11-json-http && dotnet tool restore && npm install && npm run dev`). Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION: Use the actual build outcomes recorded in Task 2 (CORS default, resolved versions). Do not claim behavior that did not build.

- 개념 (Concept) — PROSE ONLY, NO code blocks: (1) Explain why JSON decoding needs a decoder in Fable: a raw `JSON.parse` object is not a real F# record (Fable records are ES6 classes), so you describe how to read fields and build a typed value. Introduce Thoth.Json's compositional decoder (decoder) model — `Decode.object` + `get.Required.Field`, and `Decode.fromString` returning `Result<'T,string>` (Ok/Error). (2) Contrast MANUAL decoders with `Decode.Auto`: Auto generates a decoder from the type via reflection (reflection) — convenient but it BREAKS under `--noReflection` (the production flag taught in 13장), and it is less explicit. State the cross-chapter link plainly: this chapter uses manual decoders precisely so the 13장 `--noReflection` build keeps working. (Wrap `Decode.Auto` and `--noReflection` in backticks.) (3) Explain HTTP from Fable: `Fable.Fetch` wraps the browser Fetch API; `fetch url []` returns a JS Promise (Promise), and you bridge it into F# `Async` with `Async.AwaitPromise`; read the body with `response.text()`. Warn (Pitfall 6) NOT to use `response.json<'T>()` — it casts without validation. (4) Explain the Elmish bridge: the fetch+decode is a side effect, so update returns `Cmd.OfAsync.either` which runs the async and dispatches a success Msg or a failure Msg — use `either` (not `perform`) so network/decode errors are surfaced as a Failed state (Pitfall 7). Optionally include a short ASCII flow (FetchStarted → async fetch → FetchSucceeded(json) → decode → Loaded/Failed) in a plain ```text fence (NOT mermaid).
- 예제 (Example): Intro sentence, then include each anchor in its own fenced fsharp block with a 1–2 sentence Korean explanation:
  ```fsharp
  {{#include ../../examples/ch11-json-http/src/App.fs:model}}
  ```
  (repeat for decoder, fetch, update, view, program). For the `decoder` anchor, explain `Decode.object` + `get.Required.Field "id" Decode.int` builds a `Decoder<Todo>` and reiterate it is manual (not `Decode.Auto`). For `fetch`, explain the Promise→Async bridge. For `update`, explain `Cmd.OfAsync.either fetchTodo url FetchSucceeded (fun ex -> FetchFailed ex.Message)` and that FetchSucceeded runs `Decode.fromString todoDecoder json` and pattern-matches Ok/Error. For `program`, note withReactSynchronous "root" matches index.html div#root.
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` → `npm run dev`; open the browser — the app shows "로딩 중..." briefly, then displays the fetched todo (#1 and its title and 완료/미완료) once the real HTTP request to jsonplaceholder returns and decoding succeeds; if the network fails or the JSON shape is unexpected, a red error message (HTTP 오류 / 디코딩 오류) appears. Mention (per Task 2 CORS note) that jsonplaceholder is CORS-enabled so plain fetch works; if a reader's own API rejects CORS, add `RequestProperties.Mode RequestMode.Cors`. Do NOT paste raw .fs.js or terminal output.
- 핵심 포인트 (Key Points): 6–7 bullets: Thoth.Json 수동 디코더(Decode.object/get.Required.Field); Decode.fromString는 Result(Ok/Error)로 안전; `Decode.Auto`는 리플렉션 → `--noReflection`(13장)과 충돌하므로 수동 디코더 사용 (cross-link); Fable.Fetch의 fetch는 Promise → Async.AwaitPromise로 변환; response.text() 후 디코딩 (response.json<'T>()는 검증 없음); 부수효과는 Cmd.OfAsync.either로 분리 (성공/실패 둘 다 Msg로); Thoth.Fetch는 사용 안 함 (Thoth.Json 버전 충돌).

Then run `mdbook build` and confirm Ch.11 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch11-json-http/index.html`. Anchors resolved in rendered HTML: `grep -q 'Decode.object' book/ch11-json-http/index.html && grep -q 'Cmd.OfAsync.either' book/ch11-json-http/index.html && grep -q 'fetchTodo' book/ch11-json-http/index.html`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch11-json-http/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch11-json-http/index.md`. No mermaid fence: `! grep -q '```mermaid' src/ch11-json-http/index.md`. Cross-link present: `grep -q 'noReflection' src/ch11-json-http/index.md`. All code via include: `grep -c '#include' src/ch11-json-http/index.md` returns >= 6. 개념 prose-only + attributes backtick-wrapped: manual review.
  </verify>
  <done>src/ch11-json-http/index.md follows the template in Korean, 개념 prose-only (no code), includes the six anchors (model/decoder/fetch/update/view/program), explains Thoth manual decoding + Fable.Fetch Promise→Async + Cmd.OfAsync.either, states the `--noReflection` cross-link, wraps F# attributes/flags in backticks, uses plain blockquotes, no mermaid; mdbook build renders the chapter with anchors resolved.</done>
</task>

</tasks>

<verification>
- `cd examples/ch11-json-http && dotnet tool restore && npm install && npm run build` exits 0, producing src/App.fs.js (SC-1 + SC-4 + phase-wide build gate).
- App.fs fetches real JSON over HTTP (Fable.Fetch, jsonplaceholder), decodes with a MANUAL Thoth.Json decoder into a Todo record, renders Loading/Loaded/Failed via Feliz, mounted with withReactSynchronous "root".
- Async Cmd uses Cmd.OfAsync.either (error-handling), decode uses Decode.fromString with Ok/Error; no Decode.Auto, no unsafeFromString, no Thoth.Fetch.
- Open Q #2 (CORS) resolved: plain fetch kept by default; RequestMode.Cors documented as a browser-runtime remedy. Thoth.Json resolved to 10.5.1 with no version conflict. package-lock.json committed.
- src/ch11-json-http/index.md follows the authoring template, Korean, 개념 prose-only, code via {{#include}} only, plain blockquotes, no mermaid, F# attributes/flags backtick-wrapped, states the --noReflection cross-link.
- `mdbook build` exits 0 and book/ch11-json-http/index.html contains the anchored code.
</verification>

<success_criteria>
REAL-01 satisfied and Phase 5 Success Criteria 1 + 4 met: a reader runs the Ch.11 example and sees the app perform a real HTTP request to a public API, decode the JSON response with a manual Thoth.Json decoder into an F# record, and display the data (transitioning Loading → Loaded, with Failed on error). The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors and a stated cross-link to Ch.13's --noReflection.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ecosystem-and-real-world-axis/05-01-SUMMARY.md` recording: the example files created, the verified build output (npm run build exit 0), the resolution of Open Q #2 (CORS — plain fetch kept; RequestMode.Cors as remedy), the resolved Thoth.Json (10.5.1) / Fable.Fetch (2.7.0) / react versions, and confirmation that the manual decoder (not Decode.Auto) is the deliberate choice enabling Ch.13's --noReflection build. Note for Ch.13 (05-03): this example's manual decoders make it --noReflection-safe.
</output>
