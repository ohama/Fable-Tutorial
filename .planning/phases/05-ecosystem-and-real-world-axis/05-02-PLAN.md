---
phase: 05-ecosystem-and-real-world-axis
plan: "02"
type: execute
wave: 2
depends_on: []
files_modified:
  - examples/ch12-testing/Tests.fsproj
  - examples/ch12-testing/package.json
  - examples/ch12-testing/src/Tests.fs
  - examples/ch12-testing/package-lock.json
  - src/ch12-testing/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `cd examples/ch12-testing && dotnet tool restore && npm install && npm test` and it exits 0 with all tests passing (SC-2)"
    - "Reader runs `cd examples/ch12-testing && npm run build` (Fable compile of Tests.fsproj) and it exits 0 (SC-4)"
    - "Fable.Mocha 2.17.0 Fable 5.3.0 compatibility is RESOLVED BUILD-FIRST (RESEARCH Open Q #1): the example is compiled+run BEFORE prose is written; if Fable.Mocha 2.17.0 fails to compile/run, the RESEARCH ranked fallback (A→B→C→D) is applied and the working approach is documented"
    - "examples/ch12-testing/src/Tests.fs tests a PURE Elmish update function (Msg -> Model -> Model, no Cmd in the tested signature — RESEARCH Pitfall 8: Cmd<Msg> is not structurally comparable) using whatever assertion library actually ran (Fable.Mocha Expect.equal if it works, or the fallback)"
    - "mocha is pinned to EXACTLY 9.2.0 (NOT ^9.2.0, NOT 10.x — RESEARCH Pitfall 3: mocha 10 + ESM output → ERR_REQUIRE_ESM) unless Fallback A explicitly changes the runner, in which case the working version is documented"
    - "The Ch.12 project structure is the EXCEPTION: NO index.html, NO vite.config.js, NO react/vite deps — a pure Node/mocha test project (RESEARCH Pattern 3); package.json has a pretest (Fable compile) + test (mocha) lifecycle, no Vite"
    - "The Fable compile + mocha run command uses the Fable 5 form `dotnet fable Tests.fsproj -o dist` (NOT the Fable 2 `dotnet fable tests -o dist/tests` — Pitfall 4) and mocha targets `dist/*.fs.js` (NOT the whole dist dir — Pitfall 5: avoids fable_modules glob)"
    - "src/ch12-testing/index.md follows the authoring template in Korean (개념→예제→실행하기→핵심 포인트), pulls F# code via {{#include}} anchors, uses PLAIN `>` blockquotes (no [!NOTE]), no mermaid fence, wraps F# attributes in backticks, and TEACHES WHATEVER ACTUALLY RAN (Fable.Mocha or the fallback) — never claims behavior that did not run; mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch12-testing/src/Tests.fs"
      provides: "Pure Elmish update function (Model/Msg/update returning Model) + a testList of testCase assertions over update; Mocha.runTests entry (or fallback runner); ANCHOR markers (logic, tests)"
      contains: "testList"
    - path: "examples/ch12-testing/Tests.fsproj"
      provides: "net10.0 fsproj, RootNamespace Ch12Testing, OutputType Exe, Fable.Core 5.0.0 + Fable.Mocha 2.17.0 (or fallback packages if needed)"
      contains: "Fable.Mocha"
    - path: "examples/ch12-testing/package.json"
      provides: "type module; pretest (dotnet fable Tests.fsproj -o dist) + test (npx mocha dist/*.fs.js --timeout) + build scripts; mocha 9.2.0 devDependency; NO vite/react"
      contains: "pretest"
    - path: "src/ch12-testing/index.md"
      provides: "Full Ch.12 prose: why test pure update functions, Fable.Mocha (or fallback) testList/testCase/Expect.equal, npm test lifecycle, Pitfall 8 (test Model not Cmd)"
      min_lines: 70
  key_links:
    - from: "src/ch12-testing/index.md"
      to: "examples/ch12-testing/src/Tests.fs"
      via: "{{#include ...:logic}} and {{#include ...:tests}}"
      pattern: "#include.*ch12-testing/src/Tests.fs:"
    - from: "examples/ch12-testing/package.json test"
      to: "compiled Fable test JS run by mocha"
      via: "pretest: dotnet fable Tests.fsproj -o dist; test: npx mocha dist/*.fs.js"
      pattern: "dist/\\*\\.fs\\.js"
    - from: "examples/ch12-testing/src/Tests.fs tests"
      to: "pure update function"
      via: "testCase asserts update Msg model equals expected Model (not Model*Cmd)"
      pattern: "testCase"
    - from: "examples/ch12-testing/src/Tests.fs"
      to: "mocha test registration"
      via: "Mocha.runTests tests |> ignore (or fallback runner)"
      pattern: "runTests|describe"
---

<objective>
Create the standalone examples/ch12-testing project (a pure Node/mocha test project — the structural EXCEPTION to the chapter pattern) and write Ch.12 (테스팅), which teaches testing a PURE Elmish update function with Fable.Mocha, run via `npm test`. Because Fable.Mocha 2.17.0 has never been verified against Fable 5.3.0, this plan is BUILD-FIRST: the example is compiled and run BEFORE any prose is written; if Fable.Mocha fails, a ranked fallback is applied and the chapter teaches whatever actually ran.

Purpose: Satisfies REAL-02 (Fable.Mocha test authoring + execution) and Phase 5 Success Criteria 2 (Fable.Mocha tests of a pure update function pass via `npm test`) and 4 (npm run build exit 0). Resolves RESEARCH Open Q #1 (Fable.Mocha Fable 5 compat) empirically. (REAL-02)
Output: A runnable examples/ch12-testing where `npm test` exits 0 with passing tests; a Tests.fs with a pure update function and a testList of assertions; and a complete Korean Ch.12 chapter (teaching the verified approach) with code via {{#include}} anchors only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ecosystem-and-real-world-axis/05-RESEARCH.md
@.planning/phases/04-elmish-and-ui-axis/04-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

This is a wave 2 plan, INTENTIONALLY isolated AFTER wave 1 (Ch.11 + Ch.13) because Fable.Mocha 2.17.0 + Fable 5.3.0 is the single highest-risk item in this phase (RESEARCH Open Q #1, STATE.md Research Flag "Phase 5 Ch.12: Fable.Mocha + Fable 5 호환성 미확인"). depends_on is empty (it shares no files with Ch.11/13 and does not reuse their code), but wave is 2 to isolate the risk and keep the parallel wave-1 work clean.

THIS CHAPTER IS THE STRUCTURAL EXCEPTION (RESEARCH Pattern 3): tests run in Node via mocha, NOT in a browser via Vite. There is NO index.html, NO vite.config.js, NO react, NO react-dom, NO @vitejs/plugin-react, NO Feliz. Do NOT copy the browser example pattern's vite/react bits.

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 1 scaffolds the test project; Task 2 is the BUILD-FIRST verification + ranked-fallback decision gate (compile + run mocha; if it fails, apply A→B→C→D and document); Task 3 writes prose describing ONLY what actually ran. Do not reorder or parallelize. Prose MUST NOT be written before Task 2 confirms what works.

The shared dotnet tool manifest lives at repo-root `.config/dotnet-tools.json` (Fable 5.3.0 pinned) — `dotnet tool restore` from the example dir resolves it. Name = "ch12-testing", RootNamespace Ch12Testing.

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — PLAIN markdown `>` blockquotes only (STATE.md [01-01]).
- mermaid is UNAVAILABLE — no ```mermaid fence.
- Wrap F# attributes/flags in backticks in prose if mentioned, so mdbook does not parse them as HTML (STATE.md [02-03]).
- ALL code via {{#include}} anchors. Include path from src/ch12-testing/index.md is `../../examples/ch12-testing/src/Tests.fs:anchor`.
- Versions (pin EXACTLY, RESEARCH Standard Stack): Fable.Core 5.0.0, Fable.Mocha 2.17.0, net10.0, mocha 9.2.0 (EXACT pin — Pitfall 3). NO Vite, NO react.
- `.gitignore` already covers fable_modules/, **/*.fs.js, dist/, node_modules/ — do NOT commit generated files; DO commit package-lock.json.
- The "실행하기" gate command is `npm test` (NOT `npm run dev`), since this is a test project. The blockquote run command should read `cd examples/ch12-testing && dotnet tool restore && npm install && npm test`.

Fable.Mocha + Fable 5 facts (RESEARCH Pattern 3 + Code Examples + Pitfalls 3/4/5/8/10/12 + Open Q #1):
- Tests.fsproj: net10.0, OutputType Exe, Compile Include src/Tests.fs, PackageReferences Fable.Core 5.0.0 + Fable.Mocha 2.17.0.
- package.json (type module, NO vite/react): scripts pretest = `dotnet fable Tests.fsproj -o dist`, test = `npx mocha dist/*.fs.js --timeout 10000`, build = `dotnet fable Tests.fsproj -o dist`. devDependencies { "mocha": "9.2.0" } (EXACT, not caret).
- `pretest` runs automatically before `test` (npm lifecycle). Fable 5 compile form is `dotnet fable Tests.fsproj -o dist` (Pitfall 4: NOT Fable 2's `dotnet fable tests -o dist/tests`). mocha targets `dist/*.fs.js` not the whole dir (Pitfall 5: avoids running fable_modules JS).
- Tests.fs (RESEARCH Pattern 3): `open Fable.Mocha`; define a PURE update (Msg -> Model -> Model, returning Model NOT Model*Cmd — Pitfall 8); `testList "name" [ testCase "case" <| fun () -> Expect.equal actual expected "msg" ]`; end with `Mocha.runTests tests |> ignore` (Pitfall 10: must |> ignore).
- mocha 9.2.0 EXACT pin (Pitfall 3): mocha 10.x + ESM output → ERR_REQUIRE_ESM.

RANKED FALLBACKS if Fable.Mocha 2.17.0 fails to compile or run (RESEARCH Open Q #1) — apply in order, stop at the first that works, DOCUMENT which was used and why:
- Fallback A (PREFERRED if runtime/ESM failure): change the mocha invocation/version only — e.g. `"test": "NODE_OPTIONS='--experimental-vm-modules' npx mocha dist/*.fs.js --timeout 10000"`, or try mocha 10.x with `--experimental-specifier-resolution=node`. Document the working runner.
- Fallback B: change mocha discovery to an explicit file — `npx mocha dist/Tests.fs.js --timeout 10000` (not a glob/dir).
- Fallback C: Expecto via conditional compilation (`#if FABLE_COMPILER` Fable.Mocha path / `#else` Expecto + dotnet run). More complex; only if A/B fail. If used, the chapter teaches the Fable.Mocha path as primary and notes the .NET path.
- Fallback D: plain mocha bindings — bind mocha's `describe`/`it` via `[<Global>]`/`[<Emit>]` in F#, no Fable.Mocha wrapper. Most work; last resort. If used, the chapter teaches these bindings.
DECISION GATE: Task 2 MUST record the outcome — Fable.Mocha 2.17.0 worked (proceed with Pattern 3), or which fallback was applied. Task 3 prose teaches the approach that actually ran.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch12-testing pure-Node test project (Tests.fsproj + Tests.fs + package.json, NO vite/react)</name>
  <files>examples/ch12-testing/Tests.fsproj, examples/ch12-testing/package.json, examples/ch12-testing/src/Tests.fs</files>
  <action>
Create examples/ch12-testing/ as a PURE test project (RESEARCH Pattern 3). Do NOT create index.html or vite.config.js. Do NOT add react/react-dom/vite/Feliz.

`Tests.fsproj`: net10.0, RootNamespace Ch12Testing, `<OutputType>Exe</OutputType>`, `<Compile Include="src/Tests.fs" />`, PackageReferences: Fable.Core 5.0.0, Fable.Mocha 2.17.0.

`package.json`: name "ch12-testing", private, version 0.1.0, type module, scripts:
- pretest = `dotnet fable Tests.fsproj -o dist`
- test = `npx mocha dist/*.fs.js --timeout 10000`
- build = `dotnet fable Tests.fsproj -o dist`
devDependencies: { "mocha": "9.2.0" } (EXACT pin, NO caret). NO vite, NO react.

`src/Tests.fs` — a PURE Elmish update function tested with Fable.Mocha. update returns Model (NOT Model*Cmd — Pitfall 8). ANCHORs: `logic` (the Model/Msg/update), `tests` (the testList + runner). No `[<EntryPoint>]` for the Fable path (module top-level Mocha.runTests is the entry). Use this verified skeleton (RESEARCH Pattern 3):
```fsharp
module Tests

open Fable.Mocha

// ANCHOR: logic
// 테스트 대상: 순수 update 함수 (Cmd 없이 Model만 반환 — Cmd는 구조적 비교 불가)
type Model = { Count: int }
type Msg = Increment | Decrement | Reset

let update (msg: Msg) (model: Model) : Model =
    match msg with
    | Increment -> { model with Count = model.Count + 1 }
    | Decrement -> { model with Count = model.Count - 1 }
    | Reset     -> { model with Count = 0 }
// ANCHOR_END: logic

// ANCHOR: tests
let counterTests =
    testList "카운터 update 함수" [
        testCase "초기값이 0이다" <| fun () ->
            let model = { Count = 0 }
            Expect.equal model.Count 0 "초기값은 0"

        testCase "Increment가 카운트를 1 증가시킨다" <| fun () ->
            let next = update Increment { Count = 5 }
            Expect.equal next.Count 6 "5 + 1 = 6"

        testCase "Decrement가 카운트를 1 감소시킨다" <| fun () ->
            let next = update Decrement { Count = 3 }
            Expect.equal next.Count 2 "3 - 1 = 2"

        testCase "Reset이 카운트를 0으로 초기화한다" <| fun () ->
            let next = update Reset { Count = 42 }
            Expect.equal next.Count 0 "Reset -> 0"

        testCase "여러 Msg를 순서대로 적용한다" <| fun () ->
            let model =
                { Count = 0 }
                |> update Increment
                |> update Increment
                |> update Decrement
            Expect.equal model.Count 1 "0 + 1 + 1 - 1 = 1"
    ]

Mocha.runTests counterTests |> ignore
// ANCHOR_END: tests
```
Test the PURE update (returns Model). Pin mocha EXACTLY to 9.2.0. Do NOT create index.html/vite.config.js. Do NOT add vite/react.
  </action>
  <verify>
`test -f examples/ch12-testing/Tests.fsproj && test -f examples/ch12-testing/src/Tests.fs && test -f examples/ch12-testing/package.json`. NO browser files: `! test -f examples/ch12-testing/index.html && ! test -f examples/ch12-testing/vite.config.js`. Anchors present: `grep -q 'ANCHOR: logic' examples/ch12-testing/src/Tests.fs && grep -q 'ANCHOR: tests' examples/ch12-testing/src/Tests.fs`. Pure update (returns Model, testable): `grep -q 'testList' examples/ch12-testing/src/Tests.fs && grep -q 'Mocha.runTests' examples/ch12-testing/src/Tests.fs && grep -q '|> ignore' examples/ch12-testing/src/Tests.fs`. Packages: `grep -q 'Fable.Mocha' examples/ch12-testing/Tests.fsproj && grep -q 'OutputType' examples/ch12-testing/Tests.fsproj`. Scripts + mocha exact pin + no vite/react: `grep -q 'pretest' examples/ch12-testing/package.json && grep -q 'Tests.fsproj -o dist' examples/ch12-testing/package.json && grep -q 'dist/\*.fs.js' examples/ch12-testing/package.json && grep -q '"9.2.0"' examples/ch12-testing/package.json && ! grep -q 'vite' examples/ch12-testing/package.json && ! grep -q 'react' examples/ch12-testing/package.json`. Not Fable 2 form: `! grep -q 'fable tests -o' examples/ch12-testing/package.json`.
  </verify>
  <done>examples/ch12-testing scaffolded as a pure Node/mocha test project (NO index.html/vite/react): Tests.fsproj (net10.0, OutputType Exe, Fable.Core 5.0.0 + Fable.Mocha 2.17.0); Tests.fs with a PURE update (Msg->Model->Model) + a testList of testCase/Expect.equal assertions + Mocha.runTests |> ignore + logic/tests anchors; package.json with pretest (Fable 5 compile) + test (mocha dist/*.fs.js) + build scripts and mocha pinned EXACTLY to 9.2.0.</done>
</task>

<task type="auto">
  <name>Task 2: BUILD-FIRST verification + ranked-fallback decision gate (compile + run mocha)</name>
  <files>examples/ch12-testing/Tests.fsproj, examples/ch12-testing/package.json, examples/ch12-testing/src/Tests.fs (only if a fallback requires changes), examples/ch12-testing/package-lock.json, examples/ch12-testing/ (build + run)</files>
  <action>
This is the DECISION GATE for the phase's highest risk (RESEARCH Open Q #1). Do NOT write any chapter prose until this task confirms what works.

From `examples/ch12-testing/`:
1. `dotnet tool restore` (exit 0). `npm install` (exit 0 — installs mocha@9.2.0). Generates package-lock.json.
2. COMPILE check: `npm run build` (`dotnet fable Tests.fsproj -o dist`) — must exit 0. This verifies Fable.Mocha 2.17.0 COMPILES under Fable 5.3.0 (RESEARCH assessment: LOW-MEDIUM risk; Fable.Core.Testing.Assert is present in Fable.Core 5.0.0). Confirm dist/Tests.fs.js is emitted.
3. RUN check: `npm test` (runs pretest = Fable compile, then test = `npx mocha dist/*.fs.js --timeout 10000`) — must exit 0 with all 5 tests passing.

IF BOTH succeed (most likely): record "Fable.Mocha 2.17.0 works under Fable 5.3.0" — proceed; no fallback. Capture the mocha output summary (N passing).

IF COMPILE (step 2) or RUN (step 3) FAILS: apply the RESEARCH ranked fallback IN ORDER, stop at the first that makes `npm test` exit 0, and DOCUMENT which fallback + why:
- Fallback A (PREFERRED for ESM/runtime failures, e.g. ERR_REQUIRE_ESM — Pitfall 3): adjust the mocha runner/version. Try `"test": "NODE_OPTIONS='--experimental-vm-modules' npx mocha dist/*.fs.js --timeout 10000"`; or try mocha 10.x with `--experimental-specifier-resolution=node`. Update package.json; re-lock.
- Fallback B: explicit file discovery — `"test": "npx mocha dist/Tests.fs.js --timeout 10000"` (avoids dir/glob picking up fable_modules — Pitfall 5).
- Fallback C: Expecto via `#if FABLE_COMPILER` (Fable.Mocha) / `#else` (Expecto + `dotnet run`). Add Expecto PackageReference; provide both run paths. Use ONLY if A/B fail.
- Fallback D: plain mocha `describe`/`it` bindings via `[<Global>]`/`[<Emit>]`, no Fable.Mocha. Last resort.

After resolution, the FINAL state MUST be: `npm test` exits 0 with passing tests, and `npm run build` exits 0. Record for Task 3 + SUMMARY: which approach ran (Fable.Mocha 2.17.0 / Fallback A/B/C/D), the exact final test command, the resolved mocha version, and any code changes the fallback required (so the prose matches the actual Tests.fs).

Do NOT commit generated files (fable_modules/, dist/, *.fs.js, node_modules/); DO leave package-lock.json staged for commit.
  </action>
  <verify>
`cd examples/ch12-testing && npm run build` exits 0 (Fable compile of Tests.fsproj). `cd examples/ch12-testing && npm test` exits 0 with passing tests (pretest compiles, mocha runs). `test -f examples/ch12-testing/dist/Tests.fs.js && test -f examples/ch12-testing/package-lock.json`. Outcome RECORDED: Fable.Mocha 2.17.0 worked OR a documented fallback (A/B/C/D) was applied — and Tests.fs / package.json reflect whatever ran (`grep -q 'mocha' examples/ch12-testing/package.json`). Findings (approach used, final test command, mocha version) recorded for Task 3.
  </verify>
  <done>BUILD-FIRST gate passed: Fable.Mocha 2.17.0 compiles under Fable 5.3.0 and `npm test` exits 0 with all tests passing — OR the first working RESEARCH ranked fallback (A→B→C→D) was applied and documented. `npm run build` exits 0. The exact working approach, final test command, and resolved mocha version are recorded for the prose; package-lock.json committed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.12 chapter prose (testing pure update with what actually ran) and verify render</name>
  <files>src/ch12-testing/index.md</files>
  <action>
Fully replace the stub `src/ch12-testing/index.md` following the authoring template. H1 `# 12장. 테스팅 (Testing)`, then a PLAIN `>` blockquote (NOT [!NOTE]) pointing to examples/ch12-testing + run command (`cd examples/ch12-testing && dotnet tool restore && npm install && npm test`). Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION (HARD): Use ONLY what Task 2 actually ran. If Fable.Mocha 2.17.0 worked, teach Fable.Mocha (testList/testCase/Expect.equal/Mocha.runTests). If a fallback was applied, teach THAT approach and its exact commands instead. NEVER describe Fable.Mocha behavior that did not run. Match the prose to the final Tests.fs and package.json on disk.

- 개념 (Concept) — PROSE ONLY, NO code blocks: (1) Explain WHY pure functions are easy to test: an Elmish update is a pure function (pure function) of (Msg, Model) → Model — same input always gives same output, no side effects — so you can assert `update msg model = expected` directly. (2) CRITICAL design point (Pitfall 8): a real Elmish update returns `Model * Cmd<Msg>`, but `Cmd<Msg>` is a list of functions and is NOT structurally comparable (구조적 비교 불가), so `Expect.equal` on a Cmd throws — therefore you test a PURE update that returns just `Model` (split the model logic from the Cmd, test the model part). (3) Introduce the test runner that actually ran: if Fable.Mocha — explain it is the Fable-native test library that compiles F# tests to JS run by mocha (mocha) on Node; `testList` groups, `testCase` defines a case, `Expect.equal actual expected msg` asserts, `Mocha.runTests` registers with mocha (and must be `|> ignore`). If a fallback ran, describe that runner instead. (4) Explain the `npm test` lifecycle: `pretest` compiles the F# tests with Fable (`dotnet fable Tests.fsproj -o dist`), then `test` runs mocha over the compiled `dist/*.fs.js`. Note this chapter's project is different from the others — it is a Node test project with NO browser/Vite.
- 예제 (Example): Intro sentence, then include each anchor in its own fenced fsharp block with a 1–2 sentence Korean explanation:
  ```fsharp
  {{#include ../../examples/ch12-testing/src/Tests.fs:logic}}
  ```
  (then `tests`). For `logic`, explain it is the pure update returning Model (no Cmd). For `tests`, explain testList/testCase/Expect.equal and the final runner line. If a fallback changed the code, the anchors still resolve to the on-disk Tests.fs — describe what is there.
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` → `npm test`; explain the pretest→test lifecycle (Fable compiles to dist/, mocha runs the compiled tests) and that all cases pass (describe the expected pass result in prose; do NOT paste raw mocha output). State the exact test command form that Task 2 confirmed working (the default `mocha dist/*.fs.js`, or the fallback command). If a fallback was used, briefly note why (e.g. ESM/mocha-version reason) so readers understand the pin.
- 핵심 포인트 (Key Points): 5–7 bullets: Elmish update는 순수 함수 → 테스트 쉬움 (input→output 단언); `Cmd<Msg>`는 구조적 비교 불가 → Model만 반환하는 순수 update를 테스트 (Pitfall 8); 사용한 러너(Fable.Mocha 또는 폴백)의 testList/testCase/Expect.equal; `npm test`의 pretest(Fable 컴파일)→test(mocha) 라이프사이클; mocha는 정확히 9.2.0 핀 (10.x는 ESM 오류) — 또는 폴백에서 쓴 버전/이유; 이 프로젝트는 브라우저/Vite 없는 Node 테스트 프로젝트.

Then run `mdbook build` and confirm Ch.12 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch12-testing/index.html`. Anchors resolved in rendered HTML: `grep -q 'testList' book/ch12-testing/index.html && grep -q 'update' book/ch12-testing/index.html`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch12-testing/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch12-testing/index.md`. No mermaid fence: `! grep -q '```mermaid' src/ch12-testing/index.md`. Pure-update/Cmd note present: `grep -q 'Cmd' src/ch12-testing/index.md && grep -q 'update' src/ch12-testing/index.md`. npm test lifecycle mentioned: `grep -q 'npm test' src/ch12-testing/index.md && grep -q 'pretest' src/ch12-testing/index.md`. Code via include: `grep -c '#include' src/ch12-testing/index.md` returns >= 2. 개념 prose-only + matches what ran (no unrun behavior claimed): manual review against Task 2 findings.
  </verify>
  <done>src/ch12-testing/index.md follows the template in Korean, 개념 prose-only, includes the two anchors (logic/tests), teaches EXACTLY the runner that ran in Task 2 (Fable.Mocha or the documented fallback), explains testing a pure update (returns Model not Model*Cmd, Pitfall 8), the npm test pretest→test lifecycle, and the mocha pin/version reason; uses plain blockquotes, no mermaid, attributes backtick-wrapped; mdbook build renders the chapter with anchors resolved.</done>
</task>

</tasks>

<verification>
- `cd examples/ch12-testing && dotnet tool restore && npm install && npm test` exits 0 with all tests passing (SC-2), and `npm run build` (Fable compile of Tests.fsproj) exits 0 (SC-4 + phase-wide build gate).
- Open Q #1 RESOLVED build-first: Fable.Mocha 2.17.0 compiled+ran under Fable 5.3.0 — OR a documented RESEARCH ranked fallback (A→B→C→D) was applied; the prose teaches the approach that actually ran.
- Tests.fs tests a PURE update (Msg->Model->Model, returns Model not Model*Cmd — Pitfall 8); testList/testCase assertions; Mocha.runTests |> ignore (or fallback runner).
- Project is the EXCEPTION: NO index.html, NO vite.config.js, NO react/vite; package.json has pretest (dotnet fable Tests.fsproj -o dist) + test (mocha dist/*.fs.js); mocha pinned 9.2.0 (or fallback version, documented).
- src/ch12-testing/index.md follows the authoring template, Korean, 개념 prose-only, code via {{#include}} only, plain blockquotes, no mermaid, attributes backtick-wrapped, and matches the on-disk Tests.fs (teaches only what ran).
- `mdbook build` exits 0 and book/ch12-testing/index.html contains the anchored code.
</verification>

<success_criteria>
REAL-02 satisfied and Phase 5 Success Criteria 2 + 4 met: a reader runs the Ch.12 example and writes/runs tests for a pure Elmish update function that pass via `npm test` (exit 0). The Fable.Mocha 2.17.0 + Fable 5.3.0 compatibility question is resolved empirically (build-first), with a documented fallback if needed, and the chapter teaches the approach that actually ran. The test project compiles with `npm run build` (exit 0).
</success_criteria>

<output>
After completion, create `.planning/phases/05-ecosystem-and-real-world-axis/05-02-SUMMARY.md` recording: the example files created, the BUILD-FIRST resolution of Open Q #1 (did Fable.Mocha 2.17.0 compile+run under Fable 5.3.0 — most likely YES; or which ranked fallback A/B/C/D was applied and WHY), the exact final `npm test` command, the resolved mocha version, the verified result (`npm test` exit 0, N tests passing; `npm run build` exit 0), and confirmation that the chapter prose teaches the approach that actually ran. This closes the STATE.md Research Flag "Phase 5 Ch.12: Fable.Mocha + Fable 5 호환성 미확인" and completes the v1.0 milestone (all 13 chapters written).
</output>
