---
phase: 03-js-interop-axis
plan: "04"
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - examples/ch07-npm-binding/App.fsproj
  - examples/ch07-npm-binding/package.json
  - examples/ch07-npm-binding/vite.config.js
  - examples/ch07-npm-binding/index.html
  - examples/ch07-npm-binding/src/CanvasConfetti.fs
  - examples/ch07-npm-binding/src/App.fs
  - src/ch07-npm-binding/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `dotnet tool restore && npm install && npm run build` in examples/ch07-npm-binding and it exits 0, with canvas-confetti installed as a real npm dependency"
    - "examples/ch07-npm-binding/src/CanvasConfetti.fs is a hand-written Fable binding: [<ImportDefault(\"canvas-confetti\")>] for the confetti function + a [<JS.Pojo>] ConfettiOptions type (using the Ch.6 POJO pattern)"
    - "examples/ch07-npm-binding/src/App.fs wires a DOM button (#confettiBtn) onclick to fire the confetti binding (basic call + a call with custom ConfettiOptions)"
    - "The compiled App.fs.js shows `import` of canvas-confetti as a default import and the confetti call with a plain options object — verified by inspecting the real output"
    - "Glutinum CLI is RUN against @types/canvas-confetti during execution (RESEARCH Open Q #2): the executor runs npx @glutinum/cli, captures the actual output, and documents whether it produced [<ImportDefault>] correctly or needed adjustment — not assumed"
    - "Ch.7 prose covers BOTH the hand-written binding AND the tooling (Glutinum primary, ts2fable as predecessor), showing/discussing the real Glutinum output"
    - "src/ch07-npm-binding/index.md follows the authoring template in Korean and pulls all F# code via {{#include}} anchors; mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch07-npm-binding/src/CanvasConfetti.fs"
      provides: "Hand-written canvas-confetti binding: ImportDefault + [<JS.Pojo>] ConfettiOptions, with ANCHORs"
      contains: "ImportDefault"
    - path: "examples/ch07-npm-binding/src/App.fs"
      provides: "DOM button wiring that fires the confetti binding (basic + custom options)"
      contains: "confettiBtn"
    - path: "examples/ch07-npm-binding/package.json"
      provides: "canvas-confetti as a runtime dependency + @types/canvas-confetti devDependency + vite"
      contains: "canvas-confetti"
    - path: "src/ch07-npm-binding/index.md"
      provides: "Full Ch.7 prose: end-to-end binding walkthrough, ImportDefault rationale, options POJO, Glutinum + ts2fable tooling"
      min_lines: 80
  key_links:
    - from: "src/ch07-npm-binding/index.md"
      to: "examples/ch07-npm-binding/src/CanvasConfetti.fs"
      via: "{{#include ...:confetti-binding}} (and confetti-options anchor)"
      pattern: "#include.*ch07-npm-binding/src/CanvasConfetti.fs:"
    - from: "examples/ch07-npm-binding/src/CanvasConfetti.fs"
      to: "canvas-confetti npm package"
      via: "[<ImportDefault(\"canvas-confetti\")>] (CommonJS export = confetti → default import)"
      pattern: "ImportDefault\\(\"canvas-confetti\"\\)"
    - from: "examples/ch07-npm-binding/src/App.fs"
      to: "examples/ch07-npm-binding/src/CanvasConfetti.fs"
      via: "open CanvasConfetti; btn.onclick fires the binding"
      pattern: "confettiBtn"
---

<objective>
Create the standalone examples/ch07-npm-binding project and write Ch.7 (npm 라이브러리 바인딩), the capstone of the interop phase: bind a REAL npm library (canvas-confetti) from scratch and fire it from a DOM button. The binding hand-writes [<ImportDefault("canvas-confetti")>] (because canvas-confetti uses CommonJS `export = confetti`) plus a [<JS.Pojo>] options type (reusing Ch.6's POJO pattern). The chapter ALSO runs the Glutinum CLI against @types/canvas-confetti and documents the real generated output, contrasting it with the hand-written binding and mentioning ts2fable as the predecessor. The Glutinum output for the CommonJS export is RESEARCH Open Q #2 — resolved by running the tool, not assuming.

Purpose: Satisfies INTR-04 and Phase 3 Success Criterion 5 (reader completes a real npm-library binding from scratch with ts2fable/Glutinum and runs it in the example app). Contributes to the phase-wide build gate. (INTR-04)
Output: A runnable examples/ch07-npm-binding with canvas-confetti as a real dependency, a hand-written CanvasConfetti.fs binding, an App.fs that fires confetti on click, and a complete Korean Ch.7 chapter that shows the hand-written binding AND the real Glutinum output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-interop-axis/03-RESEARCH.md
@.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md
@.planning/phases/03-js-interop-axis/03-01-SUMMARY.md
@.planning/phases/03-js-interop-axis/03-03-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

DEPENDS ON 03-01 (Ch.4 Import mechanism — ImportDefault) AND 03-03 (Ch.6 POJO pattern — [<JS.Pojo>] options). Both must be complete; their SUMMARYs inform the binding (e.g. how ImportDefault emitted, how [<JS.Pojo>] emitted a plain literal).

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 2 installs canvas-confetti, builds, runs Glutinum, and inspects output; Task 3 writes prose using the verified binding + real Glutinum output, then runs mdbook build. Do not reorder or parallelize.

This example has TWO F# files (binding + app). Copy Ch.3's layout but with BOTH in the fsproj Compile list IN ORDER (binding first):
- `App.fsproj`: net10.0, RootNamespace Ch07NpmBinding, `<Compile Include="src/CanvasConfetti.fs" />` THEN `<Compile Include="src/App.fs" />` (F# compile order matters — binding before app). Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0.
- `index.html`: a visible button `<button id="confettiBtn">🎉 Confetti!</button>` inside `#app` + `<script type="module" src="./src/App.fs.js"></script>`.
- `package.json`: name "ch07-npm-binding", type module, dev/build scripts with --verbose; dependencies: canvas-confetti ^1.9.0; devDependencies: @types/canvas-confetti ^1.9.0 + vite ^6.0.0. (canvas-confetti is a runtime DEPENDENCY because Vite bundles it; @types is dev-only.)
- `vite.config.js`: identical to Ch.3.

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — plain markdown `>` blockquotes only (STATE.md [01-01]).
- ALL code via {{#include}} anchors. Include path from src/ch07-npm-binding/index.md is `../../examples/ch07-npm-binding/src/CanvasConfetti.fs:anchor` (and .../App.fs:anchor).
- `--verbose` required in dev script.
- `.gitignore` covers fable_modules/, **/*.fs.js, dist/ — do NOT commit generated files. canvas-confetti is a real dep: package.json + package-lock.json ARE committed; node_modules is NOT.

canvas-confetti facts (RESEARCH Pattern 5 + Pitfall 2/7 + Open Q #2/#4):
- npm: canvas-confetti; types: @types/canvas-confetti (DefinitelyTyped). The .d.ts uses CommonJS `export = confetti` → for Vite/ESM this is a DEFAULT export → use [<ImportDefault("canvas-confetti")>] (NOT importAll, NOT named import — Pitfall 7).
- Signature: `confetti(options?) : Promise<undefined> | null`. Use `JS.Promise<unit>` in F# (Open Q #4: unit ≈ undefined).
- Options (all optional): particleCount (int), angle/spread/startVelocity/decay/gravity/drift (float), ticks (int), origin ({| x: float; y: float |}), colors (string[]), scalar (float), zIndex (int), disableForReducedMotion (bool). Model with a [<AllowNullLiteral>] [<JS.Pojo>] ConfettiOptions type (Ch.6 pattern).
- Glutinum: `npx @glutinum/cli ./node_modules/@types/canvas-confetti/index.d.ts --out-file ./Glutinum.CanvasConfetti.fs`. v0.13.0. Open Q #2: whether it generates [<ImportDefault>] for `export = confetti` correctly or needs manual adjustment — RUN IT and document.
- ts2fable: `npx ts2fable node_modules/@types/canvas-confetti/index.d.ts Ts2fable.CanvasConfetti.fs` — older predecessor; mention, optionally run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch07-npm-binding example + hand-written CanvasConfetti.fs binding + App.fs button wiring</name>
  <files>examples/ch07-npm-binding/App.fsproj, examples/ch07-npm-binding/package.json, examples/ch07-npm-binding/vite.config.js, examples/ch07-npm-binding/index.html, examples/ch07-npm-binding/src/CanvasConfetti.fs, examples/ch07-npm-binding/src/App.fs</files>
  <action>
Create the example directory mirroring Ch.3's verified layout, with TWO F# source files.

`App.fsproj`: net10.0, RootNamespace Ch07NpmBinding, Compile order: CanvasConfetti.fs THEN App.fs. Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0.
```xml
<ItemGroup>
  <Compile Include="src/CanvasConfetti.fs" />
  <Compile Include="src/App.fs" />
</ItemGroup>
```
`package.json`: name "ch07-npm-binding", type module, dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`; dependencies: { "canvas-confetti": "^1.9.0" }; devDependencies: { "@types/canvas-confetti": "^1.9.0", "vite": "^6.0.0" }.
`vite.config.js`: identical to Ch.3.
`index.html`: lang ko, title "Ch07 - npm 바인딩", body `<div id="app"><h1>canvas-confetti 바인딩 예제</h1><button id="confettiBtn">🎉 Confetti!</button></div>` + `<script type="module" src="./src/App.fs.js"></script>`.

`src/CanvasConfetti.fs` — the hand-written binding (RESEARCH Pattern 5). ANCHORs: `confetti-options`, `confetti-binding`. Shape:
```fsharp
module CanvasConfetti

open Fable.Core
open Fable.Core.JsInterop

// ANCHOR: confetti-options
// Options POJO — reuses the Ch.6 [<JS.Pojo>] pattern (requires [<AllowNullLiteral>]).
[<AllowNullLiteral>]
[<JS.Pojo>]
type ConfettiOptions(?particleCount: int,
                     ?angle: float,
                     ?spread: float,
                     ?gravity: float,
                     ?origin: {| x: float; y: float |},
                     ?colors: string[],
                     ?zIndex: int) =
    member val particleCount: int option = jsNative with get, set
    member val angle: float option = jsNative with get, set
    member val spread: float option = jsNative with get, set
    member val gravity: float option = jsNative with get, set
    member val origin: {| x: float; y: float |} option = jsNative with get, set
    member val colors: string[] option = jsNative with get, set
    member val zIndex: int option = jsNative with get, set
// ANCHOR_END: confetti-options

// ANCHOR: confetti-binding
// canvas-confetti uses CommonJS `export = confetti` → a DEFAULT export for ESM bundlers.
// Use [<ImportDefault>], NOT a named import or importAll (Pitfall 7).
[<ImportDefault("canvas-confetti")>]
let confetti : ConfettiOptions -> JS.Promise<unit> = jsNative
// ANCHOR_END: confetti-binding
```

`src/App.fs` — fires the binding on a button click. ANCHOR: `confetti-usage`. No `[<EntryPoint>]`. Shape:
```fsharp
module App

open Browser
open Browser.Types
open CanvasConfetti

// ANCHOR: confetti-usage
let btn = document.getElementById "confettiBtn" :?> HTMLButtonElement
btn.onclick <- fun _ ->
    // Basic call with defaults
    confetti (ConfettiOptions()) |> ignore
    // Custom options — demonstrates the Ch.6 POJO pattern in a real binding
    confetti (ConfettiOptions(particleCount = 200,
                              spread = 80.0,
                              origin = {| x = 0.5; y = 0.6 |}))
    |> ignore
// ANCHOR_END: confetti-usage
```
If the binding's exact F# type causes a compile issue (e.g. options optionality), adjust minimally in Task 2 until the build passes, keeping the ImportDefault + [<JS.Pojo>] shape.
  </action>
  <verify>
`test -f examples/ch07-npm-binding/App.fsproj && test -f examples/ch07-npm-binding/src/CanvasConfetti.fs && test -f examples/ch07-npm-binding/src/App.fs && test -f examples/ch07-npm-binding/index.html && test -f examples/ch07-npm-binding/package.json`. Compile order correct (CanvasConfetti before App): `grep -n 'Compile Include' examples/ch07-npm-binding/App.fsproj` shows CanvasConfetti.fs before App.fs. Anchors present: `grep -q 'ANCHOR: confetti-options' examples/ch07-npm-binding/src/CanvasConfetti.fs && grep -q 'ANCHOR: confetti-binding' examples/ch07-npm-binding/src/CanvasConfetti.fs && grep -q 'ANCHOR: confetti-usage' examples/ch07-npm-binding/src/App.fs`. ImportDefault used: `grep -q 'ImportDefault("canvas-confetti")' examples/ch07-npm-binding/src/CanvasConfetti.fs`. canvas-confetti is a dependency: `grep -q '"canvas-confetti"' examples/ch07-npm-binding/package.json`. Button present: `grep -q 'confettiBtn' examples/ch07-npm-binding/index.html`. `grep -q -- '--verbose' examples/ch07-npm-binding/package.json`. `! grep -q 'EntryPoint' examples/ch07-npm-binding/src/App.fs`. Pitfall-7 guard: `! grep -q 'ImportAll("canvas-confetti")' examples/ch07-npm-binding/src/CanvasConfetti.fs`.
  </verify>
  <done>examples/ch07-npm-binding scaffolded with two F# files in correct compile order; CanvasConfetti.fs hand-writes [<ImportDefault("canvas-confetti")>] + a [<JS.Pojo>] ConfettiOptions; App.fs fires confetti on the #confettiBtn click (basic + custom options); package.json lists canvas-confetti as a dependency; no EntryPoint; not using importAll.</done>
</task>

<task type="auto">
  <name>Task 2: Install canvas-confetti, build, RUN Glutinum (resolve Open Q #2), INSPECT generated JS</name>
  <files>examples/ch07-npm-binding/ (install + build + glutinum + inspect; minimal binding edits only if build fails)</files>
  <action>
From `examples/ch07-npm-binding/`:
1. `dotnet tool restore` (exit 0).
2. `npm install` (exit 0) — installs canvas-confetti + @types/canvas-confetti + vite. Confirm node_modules/canvas-confetti and node_modules/@types/canvas-confetti exist.
3. `npm run build` (exit 0) — Fable compiles CanvasConfetti.fs + App.fs → src/App.fs.js; Vite bundles canvas-confetti into dist/. Fix minimal binding issues if needed (keep ImportDefault + [<JS.Pojo>]) until exit 0.

OPEN QUESTION #2 — Glutinum output for canvas-confetti's CommonJS export (RUN, do NOT assume):
4. Run: `npx @glutinum/cli ./node_modules/@types/canvas-confetti/index.d.ts --out-file ./Glutinum.CanvasConfetti.fs` (from the example dir). Capture stdout/stderr and the generated file.
5. INSPECT Glutinum.CanvasConfetti.fs. Record CONCRETELY:
   - Did it generate an import for the default export — `[<ImportDefault(...)>]`, `[<Import("default", ...)>]`, `importDefault`, or something else for `export = confetti`?
   - How did it model the Options interface (interface with abstract members? a POJO?).
   - Does the output compile as-is, or would it need adjustment to fire confetti? (You do NOT need to wire Glutinum's output into the app build — it's for documentation/contrast — but note whether its import form matches our hand-written ImportDefault.)
   This resolves Open Q #2. If Glutinum errors or produces nothing usable, record that outcome and the error. The Glutinum file is a DEMO artifact; you may delete it after capturing its content, or keep it un-compiled (do NOT add it to the fsproj Compile list — it would conflict with the hand-written module).
6. OPTIONAL: also run ts2fable (`npx ts2fable node_modules/@types/canvas-confetti/index.d.ts Ts2fable.CanvasConfetti.fs`) and note its output briefly as the predecessor contrast. Skip if ts2fable fails to install — just note it.

INSPECT `examples/ch07-npm-binding/src/App.fs.js` and record:
- The canvas-confetti import line — confirm it is a default import (e.g. `import canvasConfetti from "canvas-confetti"` or Fable's equivalent default-import form). Record the exact token.
- The confetti call site with the options object — confirm a plain object literal is passed (e.g. `confetti({ particleCount: 200, spread: 80, origin: { x: 0.5, y: 0.6 } })`). Record the exact emitted call.

Write all findings for Task 3 + SUMMARY. Commit package.json + package-lock.json (real dep); do NOT commit node_modules or generated *.fs.js/dist. Do NOT commit the Glutinum demo file unless you intentionally keep it as an un-compiled reference (prefer capturing its content into the SUMMARY/prose instead).
  </action>
  <verify>
`cd examples/ch07-npm-binding && dotnet tool restore && npm install && npm run build` exits 0. `test -f examples/ch07-npm-binding/src/App.fs.js`. canvas-confetti installed: `test -d examples/ch07-npm-binding/node_modules/canvas-confetti && test -d examples/ch07-npm-binding/node_modules/@types/canvas-confetti`. Default import confirmed in output: `grep -q 'canvas-confetti' examples/ch07-npm-binding/src/App.fs.js`. Glutinum RUN and its output recorded (Open Q #2 resolved): the executor ran `npx @glutinum/cli` against @types/canvas-confetti, captured the generated import/options form, and documented whether it matches the hand-written [<ImportDefault>] or needs adjustment (recorded in execution notes for Task 3 + SUMMARY). The confetti call with a plain options literal is recorded.
  </verify>
  <done>Example builds with canvas-confetti as a real bundled dependency (npm run build exit 0); Glutinum CLI was actually run against @types/canvas-confetti and its output documented (Open Q #2 resolved — import form for the CommonJS export recorded); App.fs.js inspected confirming a default import + a plain options-object call.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.7 chapter prose (binding + tooling, verified) and verify render</name>
  <files>src/ch07-npm-binding/index.md</files>
  <action>
Fully replace the stub `src/ch07-npm-binding/index.md` following the authoring template. H1 `# 7장. npm 라이브러리 바인딩 (Binding an npm Library)`, then a plain `>` blockquote (NOT [!NOTE]) pointing to examples/ch07-npm-binding + run command. Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION: Use the verified binding output AND the real Glutinum output recorded in Task 2. Describe what Glutinum ACTUALLY generated for the CommonJS export (the resolved Open Q #2), not an assumption.

- 개념 (Concept) — PROSE ONLY, NO code blocks: Explain the workflow for binding a real npm library from scratch: (1) inspect the library's export style (canvas-confetti uses CommonJS `export = confetti`, which ESM bundlers treat as a default export → ImportDefault, NOT a named import or importAll — reference the Ch.4 import lookup table and Pitfall 7); (2) model the options object with a POJO type — reuse the Ch.6 `[<JS.Pojo>]` pattern so the emitted object is a plain literal the library accepts; (3) declare the function signature (confetti returns a Promise → JS.Promise<unit>). Then explain the TOOLING: Glutinum CLI (`npx @glutinum/cli`) converts a TypeScript .d.ts into F# bindings and is the current recommended tool; ts2fable is the older predecessor. Explain in prose what Glutinum produced for canvas-confetti (the resolved Open Q #2 — e.g. whether it emitted ImportDefault correctly or needed adjustment) and why a hand-written binding is still valuable (control, simpler surface). Keep prose only.
- 예제 (Example): Intro sentence, then include the binding and usage anchors, each with a 1–2 sentence explanation quoting the verified emitted JS:
  ```fsharp
  {{#include ../../examples/ch07-npm-binding/src/CanvasConfetti.fs:confetti-options}}
  ```
  then confetti-binding, then:
  ```fsharp
  {{#include ../../examples/ch07-npm-binding/src/App.fs:confetti-usage}}
  ```
  After confetti-binding, state that ImportDefault emitted a default import (quote the real token from Task 2) — explain WHY (CommonJS export). After confetti-usage, note the options compiled to a plain object literal (quote the real call). Then a short prose paragraph (or a fenced text/bash block for the COMMAND only) showing the Glutinum invocation `npx @glutinum/cli ./node_modules/@types/canvas-confetti/index.d.ts --out-file ./Glutinum.CanvasConfetti.fs` and describing the real generated import/options form recorded in Task 2, contrasting with the hand-written binding. (A bash COMMAND in a fenced block is acceptable here — it is a shell command, not F# code that should be an include. Do NOT paste large generated F# blobs; summarize/excerpt in prose.)
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` (installs canvas-confetti) → `npm run dev`; click the 🎉 Confetti! button to fire the confetti animation in the browser (the visible end-to-end proof the binding works). Also instruct opening src/App.fs.js to confirm the default import + plain options object. Do NOT paste raw .fs.js or terminal output.
- 핵심 포인트 (Key Points): 5–7 bullets: export 스타일 확인이 첫 단계(CommonJS export = → ImportDefault); 옵션은 [<JS.Pojo>] POJO로(Ch.6); Promise 반환 → JS.Promise<unit>; importAll/named import는 잘못(Pitfall 7); Glutinum CLI가 .d.ts → F# 변환(현재 권장), ts2fable는 예전 도구; 수기 바인딩은 제어/단순함에 유리; 버튼 클릭으로 동작 확인(end-to-end).

Then run `mdbook build` and confirm Ch.7 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch07-npm-binding/index.html`. Anchors resolved in rendered HTML: `grep -q 'ConfettiOptions' book/ch07-npm-binding/index.html && grep -q 'ImportDefault' book/ch07-npm-binding/index.html && grep -q 'confettiBtn' book/ch07-npm-binding/index.html`. Glutinum covered: `grep -qi 'glutinum' src/ch07-npm-binding/index.md && grep -qi 'ts2fable' src/ch07-npm-binding/index.md`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch07-npm-binding/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch07-npm-binding/index.md`. F# code via include (allow a fenced bash command block for the Glutinum invocation, but F# must be included): `grep -c '#include' src/ch07-npm-binding/index.md` returns >= 3; `! grep -q 'module CanvasConfetti' src/ch07-npm-binding/index.md` (the binding module is included, not pasted). 개념 prose-only: manual review.
  </verify>
  <done>src/ch07-npm-binding/index.md follows the template in Korean, 개념 prose-only, includes the confetti-options/confetti-binding/confetti-usage anchors, explains ImportDefault for the CommonJS export (quoting verified emitted JS), reuses the Ch.6 POJO pattern for options, covers Glutinum (with the real resolved output) and ts2fable, uses plain blockquotes, and mdbook build renders the chapter.</done>
</task>

</tasks>

<verification>
- `cd examples/ch07-npm-binding && dotnet tool restore && npm install && npm run build` exits 0, with canvas-confetti installed and bundled (Success Criterion 5 + phase-wide build gate).
- CanvasConfetti.fs hand-writes [<ImportDefault("canvas-confetti")>] + [<JS.Pojo>] ConfettiOptions; App.fs fires it on the #confettiBtn click; App.fs.js shows a default import + plain options-object call (verified, not assumed).
- Glutinum CLI was RUN against @types/canvas-confetti and its real output documented (Open Q #2 resolved); ts2fable mentioned as predecessor.
- package.json/package-lock.json committed (canvas-confetti dep); node_modules + generated files NOT committed.
- src/ch07-npm-binding/index.md follows the authoring template, Korean, 개념 prose-only, F# code via {{#include}} only, plain blockquotes, covers binding + tooling.
- `mdbook build` exits 0 and book/ch07-npm-binding/index.html contains all anchored code.
</verification>

<success_criteria>
INTR-04 satisfied: a reader follows Ch.7 to bind a real npm library (canvas-confetti, with TypeScript types) from scratch — hand-writing [<ImportDefault>] + a [<JS.Pojo>] options type and/or using Glutinum/ts2fable — and fires it from the example app's button (visible confetti). The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors. (Phase 3 Success Criterion 5.)
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-interop-axis/03-04-SUMMARY.md` recording: the example files created (including the two-file fsproj compile order), the verified build output (default import token + plain options-object call from App.fs.js), and the RESOLVED Open Q #2 — the ACTUAL Glutinum output for canvas-confetti's `export = confetti` (what import form it generated, whether it matched the hand-written ImportDefault or needed adjustment), plus a note on ts2fable. Confirm package.json/package-lock.json committed and node_modules/generated files excluded. This completes the Phase 3 interop axis.
</output>
