---
phase: 03-js-interop-axis
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - examples/ch06-pojo-patterns/App.fsproj
  - examples/ch06-pojo-patterns/package.json
  - examples/ch06-pojo-patterns/vite.config.js
  - examples/ch06-pojo-patterns/index.html
  - examples/ch06-pojo-patterns/src/App.fs
  - src/ch06-pojo-patterns/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `dotnet tool restore && npm install && npm run build` in examples/ch06-pojo-patterns and it exits 0"
    - "examples/ch06-pojo-patterns/src/App.fs demonstrates all FOUR POJO patterns — anonymous record {| |}, [<AllowNullLiteral>][<JS.Pojo>] class, createObj, jsOptions — each behind its own ANCHOR, each compiling to a plain JS object literal (verified in App.fs.js)"
    - "examples/ch06-pojo-patterns/src/App.fs contains the Promise↔Async boundary: a global fetch returning JS.Promise bridged into F# via Async.AwaitPromise, fired from a DOM button click via Async.StartImmediate"
    - "The Fable.Promise 3.2.0 `promise {}` CE compatibility with Fable 5.3.0 is RESOLVED by build (RESEARCH Open Q #1): the executor adds the package, attempts a promise{} CE, and either keeps it (build exit 0) or falls back to Async.AwaitPromise only — documenting the outcome"
    - "Async.StartImmediate browser behavior is verified by build (RESEARCH Open Q #3); fallback Async.StartWithContinuations noted if it fails"
    - "Ch.6 prose presents the POJO decision table (when to use each pattern) and warns that regular F# records are NOT POJOs (they compile to ES6 Record class instances)"
    - "Ch.6 prose explains the async boundary: Async.AwaitPromise (Promise→Async), Async.StartAsPromise (Async→Promise), and that Async.RunSynchronously does NOT exist in Fable"
    - "src/ch06-pojo-patterns/index.md follows the authoring template in Korean and pulls all F# code via {{#include}} anchors; mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch06-pojo-patterns/src/App.fs"
      provides: "Four POJO patterns + Promise↔Async bridge with per-concept ANCHORs + DOM button"
      contains: "ANCHOR: jspojo-pattern"
    - path: "examples/ch06-pojo-patterns/App.fsproj"
      provides: "net10.0 fsproj, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0 (+ Fable.Promise 3.2.0 IF CE build passes), RootNamespace Ch06PojoPatterns"
      contains: "Fable.Core"
    - path: "src/ch06-pojo-patterns/index.md"
      provides: "Full Ch.6 prose: POJO decision table, record-is-not-POJO warning, async boundary (AwaitPromise/StartAsPromise/StartImmediate)"
      min_lines: 70
  key_links:
    - from: "src/ch06-pojo-patterns/index.md"
      to: "examples/ch06-pojo-patterns/src/App.fs"
      via: "{{#include ...:anonrecord-pattern}} (and jspojo/createobj/jsoptions/async anchors)"
      pattern: "#include.*ch06-pojo-patterns/src/App.fs:"
    - from: "examples/ch06-pojo-patterns/src/App.fs"
      to: "JS Promise boundary"
      via: "fetch url |> Async.AwaitPromise inside async {}, fired by Async.StartImmediate on button click"
      pattern: "Async\\.AwaitPromise"
    - from: "examples/ch06-pojo-patterns/index.html"
      to: "examples/ch06-pojo-patterns/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js> + a fetch button"
      pattern: "src/App\\.fs\\.js"
---

<objective>
Create the standalone examples/ch06-pojo-patterns project and write Ch.6 (POJO 패턴 + 비동기 경계), which teaches the four POJO-creation patterns (anonymous record, [<JS.Pojo>], createObj, jsOptions) — each compiling to a plain JS object literal — and the Promise↔Async boundary (Async.AwaitPromise / Async.StartAsPromise / Async.StartImmediate). Each POJO pattern is verified against the generated App.fs.js. The chapter also resolves three RESEARCH open questions empirically during the build (Fable.Promise CE compat, Async.StartImmediate behavior, and that records are NOT POJOs).

Purpose: Satisfies INTR-02 (POJO half) + INTR-03 (async boundary) and Phase 3 Success Criteria 3 and 4 (reader selects the right POJO pattern; reader runs Promise↔Async conversion code). Contributes to the phase-wide build gate. (INTR-02, INTR-03)
Output: A runnable examples/ch06-pojo-patterns that builds with `npm run build`, an App.fs with four POJO anchors + an async-boundary anchor with a working fetch button, and a complete Korean Ch.6 chapter whose POJO/async claims are verified against the real output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-interop-axis/03-RESEARCH.md
@.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md
@.planning/phases/03-js-interop-axis/03-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

DEPENDS ON 03-01 (Ch.4): this chapter builds on the Import mechanism taught there (the async example imports/uses a global fetch; the import lookup table from Ch.4 is assumed known). 03-01 must be complete.

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 2 builds, RESOLVES the open questions, and INSPECTS the generated POJO literals; Task 3 writes prose quoting the verified output, then runs mdbook build. Do not reorder or parallelize.

Copy the verified runnable example pattern from Ch.3 (examples/ch03-fable-core/). Name = "ch06-pojo-patterns", RootNamespace Ch06PojoPatterns. The fsproj MAY add Fable.Promise 3.2.0 (see open-question task below) but ONLY if the build passes with it.

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — plain markdown `>` blockquotes only (STATE.md [01-01]).
- ALL code via {{#include}} anchors. Include path from src/ch06-pojo-patterns/index.md is `../../examples/ch06-pojo-patterns/src/App.fs:anchor`.
- `--verbose` required in dev script.
- Versions: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, net10.0, Vite ^6; Fable.Promise 3.2.0 only if CE build passes.
- `.gitignore` covers fable_modules/, **/*.fs.js, dist/ — do NOT commit generated files.

POJO facts (RESEARCH Pattern 4 + Pitfall 3/5/6 + Code Examples):
- Anonymous record `{| name = "Alice"; age = 30 |}` → `{ name: "Alice", age: 30 }`. One-off, all fields required.
- `[<AllowNullLiteral>] [<JS.Pojo>] type Opts(?term, ?caseSensitive) = member val term: string option = jsNative with get,set ...` → constructing `Opts(term="hello")` emits `{ term: "hello" }` (omits unset optionals). REQUIRES BOTH attributes (Pitfall 5). Fable 5 recommended; do NOT use legacy `[<Pojo>]` or `[<ParamObject>]`.
- `createObj ["host" ==> "localhost"; "port" ==> 5432]` → `{ host: "localhost", port: 5432 }`. `==>` and createObj need `open Fable.Core.JsInterop`. Dynamic/string keys.
- `jsOptions<IFace>(fun o -> o.color <- "#333"; o.fontSize <- 16)` → `{ color: "#333", fontSize: 16 }`. Needs an interface with mutable abstract members; needs `open Fable.Core.JsInterop`.
- WARNING (Pitfall 3): regular F# records compile to ES6 class instances `extends Record` (NOT POJOs) — passing one to a JS lib that uses Object.keys/spread misbehaves. This was already seeded in Ch.2 ([02-02] decision). Reinforce here.

Async facts (RESEARCH Pattern 4 + Pitfall 4 + Open Q #1/#3):
- `Async.AwaitPromise : JS.Promise<'T> -> Async<'T>` and `Async.StartAsPromise : Async<'T> -> JS.Promise<'T>` are in Fable.Core (no extra package).
- `Async.RunSynchronously` does NOT exist in Fable (single-threaded). Use `Async.StartImmediate` to fire-and-forget (Open Q #3: verify in build; fallback `Async.StartWithContinuations`).
- `promise { }` CE comes from Fable.Promise NuGet 3.2.0 (Open Q #1: 3.2.0 declares Fable.Core >= 3.7.1; compat with Fable 5.3.0 UNCONFIRMED — verify by building; if it fails, drop the CE and use Async.AwaitPromise only).
- Practical async example: a global fetch returning JS.Promise<Response>, bridged via Async.AwaitPromise inside an async{} workflow, fired from a button onclick via Async.StartImmediate, writing the result to a DOM element. Use a real endpoint like https://jsonplaceholder.typicode.com/todos/1 (network call happens at runtime in the browser, not at build time — build only compiles).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch06-pojo-patterns example + App.fs with four POJO anchors + async-boundary anchor</name>
  <files>examples/ch06-pojo-patterns/App.fsproj, examples/ch06-pojo-patterns/package.json, examples/ch06-pojo-patterns/vite.config.js, examples/ch06-pojo-patterns/index.html, examples/ch06-pojo-patterns/src/App.fs</files>
  <action>
Create the example directory mirroring Ch.3's verified layout.

`App.fsproj`: net10.0, RootNamespace Ch06PojoPatterns, `<Compile Include="src/App.fs" />`, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0. (Fable.Promise added in Task 2 only if its CE build passes.)
`package.json`: name "ch06-pojo-patterns", type module, dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`, devDependencies vite ^6.0.0.
`vite.config.js`: identical to Ch.3.
`index.html`: `<div id="app"></div>` with an inner `<button id="fetchBtn">데이터 불러오기</button>` and `<pre id="output"></pre>`, plus `<script type="module" src="./src/App.fs.js"></script>`, lang ko, title "Ch06 - POJO 패턴".

`src/App.fs` — ANCHORs: `anonrecord-pattern`, `jspojo-pattern`, `createobj-pattern`, `jsoptions-pattern`, `async-boundary`. No `[<EntryPoint>]`. Reference each POJO value in DOM output so Fable emits them. Shape:
```fsharp
module App

open Fable.Core
open Fable.Core.JsInterop
open Browser

// ANCHOR: anonrecord-pattern
// Anonymous record → plain JS object literal. One-off, all fields required.
let user = {| name = "Alice"; age = 30 |}
// ANCHOR_END: anonrecord-pattern

// ANCHOR: jspojo-pattern
// [<JS.Pojo>] (Fable 5) → reusable typed options; unset optionals are omitted.
// REQUIRES BOTH [<AllowNullLiteral>] and [<JS.Pojo>].
[<AllowNullLiteral>]
[<JS.Pojo>]
type SearchOptions(?term: string, ?caseSensitive: bool) =
    member val term: string option = jsNative with get, set
    member val caseSensitive: bool option = jsNative with get, set

let opts = SearchOptions(term = "hello")
// ANCHOR_END: jspojo-pattern

// ANCHOR: createobj-pattern
// createObj → dynamic/string-keyed object. ==> is the key-value operator.
let config = createObj [ "host" ==> "localhost"; "port" ==> 5432 ]
// ANCHOR_END: createobj-pattern

// ANCHOR: jsoptions-pattern
// jsOptions<IFace> → fill selected fields of an interface via a mutator.
type ITheme =
    abstract color: string with get, set
    abstract fontSize: int with get, set

let theme = jsOptions<ITheme>(fun t ->
    t.color <- "#333"
    t.fontSize <- 16)
// ANCHOR_END: jsoptions-pattern

// ANCHOR: async-boundary
// fetch is a JS global returning a Promise. Bridge it into F# async with Async.AwaitPromise.
[<Global>]
let fetch (url: string) : JS.Promise<Browser.Types.Response> = jsNative

let loadText (url: string) : Async<string> =
    async {
        let! resp = fetch url |> Async.AwaitPromise
        let! text = resp.text() |> Async.AwaitPromise
        return text
    }

let btn = document.getElementById "fetchBtn" :?> Browser.Types.HTMLButtonElement
btn.onclick <- fun _ ->
    loadText "https://jsonplaceholder.typicode.com/todos/1"
    |> Async.map (fun text ->
        let output = document.getElementById "output"
        output.textContent <- text)
    |> Async.StartImmediate   // NOT Async.RunSynchronously (unsupported in Fable)
// ANCHOR_END: async-boundary

let app = document.getElementById "app"
let summary =
    sprintf "user=%s/%d, term=%A, host=%A, theme=%A"
        user.name user.age opts.term (config?host) theme.color
console.log summary
```
If `Async.map` is unavailable, substitute the available continuation (e.g. inline the DOM write inside the async workflow before StartImmediate). Resolve at build time.

Keep focused on POJO + async. Do NOT add npm-library binding (Ch.7).
  </action>
  <verify>
`test -f examples/ch06-pojo-patterns/App.fsproj && test -f examples/ch06-pojo-patterns/src/App.fs && test -f examples/ch06-pojo-patterns/index.html && test -f examples/ch06-pojo-patterns/package.json && test -f examples/ch06-pojo-patterns/vite.config.js`. Anchors present: `for a in anonrecord-pattern jspojo-pattern createobj-pattern jsoptions-pattern async-boundary; do grep -q "ANCHOR: $a" examples/ch06-pojo-patterns/src/App.fs || exit 1; done`. JS.Pojo paired with AllowNullLiteral: `grep -q 'AllowNullLiteral' examples/ch06-pojo-patterns/src/App.fs && grep -q 'JS.Pojo' examples/ch06-pojo-patterns/src/App.fs`. Async bridge present: `grep -q 'Async.AwaitPromise' examples/ch06-pojo-patterns/src/App.fs && grep -q 'Async.StartImmediate' examples/ch06-pojo-patterns/src/App.fs`. Button present: `grep -q 'fetchBtn' examples/ch06-pojo-patterns/index.html`. `grep -q -- '--verbose' examples/ch06-pojo-patterns/package.json`. `! grep -q 'EntryPoint' examples/ch06-pojo-patterns/src/App.fs`. Legacy attr guard: `! grep -qE '\[<Pojo>\]|ParamObject' examples/ch06-pojo-patterns/src/App.fs`.
  </verify>
  <done>examples/ch06-pojo-patterns scaffolded; App.fs has four POJO anchors + async-boundary anchor with a working fetch-button handler using Async.AwaitPromise + Async.StartImmediate; [<JS.Pojo>] correctly paired with [<AllowNullLiteral>]; no legacy [<Pojo>]/[<ParamObject>]; no EntryPoint; no Ch.7 concepts.</done>
</task>

<task type="auto">
  <name>Task 2: Build, RESOLVE open questions (Fable.Promise CE, StartImmediate), INSPECT POJO literals</name>
  <files>examples/ch06-pojo-patterns/App.fsproj, examples/ch06-pojo-patterns/src/App.fs (only if CE/StartImmediate fallback needed), examples/ch06-pojo-patterns/ (build + inspect)</files>
  <action>
From `examples/ch06-pojo-patterns/`:
1. `dotnet tool restore` (exit 0), `npm install` (exit 0).
2. Baseline build: `npm run build` (exit 0) WITHOUT Fable.Promise (Async.AwaitPromise version). Fix any compile errors (e.g. Async.map fallback) until exit 0.

OPEN QUESTION #1 — Fable.Promise `promise {}` CE compat (inspect/verify, do NOT assume):
3. `dotnet add examples/ch06-pojo-patterns/App.fsproj package Fable.Promise --version 3.2.0`.
4. Add a SMALL `promise {}` CE binding behind a NEW anchor `promise-ce` in App.fs, e.g.:
   ```fsharp
   // ANCHOR: promise-ce
   open Fable.Core   // promise CE
   let loadTextP (url: string) : JS.Promise<string> =
       promise {
           let! resp = fetch url
           let! text = resp.text()
           return text
       }
   // ANCHOR_END: promise-ce
   ```
   (Reference loadTextP somewhere — e.g. `loadTextP |> ignore` — so it is emitted.)
5. `npm run build` again.
   - If it EXITS 0: KEEP Fable.Promise + the promise-ce anchor. Record "Fable.Promise 3.2.0 promise{} CE IS compatible with Fable 5.3.0".
   - If it FAILS: REMOVE the Fable.Promise PackageReference and the promise-ce anchor/code; restore the baseline (Async.AwaitPromise only) build to exit 0. Record "Fable.Promise 3.2.0 promise{} CE FAILED with Fable 5.3.0 — fell back to Async.AwaitPromise" and capture the error excerpt.

OPEN QUESTION #3 — Async.StartImmediate (verify in build):
6. The baseline already uses Async.StartImmediate. Confirm `npm run build` compiled it without error. If it failed, switch the button handler to `Async.StartWithContinuations(work, (fun _ -> ()), (fun _ -> ()), (fun _ -> ()))` (or the available continuation form) and rebuild to exit 0. Record which form compiled.

INSPECT `examples/ch06-pojo-patterns/src/App.fs.js` and record the ACTUAL generated output:
- Each POJO pattern → confirm it produced a plain object literal: `user` → `{ name: "Alice", age: 30 }`; `opts` (SearchOptions term="hello") → `{ term: "hello" }` (caseSensitive OMITTED); `config` → `{ host: "localhost", port: 5432 }`; `theme` → `{ color: "#333", fontSize: 16 }`. Record exact tokens — confirm NONE is a class instance / `extends Record`.
- Confirm the async boundary compiled (fetch global referenced, Async.AwaitPromise present in output).

Write findings for Task 3 + SUMMARY. Do not commit generated files.
  </action>
  <verify>
Final `cd examples/ch06-pojo-patterns && npm run build` exits 0 (whichever async/CE configuration was kept). `test -f examples/ch06-pojo-patterns/src/App.fs.js`. Open-question outcomes RECORDED: (Q1) whether Fable.Promise 3.2.0 promise{} CE compiled with Fable 5.3.0 (kept) or failed (fell back) — and the App.fsproj reflects the decision (`grep -q 'Fable.Promise' examples/ch06-pojo-patterns/App.fsproj` is true ONLY if it built). (Q3) Async.StartImmediate (or the StartWithContinuations fallback) compiled. POJO literals inspected: all four produced plain object literals, none is `extends Record` — confirm at least one literal token exists: `grep -q 'host' examples/ch06-pojo-patterns/src/App.fs.js`. Findings recorded for Task 3.
  </verify>
  <done>Example builds (npm run build exit 0); the Fable.Promise CE compat (Open Q #1) and Async.StartImmediate behavior (Open Q #3) are RESOLVED by actual build with the outcome documented (kept or fallback); App.fs.js inspected and all four POJO patterns confirmed to emit plain object literals (not Record class instances).</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.6 chapter prose (POJO + async, verified) and verify render</name>
  <files>src/ch06-pojo-patterns/index.md</files>
  <action>
Fully replace the stub `src/ch06-pojo-patterns/index.md` following the authoring template. H1 `# 6장. POJO 패턴과 비동기 경계 (POJO Patterns and the Async Boundary)`, then a plain `>` blockquote (NOT [!NOTE]) pointing to examples/ch06-pojo-patterns + run command. Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION: Use the actual generated POJO literals and the open-question outcomes recorded in Task 2. If the promise{} CE was dropped, do NOT include a promise-ce block or claim it works — describe only what built.

- 개념 (Concept) — PROSE ONLY, NO code blocks: Two themes. (1) POJO patterns: explain that JS libraries expect plain objects (POJO — Plain Old JavaScript Object), but regular F# records compile to ES6 class instances (extends Record), so they are NOT POJOs (reinforce the Ch.2 seed). Present the decision table IN PROSE: anonymous record `{| |}` for one-off all-required objects; `[<JS.Pojo>]` class (with `[<AllowNullLiteral>]`) for reusable typed options with optional params; createObj for dynamic/string-keyed; jsOptions<IFace> for filling selected interface fields. Note createObj/jsOptions/`==>` need `open Fable.Core.JsInterop`. (2) The async boundary: explain JS Promise (eager/hot) vs F# Async (lazy/cold); Async.AwaitPromise converts Promise→Async, Async.StartAsPromise converts Async→Promise; and that Async.RunSynchronously does NOT exist in Fable (single-threaded event loop) — use Async.StartImmediate to fire-and-forget. If Task 2 kept the promise{} CE, mention it as an alternative that stays in the Promise world; if it was dropped, do not mention it as working.
- 예제 (Example): Intro sentence, then include each anchor in its own fenced fsharp block with a 1–2 sentence explanation that QUOTES the verified generated literal from Task 2:
  ```fsharp
  {{#include ../../examples/ch06-pojo-patterns/src/App.fs:anonrecord-pattern}}
  ```
  (repeat for jspojo-pattern, createobj-pattern, jsoptions-pattern, async-boundary; and promise-ce ONLY if kept). After each POJO anchor, state the emitted object literal (e.g. `{ term: "hello" }` — note caseSensitive omitted). After async-boundary, explain the Promise→Async bridge and that StartImmediate (or the verified fallback) fires it from the click.
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` → `npm run dev`; click the 데이터 불러오기 button to fetch a todo and see it appear in the output area (runtime network call). Also instruct opening src/App.fs.js to confirm each POJO compiled to a plain `{ ... }` literal (NOT a Record class). Do NOT paste raw .fs.js or terminal output.
- 핵심 포인트 (Key Points): 5–7 bullets: F# 레코드는 POJO가 아님(extends Record); 4패턴 선택 기준(anon/JS.Pojo/createObj/jsOptions); [<JS.Pojo>]는 [<AllowNullLiteral>]와 함께; Promise(hot) vs Async(cold); Async.AwaitPromise / Async.StartAsPromise; Async.RunSynchronously는 Fable에 없음 → Async.StartImmediate; (if kept) promise{} CE는 Fable.Promise 패키지; 생성 JS는 src/App.fs.js에서 확인.

Then run `mdbook build` and confirm Ch.6 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch06-pojo-patterns/index.html`. Anchors resolved in rendered HTML: `grep -q 'SearchOptions' book/ch06-pojo-patterns/index.html && grep -q 'createObj' book/ch06-pojo-patterns/index.html && grep -q 'jsOptions' book/ch06-pojo-patterns/index.html && grep -q 'AwaitPromise' book/ch06-pojo-patterns/index.html`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch06-pojo-patterns/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch06-pojo-patterns/index.md`. Record-not-POJO warning + RunSynchronously note present: `grep -q 'Record' src/ch06-pojo-patterns/index.md && grep -q 'RunSynchronously' src/ch06-pojo-patterns/index.md`. All code via include: `grep -c '#include' src/ch06-pojo-patterns/index.md` returns >= 5. 개념 prose-only: manual review.
  </verify>
  <done>src/ch06-pojo-patterns/index.md follows the template in Korean, 개념 prose-only, includes the four POJO anchors + async-boundary (+ promise-ce only if it built), presents the POJO decision table, warns records are not POJOs, explains the async boundary (AwaitPromise/StartAsPromise/StartImmediate, RunSynchronously absent), quotes the verified emitted literals, uses plain blockquotes, and mdbook build renders the chapter.</done>
</task>

</tasks>

<verification>
- `cd examples/ch06-pojo-patterns && dotnet tool restore && npm install && npm run build` exits 0 (Success Criteria 3 + 4 + phase-wide build gate).
- Open Q #1 (Fable.Promise CE) and Open Q #3 (Async.StartImmediate) resolved BY BUILD with the outcome documented (kept or fallback) — not assumed.
- App.fs.js inspected; all four POJO patterns confirmed to emit plain object literals (none is extends Record); async boundary compiled.
- App.fs has anonrecord/jspojo/createobj/jsoptions/async anchors + working fetch button; no EntryPoint; no legacy [<Pojo>]/[<ParamObject>]; no Ch.7 concepts.
- src/ch06-pojo-patterns/index.md follows the authoring template, Korean, 개념 prose-only, code via {{#include}} only, plain blockquotes, includes the POJO decision table + record-not-POJO warning + async-boundary explanation.
- `mdbook build` exits 0 and book/ch06-pojo-patterns/index.html contains all anchored code.
</verification>

<success_criteria>
INTR-02 (POJO half) + INTR-03 satisfied: a reader runs the Ch.6 example, selects among [<JS.Pojo>]/createObj/jsOptions/anonymous record (each verified to emit a plain object literal in src/App.fs.js), and runs Promise↔Async conversion code (fetch → Async.AwaitPromise → DOM, fired by Async.StartImmediate). The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors. (Phase 3 Success Criteria 3 and 4.)
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-interop-axis/03-03-SUMMARY.md` recording: the example files created, verified build output, the ACTUAL generated POJO literals for all four patterns (confirming none is a Record class), and — critically — the resolutions of the open questions: (Q1) did Fable.Promise 3.2.0 promise{} CE build with Fable 5.3.0 (kept) or fail (fell back to Async.AwaitPromise), including any error excerpt; (Q3) did Async.StartImmediate compile (or did the StartWithContinuations fallback get used). These resolutions inform Ch.7 (which uses Promise) and future async chapters.
</output>
