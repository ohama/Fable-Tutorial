---
phase: 04-elmish-and-ui-axis
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - examples/ch10-routing/App.fsproj
  - examples/ch10-routing/package.json
  - examples/ch10-routing/vite.config.js
  - examples/ch10-routing/index.html
  - examples/ch10-routing/src/App.fs
  - examples/ch10-routing/package-lock.json
  - src/ch10-routing/index.md

must_haves:
  truths:
    - "Reader runs `cd examples/ch10-routing && dotnet tool restore && npm install && npm run build` and it exits 0, producing examples/ch10-routing/src/App.fs.js"
    - "examples/ch10-routing/src/App.fs implements a 2-3 page client-side SPA: a Page DU (e.g. Home | About | Counter/NotFound), a State holding the current URL segment list, and a view that pattern-matches the current URL segments to render the matching page — so changing the URL swaps the rendered view (SC-3, UI-03)"
    - "PRIMARY path: routing uses Feliz.Router 4.0.0 `React.router [ router.onUrlChanged (UrlChanged >> dispatch); router.children [ ... ] ]` (NOT old Router.router), with `Router.currentUrl()` to read segments and `Router.format`/`Router.navigatePath` for links; default hash mode (URL like #/about) needs no server config"
    - "Feliz.Router 4.0.0 + Fable.Elmish 5.0.2 runtime compat is RESOLVED BY BUILD (RESEARCH Open Q #1): add Feliz.Router 4.0.0 and BUILD FIRST; if NuGet resolution / Cmd.ofEffect / compile fails, FALL BACK to hand-rolled hash routing (window.location.hash + a hashchange listener dispatching into the model) and document the fallback. The EXACT Feliz.Router version that resolves is recorded."
    - "The app mounts the URL-driven view onto `<div id=\"root\">` (Program.withReactSynchronous \"root\" for the Feliz.Router path, or ReactDOM.createRoot for the hand-rolled fallback — whichever the build supports); react/react-dom pinned at ^18.3 as runtime dependencies; package-lock.json committed to lock the resolved version"
    - "src/ch10-routing/index.md follows the authoring template in Korean (개념→예제→실행하기→핵심 포인트), pulls ALL F# code via {{#include}} anchors, uses PLAIN `>` blockquotes (no [!NOTE]), no mermaid fence; mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch10-routing/src/App.fs"
      provides: "2-3 page SPA: Page DU + URL-segment State + URL→page parse + per-page views + router view (React.router OR hand-rolled hashchange fallback), per-concept ANCHORs"
      contains: "onUrlChanged"
    - path: "examples/ch10-routing/App.fsproj"
      provides: "net10.0 fsproj, RootNamespace Ch10Router, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0 + Fable.Elmish 5.0.2 + Fable.Elmish.React 5.0.1 + Feliz 3.3.3 + Feliz.Router 4.0.0 (Feliz.Router removed only if fallback path is taken)"
      contains: "Feliz"
    - path: "examples/ch10-routing/package.json"
      provides: "dev/build scripts (--verbose), react@^18.3 + react-dom@^18.3 in dependencies, vite ^6 in devDependencies"
      contains: "react-dom"
    - path: "examples/ch10-routing/index.html"
      provides: "<div id=\"root\"></div> mount node + <script type=module src=./src/App.fs.js>"
      contains: "id=\"root\""
    - path: "src/ch10-routing/index.md"
      provides: "Full Ch.10 prose: SPA client-side routing concept, hash vs path mode, React.router + onUrlChanged + children, URL-segment pattern matching, navigation links"
      min_lines: 70
  key_links:
    - from: "src/ch10-routing/index.md"
      to: "examples/ch10-routing/src/App.fs"
      via: "{{#include ...:pages}} (and router/types/mount anchors)"
      pattern: "#include.*ch10-routing/src/App.fs:"
    - from: "examples/ch10-routing/src/App.fs view"
      to: "URL change → view swap"
      via: "React.router [ router.onUrlChanged (UrlChanged >> dispatch); router.children [ match parseUrl ... ] ] (or hashchange listener)"
      pattern: "onUrlChanged|onhashchange|hashchange"
    - from: "examples/ch10-routing/src/App.fs"
      to: "React DOM #root"
      via: "Program.withReactSynchronous \"root\" |> Program.run (or ReactDOM.createRoot fallback)"
      pattern: "withReactSynchronous|createRoot"
    - from: "examples/ch10-routing/index.html"
      to: "examples/ch10-routing/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js> + div#root"
      pattern: "src/App\\.fs\\.js"
---

<objective>
Create the standalone examples/ch10-routing project and write Ch.10 (SPA 라우팅), which teaches client-side routing for a single-page application: a Page DU, a State holding the current URL as a segment list, a URL→page parse function, per-page views, and a router that swaps the rendered view when the URL changes. The PRIMARY implementation uses Feliz.Router 4.0.0 (`React.router` with `router.onUrlChanged` + `router.children`, `Router.currentUrl()`, `Router.format`/`navigatePath` for links, default hash mode). Because RESEARCH Open Q #1 flags Feliz.Router 4.0.0 + Fable.Elmish 5.0.2 runtime compat as not live-tested, the plan BUILDS FIRST and FALLS BACK to hand-rolled hash routing (window.location.hash + hashchange listener) only if the package fails to resolve/compile.

Purpose: Satisfies UI-03 (Feliz.Router SPA routing) and Phase 4 Success Criteria 3 (multi-page SPA routing, view swaps on URL change) and 4 (npm run build exit 0). Reuses the React 18 + Feliz 3.3.3 stack proven in Ch.8 (04-01). (UI-03)
Output: A runnable examples/ch10-routing that builds with `npm run build` (exit 0), an App.fs with a 2-3 page SPA whose view changes with the URL (Feliz.Router primary, hand-rolled hash fallback documented), and a complete Korean Ch.10 chapter with code via {{#include}} anchors only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-elmish-and-ui-axis/04-RESEARCH.md
@.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

This is a wave-2 plan for Phase 4. It depends on 04-01 (Ch.8) ONLY conceptually — 04-01 established and empirically locked the React 18 + Feliz 3.3.3 stack (react@^18.3 / react-dom@^18.3 in dependencies, no @vitejs/plugin-react). Files are DISJOINT from 04-02 (Ch.9), so 04-02 and 04-03 run in parallel within wave 2. Do NOT read or depend on any 04-02 ↔ 04-03 shared file.

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 1 scaffolds the Feliz.Router primary; Task 2 builds, RESOLVES the Feliz.Router compat open question BY BUILD (with a documented hand-rolled hash-routing fallback), and inspects output; Task 3 writes prose quoting the verified behavior, then runs mdbook build. Do not reorder or parallelize.

Copy the verified runnable example pattern from Ch.6 (examples/ch06-pojo-patterns/) and mirror Ch.8's Phase-4 scaffolding (examples/ch08-elmish/): fsproj at example ROOT, `<Compile Include="src/App.fs" />`, Fable emits src/App.fs.js adjacent to source, index.html loads ./src/App.fs.js. The shared dotnet tool manifest lives at repo-root `.config/dotnet-tools.json` (Fable 5.3.0 pinned) — `dotnet tool restore` from the example dir resolves it. Name = "ch10-routing", RootNamespace Ch10Router.

SAME as Ch.8 (reuse exactly):
- package.json: react@^18.3 + react-dom@^18.3 as runtime `dependencies` (NOT devDependencies — RESEARCH Pitfall 4). vite ^6 in devDependencies. dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`.
- vite.config.js IDENTICAL to Ch.6/Ch.8 (ignore **/*.fs, **/*.fsproj, **/obj/**). NO @vitejs/plugin-react — Feliz emits React.createElement directly.
- index.html uses `<div id="root"></div>` (RESEARCH Pitfall 1: the id MUST match the mount target string).
- PRIMARY mount is the Elmish whole-app pattern (`Program.mkProgram init update view |> Program.withReactSynchronous "root" |> Program.run` from `open Elmish.React`) — same as Ch.8 — because Feliz.Router's view integrates naturally with an Elmish program. (The hand-rolled fallback may instead use ReactDOM.createRoot — see Task 2.)

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — PLAIN markdown `>` blockquotes only (the admonish preprocessor is COMMENTED OUT — use plain `>`).
- mermaid is UNAVAILABLE (preprocessor commented out). Any routing-flow diagram MUST be ASCII art / prose inside a plain ```text fence — NOT a ```mermaid fence.
- ALL code via {{#include}} anchors. Include path from src/ch10-routing/index.md is `../../examples/ch10-routing/src/App.fs:anchor`.
- `--verbose` required in dev script (Fable #3631 deadlock).
- Versions (pin EXACTLY, RESEARCH Standard Stack): Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Feliz 3.3.3, Feliz.Router 4.0.0, net10.0, Vite ^6, react@^18.3, react-dom@^18.3 (react@18 ONLY — Feliz 3.3.3 does NOT support React 19).
- `.gitignore` already covers fable_modules/, **/*.fs.js, dist/, node_modules/ — do NOT commit generated files; DO commit package-lock.json.

Feliz.Router facts (RESEARCH Pattern 3 + Code Examples ch10 + Pitfall 8):
- The component is `React.router [ ... ]` (NOT `Router.router` — renamed in Feliz.Router 4.0.0, Pitfall 8).
- `router.onUrlChanged (fn)` — callback receiving the new `string list` of URL segments; wire as `router.onUrlChanged (UrlChanged >> dispatch)`.
- `router.children [ elem ]` — the content to render (pattern-match the current URL segments here).
- `Router.currentUrl() : string list` — read current segments at init.
- `Router.format "about"` / `Router.format [ "about" ]` — build an href for hash navigation; `Router.navigatePath` for path-mode navigation; `Cmd.navigate "seg"` for programmatic navigation from update.
- Hash mode is the DEFAULT (URL like `#/about` → segments `["about"]`) and needs no server config — USE IT for the tutorial. Path mode needs server rewrites — do NOT use.
- Required opens: `open Feliz`, `open Feliz.Router`, `open Elmish`, `open Elmish.React` (for withReactSynchronous mount).
- update is `Msg -> State -> State * Cmd<Msg>`; NEVER dispatch inside update; NEVER store dispatch in state (Pitfall 10).

Hand-rolled fallback facts (ONLY if Feliz.Router fails to build — RESEARCH Open Q #1 / Don't-Hand-Roll note):
- Drop Feliz.Router from fsproj. Read `Browser.Dom.window.location.hash`, strip the leading `#`/`#/`, split on `/` to segments. Register a `window.addEventListener("hashchange", ...)` (or set `window.onhashchange`) in an Elmish subscription (`Program.withSubscription`) that dispatches `UrlChanged segments`; the subscription's IDisposable MUST removeEventListener on Dispose (RESEARCH Pitfall 9). Links use plain `prop.href "#/about"`. Keep the same Page DU + parseUrl + per-page views so the chapter narrative is identical; only the routing mechanism differs.

State of the art (do NOT use): `Router.router` (old name → use React.router); `ReactDOM.render` (deprecated → createRoot only if fallback uses it); `React.functionComponent` (legacy → not needed here).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch10-routing example + App.fs with a 2-3 page Feliz.Router SPA</name>
  <files>examples/ch10-routing/App.fsproj, examples/ch10-routing/package.json, examples/ch10-routing/vite.config.js, examples/ch10-routing/index.html, examples/ch10-routing/src/App.fs</files>
  <action>
Create the example directory mirroring Ch.6/Ch.8's verified layout (examples/ch06-pojo-patterns/, examples/ch08-elmish/).

`App.fsproj`: net10.0, RootNamespace Ch10Router, `<Compile Include="src/App.fs" />`, PackageReferences: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Feliz 3.3.3, Feliz.Router 4.0.0.

`package.json`: name "ch10-routing", private, version 0.1.0, type module, scripts dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`. `dependencies`: { "react": "^18.3", "react-dom": "^18.3" }. `devDependencies`: { "vite": "^6.0.0" }. (react/react-dom in dependencies NOT devDependencies — RESEARCH Pitfall 4.)

`vite.config.js`: identical to Ch.6/Ch.8 (defineConfig, server.watch.ignored = ["**/*.fs", "**/*.fsproj", "**/obj/**"]). NO @vitejs/plugin-react.

`index.html`: lang ko, charset UTF-8, viewport meta, title "Ch10 - SPA 라우팅", body has `<div id="root"></div>` and `<script type="module" src="./src/App.fs.js"></script>`. (id="root" MUST match Program.withReactSynchronous "root".)

`src/App.fs` — a 2-3 page SPA using Feliz.Router. ANCHORs (each marking a teachable unit): `types`, `pages`, `router`, `program`. No `[<EntryPoint>]`. Use this verified skeleton (RESEARCH Pattern 3 / Code Examples ch10-pages + ch10-router):
```fsharp
module App

open Elmish
open Elmish.React
open Feliz
open Feliz.Router

// ANCHOR: types
type State = { CurrentUrl: string list }

type Msg =
    | UrlChanged of string list

let init () : State * Cmd<Msg> =
    { CurrentUrl = Router.currentUrl() }, Cmd.none

let update (msg: Msg) (state: State) : State * Cmd<Msg> =
    match msg with
    | UrlChanged url -> { state with CurrentUrl = url }, Cmd.none
// ANCHOR_END: types

// ANCHOR: pages
let homePage () =
    Html.div [
        prop.children [
            Html.h1 [ prop.text "홈" ]
            Html.a [ prop.href (Router.format [ "about" ]); prop.text "About으로" ]
        ]
    ]

let aboutPage () =
    Html.div [
        prop.children [
            Html.h1 [ prop.text "About" ]
            Html.a [ prop.href (Router.format []); prop.text "홈으로" ]
        ]
    ]

let notFoundPage () =
    Html.div [ prop.children [ Html.h1 [ prop.text "404 - 페이지 없음" ] ] ]
// ANCHOR_END: pages

// ANCHOR: router
let view (state: State) (dispatch: Msg -> unit) : ReactElement =
    React.router [
        router.onUrlChanged (UrlChanged >> dispatch)
        router.children [
            match state.CurrentUrl with
            | []          -> homePage ()
            | [ "about" ] -> aboutPage ()
            | _           -> notFoundPage ()
        ]
    ]
// ANCHOR_END: router

// ANCHOR: program
Program.mkProgram init update view
|> Program.withReactSynchronous "root"
|> Program.run
// ANCHOR_END: program
```
Keep focused on SPA routing (URL → view swap). Use `React.router` (NOT Router.router). Default hash mode (no path-mode config). Do NOT use Feliz.UseElmish (Ch.9). Do NOT use React.functionComponent.
  </action>
  <verify>
`test -f examples/ch10-routing/App.fsproj && test -f examples/ch10-routing/src/App.fs && test -f examples/ch10-routing/index.html && test -f examples/ch10-routing/package.json && test -f examples/ch10-routing/vite.config.js`. Anchors present: `for a in types pages router program; do grep -q "ANCHOR: $a" examples/ch10-routing/src/App.fs || exit 1; done`. Router API present (new name): `grep -q 'React.router' examples/ch10-routing/src/App.fs && grep -q 'router.onUrlChanged' examples/ch10-routing/src/App.fs && ! grep -q 'Router.router' examples/ch10-routing/src/App.fs`. URL parse present: `grep -q 'Router.currentUrl' examples/ch10-routing/src/App.fs && grep -q 'Router.format' examples/ch10-routing/src/App.fs`. Mount present: `grep -q 'withReactSynchronous' examples/ch10-routing/src/App.fs`. id=root: `grep -q 'id="root"' examples/ch10-routing/index.html`. Packages pinned: `grep -q 'Feliz.Router' examples/ch10-routing/App.fsproj && grep -q 'Fable.Elmish.React' examples/ch10-routing/App.fsproj`. react in dependencies: `grep -q '"react"' examples/ch10-routing/package.json && grep -q '"react-dom"' examples/ch10-routing/package.json`. `grep -q -- '--verbose' examples/ch10-routing/package.json`. `! grep -q 'EntryPoint' examples/ch10-routing/src/App.fs`. Legacy guards: `! grep -q 'React.functionComponent' examples/ch10-routing/src/App.fs && ! grep -q 'useElmish' examples/ch10-routing/src/App.fs`.
  </verify>
  <done>examples/ch10-routing scaffolded; App.fs has a 2-3 page SPA (Page/URL state + parseUrl + per-page views) using React.router with router.onUrlChanged + router.children + Router.currentUrl/format, mounted via Program.withReactSynchronous "root"; index.html has div#root; fsproj pins Feliz.Router 4.0.0; react/react-dom in dependencies; --verbose dev script; no EntryPoint; no Router.router/useElmish/functionComponent.</done>
</task>

<task type="auto">
  <name>Task 2: Build, RESOLVE Feliz.Router 4.0.0 compat BY BUILD (hand-rolled hash fallback), inspect output</name>
  <files>examples/ch10-routing/App.fsproj, examples/ch10-routing/src/App.fs (only if fallback or build fix needed), examples/ch10-routing/package-lock.json, examples/ch10-routing/ (build + inspect)</files>
  <action>
From `examples/ch10-routing/`:
1. `dotnet tool restore` (exit 0 — resolves Fable 5.3.0 from repo-root .config/dotnet-tools.json), `npm install` (exit 0 — installs react@^18.3, react-dom@^18.3, vite). This generates package-lock.json.
2. `npm run build` (`dotnet fable --run npx vite build`) — attempt with Feliz.Router 4.0.0. Fix trivial compile errors until exit 0.

OPEN QUESTION #1 — Feliz.Router 4.0.0 + Fable.Elmish 5.0.2 runtime/restore compat (RESOLVE BY BUILD, do NOT assume — RESEARCH Open Q #1):
3. BUILD FIRST with Feliz.Router 4.0.0. If `dotnet tool restore`/`dotnet fable` step:
   - SUCCEEDS → KEEP the Feliz.Router path. Record the EXACT Feliz.Router version that resolved (from obj/project.assets.json or `dotnet list examples/ch10-routing/App.fsproj package`). This is the expected outcome (NuGet semver: Fable.Elmish 5.0.2 >= the package's `>= 4.0.0` requirement).
   - FAILS with a NuGet resolution error, a `Cmd.ofEffect`/missing-API compile error, or a hard Feliz.Router incompatibility → FALL BACK to hand-rolled hash routing:
     a. Remove `<PackageReference Include="Feliz.Router" ... />` from App.fsproj; remove `open Feliz.Router`.
     b. Rewrite the `types`/`router`/`program` anchors to use a hashchange listener: parse `Browser.Dom.window.location.hash` (strip leading `#`/`#/`, split on `/`, drop empties) into a `string list`; init reads the current hash; register a `window.addEventListener("hashchange", ...)` via `Program.withSubscription` that dispatches `UrlChanged (parseHash())`; the subscription IDisposable MUST `removeEventListener` on Dispose (RESEARCH Pitfall 9). Links keep `prop.href "#/about"` (use a small `formatHash` helper instead of Router.format). KEEP the same Page DU + parseUrl + per-page views + anchor names so the chapter narrative is unchanged; only the routing mechanism differs. (Mount may stay Program.withReactSynchronous, or switch to ReactDOM.createRoot if simpler — record which.)
     c. Rebuild to exit 0.
   Record clearly in findings: which path was taken (Feliz.Router KEPT @ resolved version, or hand-rolled fallback) and why.

react@^18.3 lock:
4. Inspect `examples/ch10-routing/package-lock.json` and record the EXACT resolved react / react-dom version. package-lock.json MUST be committed so subsequent installs are reproducible. (If npm failed to resolve `^18.3`, pin exact `react@18.3.1 react-dom@18.3.1` and re-lock.)

INSPECT `examples/ch10-routing/src/App.fs.js` and record for Task 3:
- Confirm the file exists and the router/view is emitted (React.createElement-style calls from the Feliz views — confirming NO JSX / no @vitejs/plugin-react needed).
- Confirm the URL-change handler is present (onUrlChanged from Feliz.Router, or the hashchange listener from the fallback).

Write findings for Task 3 + SUMMARY. Do NOT commit generated files (fable_modules/, *.fs.js, dist/, node_modules/); DO leave package-lock.json staged for commit.
  </action>
  <verify>
Final `cd examples/ch10-routing && npm run build` exits 0. `test -f examples/ch10-routing/src/App.fs.js && test -f examples/ch10-routing/package-lock.json`. Open-question outcome RECORDED: which routing path the build used — `grep -q 'Feliz.Router' examples/ch10-routing/App.fsproj` (Feliz.Router KEPT) OR `grep -q 'hashchange' examples/ch10-routing/src/App.fs` (fallback). URL-driven view present either way: `grep -q 'onUrlChanged\|hashchange' examples/ch10-routing/src/App.fs`. Resolved react version captured: `grep -q 'react' examples/ch10-routing/package-lock.json`. App.fs.js emitted with view: `grep -q 'createElement\|react' examples/ch10-routing/src/App.fs.js`. Findings recorded for Task 3 (path taken + resolved Feliz.Router version if kept).
  </verify>
  <done>Example builds (npm run build exit 0); Feliz.Router 4.0.0 compat (RESEARCH Open Q #1) RESOLVED by actual build — either Feliz.Router KEPT at the recorded resolved version, or hand-rolled hashchange fallback applied and documented; react@^18.3 lock captured; package-lock.json committed; App.fs.js inspected (URL-driven view emitting React.createElement, no JSX).</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.10 chapter prose (SPA routing, hash mode, URL→view swap) and verify render</name>
  <files>src/ch10-routing/index.md</files>
  <action>
Fully replace the stub `src/ch10-routing/index.md` following the authoring template. H1 `# 10장. SPA 라우팅 (SPA Routing)`, then a PLAIN `>` blockquote (NOT [!NOTE]) pointing to examples/ch10-routing + run command (`cd examples/ch10-routing && dotnet tool restore && npm install && npm run dev`). Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION: Use the actual build outcome recorded in Task 2. If Feliz.Router was kept, describe React.router / onUrlChanged / Router.format. If the hand-rolled hash fallback was used, describe the hashchange listener + window.location.hash parsing INSTEAD. Do not claim behavior that did not build. Either way the narrative is: a single page (single-page application) whose view swaps based on the URL segment list — no full page reload.

- 개념 (Concept) — PROSE ONLY, NO code blocks: Introduce client-side routing (client-side routing) for an SPA (single-page application, SPA): the browser loads ONE HTML page, and JavaScript swaps the visible view as the URL changes — without a server round-trip or full reload. Explain the URL as a list of segments (segment): `#/about` → `["about"]`, `#/` → `[]`. Explain hash mode (hash mode) vs path mode (path mode): hash mode puts the route after `#` (e.g. `#/about`) and needs NO server configuration (the server only ever serves index.html), while path mode (`/about`) needs server rewrite rules — so the tutorial uses hash mode. Explain the routing loop: the current URL lives in the model as a segment list; a URL-change event (onUrlChanged / hashchange — describe whichever Task 2 used) dispatches a `UrlChanged` Msg that updates the model; the view pattern-matches the segment list to choose which page to render; navigation links set the hash (e.g. via Router.format or a plain `#/about` href) which triggers the same loop. Note (RESEARCH Pitfall 8, if Feliz.Router kept) that the component is `React.router` (the older `Router.router` was renamed in 4.0.0). Reuse Ch.8's Elmish discipline: update is pure, never dispatch inside update. Optionally include a small routing-flow loop as a ```text fence (NOT mermaid) showing URL변경 → UrlChanged → update → view 전환.
- 예제 (Example): Intro sentence, then include each anchor in its own fenced fsharp block with a 1–2 sentence Korean explanation:
  ```fsharp
  {{#include ../../examples/ch10-routing/src/App.fs:types}}
  ```
  (repeat for pages, router, program). For the `router` anchor, explain that the view matches `state.CurrentUrl` (the segment list) — `[]` → 홈, `["about"]` → About, otherwise 404 — and that onUrlChanged (or the hashchange listener) dispatches `UrlChanged` whenever the URL changes, swapping the rendered page. For the `pages` anchor, explain the navigation links (Router.format / `#/about` href). For the `program` anchor, explain the mount onto `<div id="root">` (note the id must match).
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` → `npm run dev`; open the browser at the home page, click the "About으로" link and watch the URL change to `#/about` and the view swap to the About page WITHOUT a full reload; click "홈으로" to return; type an unknown route (e.g. `#/xyz`) to see the 404 page. Do NOT paste raw .fs.js or terminal output.
- 핵심 포인트 (Key Points): 5–7 bullets: SPA는 한 HTML 페이지에서 JS로 뷰를 교체 (전체 리로드 없음); URL을 세그먼트 리스트로 다룬다 (`#/about` → `["about"]`); hash 모드는 서버 설정이 필요 없음 (path 모드는 rewrite 필요); URL 변경 → UrlChanged Msg → update → view 전환 루프; 현재 URL은 모델에 보관, view에서 패턴 매칭으로 페이지 선택; (Feliz.Router 유지 시) 컴포넌트는 React.router (구버전 Router.router 아님); update는 순수 함수 (dispatch를 update에서 호출 금지).

Then run `mdbook build` and confirm Ch.10 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch10-routing/index.html`. Anchors resolved in rendered HTML: `grep -q 'UrlChanged' book/ch10-routing/index.html && grep -q 'currentUrl\|location.hash' book/ch10-routing/index.html && grep -q 'CurrentUrl' book/ch10-routing/index.html`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch10-routing/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch10-routing/index.md`. No mermaid fence: `! grep -q '```mermaid' src/ch10-routing/index.md`. Routing concept present: `grep -q 'SPA' src/ch10-routing/index.md && grep -q 'hash' src/ch10-routing/index.md`. All code via include: `grep -c '#include' src/ch10-routing/index.md` returns >= 4. 개념 prose-only: manual review.
  </verify>
  <done>src/ch10-routing/index.md follows the template in Korean, 개념 prose-only (no code blocks), includes the four anchors (types/pages/router/program), explains client-side routing + hash vs path mode + the URL→view-swap loop matching the build outcome (Feliz.Router OR hand-rolled fallback), uses plain blockquotes, no mermaid; mdbook build renders the chapter.</done>
</task>

</tasks>

<verification>
- `cd examples/ch10-routing && dotnet tool restore && npm install && npm run build` exits 0, producing src/App.fs.js (Success Criteria 3 + 4 + phase-wide build gate).
- App.fs has a 2-3 page SPA whose view swaps with the URL: Page/URL-segment state + parseUrl + per-page views + a URL-change handler (React.router onUrlChanged, or hand-rolled hashchange) + mount onto #root.
- Open Q #1 (Feliz.Router 4.0.0 + Fable.Elmish 5.0.2 compat) RESOLVED by build: Feliz.Router KEPT at the recorded resolved version, OR hand-rolled hash-routing fallback applied and documented. react@^18.3 lock captured; package-lock.json committed.
- index.html has div#root; react/react-dom in dependencies (not devDependencies); no @vitejs/plugin-react; default hash mode (no path-mode server config); no useElmish/functionComponent/Router.router.
- src/ch10-routing/index.md follows the authoring template, Korean, 개념 prose-only, code via {{#include}} only, plain blockquotes, no mermaid.
- `mdbook build` exits 0 and book/ch10-routing/index.html contains the anchored code.
</verification>

<success_criteria>
UI-03 satisfied and Phase 4 Success Criteria 3 + 4 met: a reader runs the Ch.10 example and sees a multi-page SPA where changing the URL (clicking links to `#/about`, `#/`, or an unknown route) swaps the rendered view without a full reload — implemented with Feliz.Router 4.0.0 (`React.router` + `onUrlChanged` + segment-list pattern matching) or a documented hand-rolled hash-routing fallback. The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-elmish-and-ui-axis/04-03-SUMMARY.md` recording: the example files created, the verified build output (npm run build exit 0), and the resolution of Open Q #1 — whether Feliz.Router 4.0.0 was KEPT (and the EXACT resolved version) or the hand-rolled hash-routing fallback was used (and why, and which mount it used). Record the EXACT resolved react/react-dom version from package-lock.json. Note that runtime browser route-switching (visible view swap on URL change) is a human-verify item, NOT a build gate. This is the final chapter of Phase 4; note for Phase 5 that the React 18 + Feliz + Elmish stack and the example pattern (react/react-dom in dependencies, no @vitejs/plugin-react) are now established across Ch.8-10.
</output>
