---
phase: 02-core-toolchain-chapters
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - examples/ch02-compile-model/App.fsproj
  - examples/ch02-compile-model/src/App.fs
  - examples/ch02-compile-model/index.html
  - examples/ch02-compile-model/package.json
  - examples/ch02-compile-model/vite.config.js
  - src/ch02-compile-model/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `dotnet tool restore && npm install && npm run build` in examples/ch02-compile-model and it exits 0"
    - "examples/ch02-compile-model/src/App.fs.js is generated and the reader can open it to SEE how each F# type compiled to JS"
    - "The example contains record, DU, numeric (int/int64/float/decimal), Option, and tuple examples each behind its own ANCHOR"
    - "Ch.2 prose accurately describes the ACTUAL generated JS (Option None value verified against App.fs.js, not assumed)"
    - "src/ch02-compile-model/index.md follows the authoring template in Korean and pulls all F# code via {{#include}} anchors"
    - "mdbook build renders Ch.2 with the anchored code visible"
  artifacts:
    - path: "examples/ch02-compile-model/src/App.fs"
      provides: "F# record/DU/numeric/Option/tuple examples with per-type ANCHORs + DOM output"
      contains: "ANCHOR: option-example"
    - path: "examples/ch02-compile-model/App.fsproj"
      provides: "net10.0 fsproj, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0, RootNamespace Ch02CompileModel"
      contains: "Fable.Core"
    - path: "src/ch02-compile-model/index.md"
      provides: "Full Ch.2 prose: F# type → JS table, generated-JS walkthrough, Option erasure (verified)"
      min_lines: 50
  key_links:
    - from: "src/ch02-compile-model/index.md"
      to: "examples/ch02-compile-model/src/App.fs"
      via: "{{#include ...:record-example}} (and du/numeric/option/tuple anchors)"
      pattern: "#include.*ch02-compile-model/src/App.fs:"
    - from: "examples/ch02-compile-model/index.html"
      to: "examples/ch02-compile-model/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js>"
      pattern: "src/App\\.fs\\.js"
---

<objective>
Create the standalone examples/ch02-compile-model project and write the Ch.2 (컴파일 모델) chapter, which teaches how F# types (record, DU, numeric, Option, tuple) compile to JS. The example must let the reader SEE the generated JS by opening the emitted `src/App.fs.js`. Crucially, prose must match the ACTUAL generated output — the Option None erasure value (null vs undefined) is an OPEN QUESTION the executor resolves by inspecting the real .fs.js, not by assuming.

Purpose: Satisfies SETUP-02 and Phase 2 Success Criterion 2 (reader confirms record/DU/numeric JS compilation and understands Option erasure) and contributes to Criterion 4 (npm run build exit 0, {{#include}} end-to-end). (SETUP-02)
Output: A runnable examples/ch02-compile-model that builds with `npm run build`, an App.fs with anchored type examples, and a complete Korean Ch.2 chapter whose generated-JS claims are verified against the real output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-toolchain-chapters/02-RESEARCH.md
@.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

Copy the verified runnable example pattern from Ch.1 (see 02-01-SUMMARY.md). Match its on-disk layout EXACTLY:
- `App.fsproj` at the example ROOT (NOT in src/), with `<Compile Include="src/App.fs" />`, net10.0.
- `src/App.fs` holds the F# source; Fable emits `src/App.fs.js` adjacent.
- `index.html` at root with `<div id="app"></div>` and `<script type="module" src="./src/App.fs.js">`.
- `package.json` dev/build scripts WITH `--verbose` (identical to Ch.1 except `name`).
- `vite.config.js` ignoring `**/*.fs`, `**/*.fsproj`, `**/obj/**`.

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on first occurrence.

CRITICAL constraints:
- NO admonish syntax (`> [!NOTE]` etc.) — plain markdown blockquotes only (STATE.md [01-01]).
- ALL code via {{#include}} anchors. Include path from src/ch02-compile-model/index.md is `../../examples/ch02-compile-model/src/App.fs:anchor`.
- `--verbose` required in dev script.
- Versions: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, net10.0, Vite ^6.
- `.gitignore` (already updated in 02-01 to cover fable_modules/, **/*.fs.js, dist/) applies repo-wide — do NOT commit this example's generated files.

Compile-model facts (RESEARCH Pattern 3): int/float → JS number; int64/uint64 → BigInt (since Fable 4.0.5); decimal → fable-library Decimal class; record → ES6 class `extends Record`; DU case → class `extends Union` with `tag` (int, 0-based) + `fields` (array); tuple → plain JS array; Some x → x unwrapped; None → erased (verify null vs undefined). Full table in RESEARCH Pattern 3.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the ch02-compile-model example project + App.fs with type anchors</name>
  <files>examples/ch02-compile-model/App.fsproj, examples/ch02-compile-model/package.json, examples/ch02-compile-model/vite.config.js, examples/ch02-compile-model/index.html, examples/ch02-compile-model/src/App.fs</files>
  <action>
Create the example directory mirroring Ch.1's verified layout (see 02-01-SUMMARY.md for the exact working files).

`App.fsproj` (at example root):
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Ch02CompileModel</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="src/App.fs" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Fable.Core" Version="5.0.0" />
    <PackageReference Include="Fable.Browser.Dom" Version="2.20.0" />
  </ItemGroup>
</Project>
```

`package.json` (identical scripts to Ch.1, name = "ch02-compile-model"): type module; scripts dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`; devDependencies vite ^6.0.0.

`vite.config.js`: identical to Ch.1 (ignore **/*.fs, **/*.fsproj, **/obj/**).

`index.html` at root: `<div id="app"></div>` + `<script type="module" src="./src/App.fs.js"></script>`, title "Ch02 - 컴파일 모델".

`src/App.fs` — F# source with one ANCHOR per type category (RESEARCH Pattern 3 App.fs structure). Include record, DU (3 cases incl. nullary), numeric (int, int64, float, decimal), Option (Some + None), tuple. End with DOM output so the example is runnable. Use these anchor names exactly so the chapter includes resolve: `record-example`, `du-example`, `numeric-example`, `option-example`, `tuple-example`. Example shape:

```fsharp
module App

open Browser

// ANCHOR: record-example
type Person = { Name: string; Age: int }
let alice = { Name = "Alice"; Age = 30 }
// ANCHOR_END: record-example

// ANCHOR: du-example
type Shape =
    | Circle of radius: float
    | Rectangle of width: float * height: float
    | Point
let shape1 = Circle 3.14
let shape2 = Rectangle(10.0, 5.0)
let shape3 = Point
// ANCHOR_END: du-example

// ANCHOR: numeric-example
let i : int = 42
let f : float = 3.14
let i64 : int64 = 9999999999999L
let dec : decimal = 1.5m
// ANCHOR_END: numeric-example

// ANCHOR: option-example
let someValue = Some 42
let noneValue : int option = None
// ANCHOR_END: option-example

// ANCHOR: tuple-example
let pair = (1, "hello")
// ANCHOR_END: tuple-example

let app = document.getElementById "app"
app.innerHTML <- sprintf "<h1>컴파일 모델 예제</h1><p>%s, %d세</p>" alice.Name alice.Age
```

No `[<EntryPoint>]` (browser top-level). All values must actually be USED or referenced so Fable emits them (the sprintf uses alice; reference the others if Fable tree-shakes unused bindings — if any binding is omitted from App.fs.js, add a line that consumes it, e.g. append their string forms to innerHTML).
  </action>
  <verify>
`test -f examples/ch02-compile-model/App.fsproj && test -f examples/ch02-compile-model/src/App.fs && test -f examples/ch02-compile-model/index.html && test -f examples/ch02-compile-model/package.json && test -f examples/ch02-compile-model/vite.config.js`. `grep -q 'ANCHOR: record-example' examples/ch02-compile-model/src/App.fs && grep -q 'ANCHOR: du-example' examples/ch02-compile-model/src/App.fs && grep -q 'ANCHOR: numeric-example' examples/ch02-compile-model/src/App.fs && grep -q 'ANCHOR: option-example' examples/ch02-compile-model/src/App.fs && grep -q 'ANCHOR: tuple-example' examples/ch02-compile-model/src/App.fs`. `grep -q -- '--verbose' examples/ch02-compile-model/package.json`. `! grep -q 'EntryPoint' examples/ch02-compile-model/src/App.fs`.
  </verify>
  <done>examples/ch02-compile-model scaffolded matching Ch.1's verified pattern; App.fs has record/du/numeric/option/tuple anchors and DOM output, no EntryPoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build the example and INSPECT generated JS (resolve Option None open question)</name>
  <files>examples/ch02-compile-model/ (build + inspect — no source edits unless build fails)</files>
  <action>
From `examples/ch02-compile-model/`:
1. `dotnet tool restore` (exit 0) — Fable 5.3.0 from repo-root manifest.
2. `npm install` (exit 0) — vite.
3. `npm run build` (exit 0) — Fable compiles src/App.fs → src/App.fs.js; Vite bundles dist/.

If build fails, fix and retry until exit 0 (do not mark done otherwise).

Then INSPECT `examples/ch02-compile-model/src/App.fs.js` and record the ACTUAL generated representation for each type. This is load-bearing for prose accuracy (RESEARCH Open Question 2). Specifically capture:
- Record `Person`: confirm it compiles to a class (note `extends Record` or equivalent import).
- DU `Shape`: confirm `tag` + `fields`; note the tag numbering (Circle=0, Rectangle=1, Point=2).
- Numerics: confirm `i64` is a BigInt literal (e.g. `9999999999999n` or BigInt(...)) and `dec` uses a Decimal type from fable-library; `i`/`f` are plain numbers.
- **Option None — THE open question:** find the line for `noneValue` in App.fs.js. Record whether it is `null`, `void 0`/`undefined`, or omitted. Also note how `someValue` (Some 42) appears (likely `42` directly). Write down the exact tokens — the Ch.2 prose in Task 3 MUST quote what you actually saw, not the historical "None = null" shorthand.
- Tuple `pair`: confirm it is a plain JS array `[1, "hello"]`.

Save these observations for the SUMMARY and for writing accurate prose. Do not commit generated files (gitignored).
  </action>
  <verify>
`cd examples/ch02-compile-model && dotnet tool restore && npm install && npm run build` exits 0. `test -f examples/ch02-compile-model/src/App.fs.js`. The inspection produced concrete findings for record/DU/numeric/Option/tuple (recorded in execution notes for Task 3 + SUMMARY). Confirm the Option None token was actually located in App.fs.js (grep the file for the noneValue binding).
  </verify>
  <done>Example builds (npm run build exit 0); App.fs.js generated and inspected; the actual JS representation of each type — especially Option None (null vs undefined) — is recorded for use in prose.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.2 chapter prose (generated-JS accurate) and verify render</name>
  <files>src/ch02-compile-model/index.md</files>
  <action>
Fully rewrite `src/ch02-compile-model/index.md` following the authoring template. H1 `# 2장. 컴파일 모델 (Compile Model)`, then a plain blockquote (NOT [!NOTE]) pointing to examples/ch02-compile-model + run command. Korean prose, English term in parens on first occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

- 개념 (Concept): Explain that Fable does not interpret F# at runtime — it COMPILES each F# type to a specific JS shape, and the reader can inspect this in the emitted `src/App.fs.js` (adjacent to source). Introduce the F# 타입 → JS 표현 mapping at a high level. Include a Korean prose table mapping: int/float → number; int64 → BigInt; decimal → fable-library Decimal 클래스; record → ES6 클래스(`extends Record`); DU → tag+fields 클래스(`extends Union`); tuple → JS 배열; Some x → x (값 그대로); None → [USE THE VERIFIED TOKEN from Task 2]. Plant the awareness seed (RESEARCH Pitfall 6): "F# 레코드는 일반 JS 객체(POJO)가 아닙니다 — 4장·6장에서 JS 라이브러리에 데이터를 넘기는 패턴을 배웁니다." Do NOT teach POJO patterns here.
- 예제 (Example): Brief intro sentence, then include each anchor in its own fenced fsharp block with a 1–2 sentence explanation after each:
  ```fsharp
  {{#include ../../examples/ch02-compile-model/src/App.fs:record-example}}
  ```
  (repeat for du-example, numeric-example, option-example, tuple-example). After option-example, write the VERIFIED Option erasure explanation using the actual token observed in Task 2. If None compiled to `undefined`, say so precisely; if `null`, say so. Use the RESEARCH Pitfall 4 phrasing as a base but make it match reality: e.g. "None은 런타임에 `undefined`가 됩니다. Fable은 `x == null` (loose equality)로 null과 undefined를 모두 None으로 처리합니다." Call out int64 → BigInt and decimal → Decimal 클래스 explicitly (RESEARCH Pitfall 5) so readers know they are NOT plain numbers.
- 실행하기 (Running): Prose describing `dotnet tool restore` → `npm install` → `npm run dev`, then the key instruction: open `examples/ch02-compile-model/src/App.fs.js` in your editor to see the compiled output. Describe in prose what they will find (Person 클래스, Shape의 tag/fields, BigInt, None 표현). Do NOT paste raw .fs.js or terminal output — describe in prose.
- 핵심 포인트 (Key Points): 4–6 bullets covering: record→클래스(POJO 아님); DU→tag+fields; int64→BigInt / decimal→Decimal(plain number 아님); Some는 값 그대로 / None은 [verified token]; tuple→배열; 생성 JS는 src/App.fs.js에서 직접 확인.

Then run `mdbook build` and confirm Ch.2 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch02-compile-model/index.html`. All five anchors resolved in the rendered HTML: `grep -q 'type Person' book/ch02-compile-model/index.html && grep -q 'type Shape' book/ch02-compile-model/index.html && grep -q 'int64' book/ch02-compile-model/index.html && grep -q 'option' book/ch02-compile-model/index.html`. Template sections present: `grep -q '개념' src/ch02-compile-model/index.md && grep -q '실행하기' src/ch02-compile-model/index.md && grep -q '핵심 포인트' src/ch02-compile-model/index.md`. No admonish: `! grep -q '\[!NOTE\]' src/ch02-compile-model/index.md`. Prose mentions BigInt and the verified None token (manual review: the Option explanation matches what Task 2 observed in App.fs.js).
  </verify>
  <done>src/ch02-compile-model/index.md follows the template in Korean, includes all five type anchors, states the Option None erasure using the VERIFIED generated-JS token (not an assumption), calls out int64/decimal as non-number types, uses plain blockquotes, and mdbook build renders the chapter with anchored code visible.</done>
</task>

</tasks>

<verification>
- `cd examples/ch02-compile-model && dotnet tool restore && npm install && npm run build` exits 0 (Success Criterion 2 + 4).
- src/App.fs.js generated and inspected; the Option None representation in prose matches the actual token in App.fs.js (Open Question resolved with evidence).
- App.fs has record/du/numeric/option/tuple anchors; example produces visible DOM output; no EntryPoint.
- src/ch02-compile-model/index.md follows the authoring template, Korean, code via {{#include}} only, plain blockquotes, calls out int64→BigInt and decimal→Decimal.
- `mdbook build` exits 0 and book/ch02-compile-model/index.html contains all anchored code.
</verification>

<success_criteria>
SETUP-02 satisfied: a reader runs the Ch.2 example, opens the generated src/App.fs.js, and sees how F# record/DU/numeric/Option/tuple types compile to JS — with the Option erasure behavior accurately documented from the real output. The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-toolchain-chapters/02-02-SUMMARY.md` recording: the example project files created, the verified build outputs, and — most importantly — the ACTUAL generated-JS findings for each type (record class shape, DU tag numbering, int64 BigInt form, decimal form, and the exact Option None token observed: null vs undefined). Note explicitly how the verified None token compares to the historical "None = null" shorthand.
</output>
