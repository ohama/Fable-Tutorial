---
phase: 02-core-toolchain-chapters
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - examples/ch03-fable-core/App.fsproj
  - examples/ch03-fable-core/src/App.fs
  - examples/ch03-fable-core/index.html
  - examples/ch03-fable-core/package.json
  - examples/ch03-fable-core/vite.config.js
  - src/ch03-fable-core/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `dotnet tool restore && npm install && npm run build` in examples/ch03-fable-core and it exits 0"
    - "The example demonstrates intro-level Fable.Core: jsNative, emitJsExpr, [<Global>], a basic [<Import>], Browser DOM, plus .NET BCL supported/unsupported examples — each behind an ANCHOR"
    - "Ch.3 prose explicitly states the scope boundary: what is taught here vs deferred to Phase 3 (no [<Emit>] for binding, no erased unions/U2-U9, no StringEnum, no POJO, no dynamic ?)"
    - "Ch.3 prose maps the namespaces (Fable.Core vs Fable.Core.JsInterop vs Browser) and shows the correct `open` statements"
    - "Ch.3 prose includes a .NET BCL support/limits table (works: string/Math/DateTime/Regex; not available: Thread/File/Async.RunSynchronously)"
    - "src/ch03-fable-core/index.md follows the authoring template in Korean and pulls all code via {{#include}} anchors; mdbook build renders it"
  artifacts:
    - path: "examples/ch03-fable-core/src/App.fs"
      provides: "Intro Fable.Core demo: jsNative/[<Global>], emitJsExpr, BCL supported + unsupported anchors + DOM output"
      contains: "emitJsExpr"
    - path: "examples/ch03-fable-core/App.fsproj"
      provides: "net10.0 fsproj, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0, RootNamespace Ch03FableCore"
      contains: "Fable.Browser.Dom"
    - path: "src/ch03-fable-core/index.md"
      provides: "Full Ch.3 prose: scope boundary, namespace map, BCL support table, worked examples"
      min_lines: 55
  key_links:
    - from: "src/ch03-fable-core/index.md"
      to: "examples/ch03-fable-core/src/App.fs"
      via: "{{#include ...:emit-example}} (and jsnative/bcl anchors)"
      pattern: "#include.*ch03-fable-core/src/App.fs:"
    - from: "examples/ch03-fable-core/index.html"
      to: "examples/ch03-fable-core/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js>"
      pattern: "src/App\\.fs\\.js"
---

<objective>
Create the standalone examples/ch03-fable-core project and write the Ch.3 (Fable.Core 기초) chapter, an INTRO-level survey of Fable.Core: jsNative, emitJsExpr/emitJsStatement basics, basic [<Global>]/[<Import>], Browser DOM as the practical payoff, and a .NET BCL support/limits table. The chapter must EXPLICITLY state the scope boundary with Phase 3 (no [<Emit>] for library binding, no erased unions, no StringEnum, no POJO, no dynamic `?` — those are Ch.4–7) to avoid overlap.

Purpose: Satisfies SETUP-03 and Phase 2 Success Criterion 3 (reader grasps Fable.Core's basic API range and .NET stdlib support/limits) and contributes to Criterion 4 (npm run build exit 0, {{#include}} end-to-end). (SETUP-03)
Output: A runnable examples/ch03-fable-core that builds with `npm run build`, an App.fs with anchored intro-interop examples, and a complete Korean Ch.3 chapter with an explicit Phase-3 scope boundary, namespace map, and BCL table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-toolchain-chapters/02-RESEARCH.md
@.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

Copy the verified runnable example pattern from Ch.1 (see 02-01-SUMMARY.md). Match its on-disk layout EXACTLY:
- `App.fsproj` at example ROOT (NOT src/), `<Compile Include="src/App.fs" />`, net10.0.
- `src/App.fs` source; Fable emits `src/App.fs.js`.
- `index.html` at root: `<div id="app"></div>` + `<script type="module" src="./src/App.fs.js">`.
- `package.json` scripts WITH `--verbose`.
- `vite.config.js` ignoring **/*.fs, **/*.fsproj, **/obj/**.
- Fable.Browser.Dom 2.20.0 is REQUIRED here (Browser DOM is the chapter's payoff).

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on first occurrence.

CRITICAL constraints:
- NO admonish syntax — plain blockquotes only (STATE.md [01-01]).
- ALL code via {{#include}} anchors. Include path from src/ch03-fable-core/index.md is `../../examples/ch03-fable-core/src/App.fs:anchor`.
- `--verbose` required in dev script.
- Versions: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, net10.0, Vite ^6.
- `.gitignore` (updated in 02-01) covers fable_modules/, **/*.fs.js, dist/ repo-wide — do not commit generated files.

SCOPE (RESEARCH Pattern 4) — Ch.3 covers ONLY: `open Fable.Core`; `jsNative`; `open Fable.Core.JsInterop`; `[<Global>]`; basic `[<Import>]`/`[<ImportDefault>]` (concept, one example); `emitJsExpr` + `emitJsStatement`; `open Browser` (Fable.Browser.Dom) as payoff; .NET BCL support/limits table.
SCOPE BOUNDARY — Ch.3 must EXPLICITLY DEFER to Phase 3 (Ch.4–7): `[<Emit>]` for library binding (Ch.4), erased unions U2–U9 (Ch.5), `[<StringEnum>]`/CaseRules (Ch.5), `[<JS.Pojo>]`/POJO (Ch.6), dynamic `?`/unbox (Ch.4), name mangling (Ch.5). The chapter may NAME these as "covered later" but must NOT teach them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the ch03-fable-core example project + App.fs with intro-interop anchors</name>
  <files>examples/ch03-fable-core/App.fsproj, examples/ch03-fable-core/package.json, examples/ch03-fable-core/vite.config.js, examples/ch03-fable-core/index.html, examples/ch03-fable-core/src/App.fs</files>
  <action>
Create the example mirroring Ch.1's verified layout (see 02-01-SUMMARY.md).

`App.fsproj` (root): net10.0, RootNamespace Ch03FableCore, `<Compile Include="src/App.fs" />`, PackageReferences Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0.

`package.json`: name "ch03-fable-core"; type module; dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`; devDependencies vite ^6.0.0.

`vite.config.js`: identical to Ch.1.

`index.html` (root): `<div id="app"></div>` + `<script type="module" src="./src/App.fs.js">`, title "Ch03 - Fable.Core 기초".

`src/App.fs` — intro-interop demo with one ANCHOR per concept (RESEARCH Pattern 4 App.fs structure). Use these exact anchor names: `jsnative-example`, `emit-example`, `bcl-supported`, `bcl-unsupported`. Keep to INTRO scope only — NO [<Emit>] binding patterns, NO erased unions, NO StringEnum, NO POJO, NO dynamic `?`. Example shape:

```fsharp
module App

open Fable.Core
open Fable.Core.JsInterop
open Browser

// ANCHOR: jsnative-example
[<Global>]
let console : obj = jsNative
// [<Global>] binds an existing JS global; jsNative is the placeholder body (throws if run on .NET)
// ANCHOR_END: jsnative-example

// ANCHOR: emit-example
let add (a: int) (b: int) : int = emitJsExpr (a, b) "$0 + $1"
// emitJsExpr injects a raw JS expression; $0/$1 are the F# arguments
// ANCHOR_END: emit-example

// ANCHOR: bcl-supported
// 이 .NET 타입들은 Fable에서 동작합니다:
let isEmpty = System.String.IsNullOrEmpty ""
let pi = System.Math.PI
let now = System.DateTime.Now
let hasDigit = System.Text.RegularExpressions.Regex(@"\d+").IsMatch "abc123"
// ANCHOR_END: bcl-supported

// ANCHOR: bcl-unsupported
// 이 .NET API들은 Fable에서 사용할 수 없습니다 (JS 런타임 제약):
//   System.Threading.Thread        — JS에는 스레드가 없음
//   System.IO.File                 — 브라우저에 파일 시스템 없음
//   Async.RunSynchronously         — 단일 스레드 이벤트 루프에서 블로킹 불가
// ANCHOR_END: bcl-unsupported

let app = document.getElementById "app"
app.innerHTML <- sprintf "<h1>Fable.Core 기초</h1><p>add 3 4 = %d, π = %f</p>" (add 3 4) pi
```

Note: choose a `[<Global>]`/jsNative binding that actually compiles cleanly under Fable 5 (the `console` example above is illustrative — if it does not compile, use a simpler working [<Global>] binding such as binding the JS global `window` or a `[<Emit>]`-free [<Import>] concept). The bindings must compile; adjust as needed but stay within intro scope. Ensure `add`, `pi` etc. are consumed by the innerHTML so Fable emits them. No `[<EntryPoint>]`.
  </action>
  <verify>
`test -f examples/ch03-fable-core/App.fsproj && test -f examples/ch03-fable-core/src/App.fs && test -f examples/ch03-fable-core/index.html && test -f examples/ch03-fable-core/package.json && test -f examples/ch03-fable-core/vite.config.js`. `grep -q 'ANCHOR: jsnative-example' examples/ch03-fable-core/src/App.fs && grep -q 'ANCHOR: emit-example' examples/ch03-fable-core/src/App.fs && grep -q 'ANCHOR: bcl-supported' examples/ch03-fable-core/src/App.fs && grep -q 'ANCHOR: bcl-unsupported' examples/ch03-fable-core/src/App.fs`. `grep -q 'Fable.Browser.Dom' examples/ch03-fable-core/App.fsproj`. `grep -q -- '--verbose' examples/ch03-fable-core/package.json`. `! grep -q 'EntryPoint' examples/ch03-fable-core/src/App.fs`. Confirm NO out-of-scope content: `! grep -qE '\[<Emit\(|U2<|StringEnum|JS.Pojo' examples/ch03-fable-core/src/App.fs`.
  </verify>
  <done>examples/ch03-fable-core scaffolded matching Ch.1's verified pattern; App.fs has jsnative/emit/bcl-supported/bcl-unsupported anchors + DOM output, intro scope only (no Emit-binding/erased-union/StringEnum/POJO), no EntryPoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build the example end-to-end</name>
  <files>examples/ch03-fable-core/ (build only — no source edits unless build fails)</files>
  <action>
From `examples/ch03-fable-core/`:
1. `dotnet tool restore` (exit 0).
2. `npm install` (exit 0).
3. `npm run build` (exit 0) — Fable compiles src/App.fs → src/App.fs.js; Vite bundles dist/.

If the build fails (likely cause: a [<Global>]/jsNative or emitJsExpr binding that does not type-check under Fable 5), adjust the offending binding in App.fs to the simplest INTRO-scope form that compiles, keeping the four anchors meaningful. Do NOT introduce out-of-scope interop (no [<Emit>] binding, no erased unions) to make it build. Retry until `npm run build` exits 0.

Confirm outputs: `examples/ch03-fable-core/src/App.fs.js` and `examples/ch03-fable-core/dist/index.html`. Do not commit generated files (gitignored).
  </action>
  <verify>
`cd examples/ch03-fable-core && dotnet tool restore && npm install && npm run build` exits 0. `test -f examples/ch03-fable-core/src/App.fs.js`. `test -f examples/ch03-fable-core/dist/index.html`. `git status --porcelain examples/ch03-fable-core` shows no tracked *.fs.js or dist/.
  </verify>
  <done>npm run build exits 0; App.fs.js and dist/index.html generated; intro-scope bindings all compile under Fable 5.3.0; no generated artifacts tracked by git.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.3 chapter prose (scope boundary + BCL table) and verify render</name>
  <files>src/ch03-fable-core/index.md</files>
  <action>
Fully rewrite `src/ch03-fable-core/index.md` following the authoring template. H1 `# 3장. Fable.Core 기초 (Fable.Core Basics)`, then a plain blockquote (NOT [!NOTE]) pointing to examples/ch03-fable-core + run command. Korean prose, English term in parens on first occurrence. Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트.

- 개념 (Concept):
  1. Explain that `Fable.Core` provides the interop primitives, and DOM access comes from the SEPARATE `Fable.Browser.Dom` package (RESEARCH Pitfall 7). Provide a namespace map (Korean prose or small table): `open Fable.Core` → 인터롭 속성(attributes) + jsNative; `open Fable.Core.JsInterop` → emitJsExpr/emitJsStatement 등; `open Browser` (Fable.Browser.Dom) → document/window/console/DOM 타입. Show the exact `open` lines.
  2. Briefly explain jsNative (placeholder body for external bindings), [<Global>] (bind an existing JS global), [<Import>] (named import from a module), and emitJsExpr/emitJsStatement (raw-JS escape hatch with $0/$1 substitution) — at INTRO level.
  3. State the .NET BCL story: a large subset of the standard library works (string, Math, DateTime, Regex, collections), but runtime-bound APIs do not. Include a support/limits TABLE: 동작 (System.String, System.Math, System.DateTime → JS Date, Regex → JS RegExp, List/Array/Map) vs 미지원 (System.Threading.Thread, System.IO.File, Async.RunSynchronously, reflection-heavy APIs).
  4. SCOPE BOUNDARY paragraph (MANDATORY): explicitly say what this chapter does NOT cover and where it is taught — e.g. "이 장은 Fable.Core의 입문 범위만 다룹니다. 실전 라이브러리 바인딩을 위한 `[<Emit>]`은 4장, erased union(U2–U9)과 `[<StringEnum>]`은 5장, `[<JS.Pojo>]`와 POJO 패턴 및 dynamic `?` 연산자는 6장에서 다룹니다." This boundary statement is a hard requirement (avoids Phase 3 overlap).
- 예제 (Example): Brief intro, then include each anchor in its own fsharp block with 1–2 sentence explanation after each:
  ```fsharp
  {{#include ../../examples/ch03-fable-core/src/App.fs:jsnative-example}}
  ```
  (repeat for emit-example, bcl-supported, bcl-unsupported). Explain emitJsExpr's $0/$1 substitution and note that bcl-unsupported lines are commented because they would not compile/run.
- 실행하기 (Running): Prose for `dotnet tool restore` → `npm install` → `npm run dev`; expected page shows the Fable.Core demo (add result, π). Describe in prose; no raw terminal output.
- 핵심 포인트 (Key Points): 4–6 bullets: Fable.Core=인터롭 프리미티브 / DOM은 Fable.Browser.Dom; emitJsExpr는 raw JS 탈출구; BCL은 부분 지원(스레드·파일·블로킹 비동기 제외); 고급 인터롭(Emit 바인딩·erased union·POJO)은 4–6장.

Then run `mdbook build` and confirm Ch.3 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch03-fable-core/index.html`. Anchors resolved: `grep -q 'emitJsExpr' book/ch03-fable-core/index.html && grep -q 'jsNative' book/ch03-fable-core/index.html && grep -q 'System.Math' book/ch03-fable-core/index.html`. Template sections: `grep -q '개념' src/ch03-fable-core/index.md && grep -q '실행하기' src/ch03-fable-core/index.md && grep -q '핵심 포인트' src/ch03-fable-core/index.md`. Scope boundary present: `grep -q '4장' src/ch03-fable-core/index.md && grep -q '6장' src/ch03-fable-core/index.md` (defers Emit/POJO to later chapters). No admonish: `! grep -q '\[!NOTE\]' src/ch03-fable-core/index.md`. BCL table present (manual review: lists both supported and unsupported APIs).
  </verify>
  <done>src/ch03-fable-core/index.md follows the template in Korean, includes the four intro-interop anchors, contains the namespace map and BCL support/limits table, has an EXPLICIT scope-boundary paragraph deferring Emit-binding/erased-union/StringEnum/POJO/dynamic to Ch.4–6, uses plain blockquotes, and mdbook build renders the chapter with anchored code visible.</done>
</task>

</tasks>

<verification>
- `cd examples/ch03-fable-core && dotnet tool restore && npm install && npm run build` exits 0 (Success Criterion 3 + 4).
- App.fs has jsnative/emit/bcl-supported/bcl-unsupported anchors; intro scope only (no Emit-binding/erased-union/StringEnum/POJO/dynamic); visible DOM output; no EntryPoint.
- src/ch03-fable-core/index.md follows the template, Korean, code via {{#include}} only, plain blockquotes, includes namespace map + BCL support/limits table + explicit Phase-3 scope boundary.
- `mdbook build` exits 0 and book/ch03-fable-core/index.html contains the anchored code.
</verification>

<success_criteria>
SETUP-03 satisfied: a reader runs the Ch.3 example and understands Fable.Core's basic API surface (jsNative, emitJsExpr, [<Global>]/[<Import>] basics, Browser DOM) and the .NET BCL support/limits — with a clear boundary marking what is deferred to Phase 3. The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-toolchain-chapters/02-03-SUMMARY.md` recording: the example files created, the verified build outputs, the final form of any [<Global>]/jsNative/emitJsExpr binding that needed adjustment to compile under Fable 5.3.0, and confirmation that the chapter states the Phase-3 scope boundary (Emit-binding/erased-union/StringEnum/POJO deferred to Ch.4–6).
</output>
