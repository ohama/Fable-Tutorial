---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - book.toml
  - global.json
  - .config/dotnet-tools.json
  - src/SUMMARY.md
  - src/introduction.md
  - src/ch01-setup/index.md
  - src/ch02-compile-model/index.md
  - src/ch03-fable-core/index.md
  - src/ch04-basic-interop/index.md
  - src/ch05-advanced-interop/index.md
  - src/ch06-pojo-patterns/index.md
  - src/ch07-npm-binding/index.md
  - src/ch08-elmish/index.md
  - src/ch09-feliz/index.md
  - src/ch10-routing/index.md
  - src/ch11-json-http/index.md
  - src/ch12-testing/index.md
  - src/ch13-build/index.md
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "mdbook build exits 0 with no content written, only scaffold"
    - "book/index.html exists after build"
    - "All 13 chapter stubs listed in SUMMARY.md have a real .md file (create-missing=false does not error)"
    - "book.toml uses src = \"src\", create-missing = false, extra-watch-dirs = [\"examples\"]"
  artifacts:
    - path: "book.toml"
      provides: "mdBook config with project-specific layout and pinned preprocessors"
      contains: "src = \"src\""
    - path: "src/SUMMARY.md"
      provides: "Navigation tree listing introduction + 13 chapters"
      contains: "ch01-setup/index.md"
    - path: "global.json"
      provides: ".NET SDK 10.x pin"
      contains: "10.0"
    - path: ".config/dotnet-tools.json"
      provides: "Fable 5.3.0 tool pin"
      contains: "5.3.0"
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/chNN-name/index.md"
      via: "markdown link entries (one per chapter, all files must exist)"
      pattern: "ch01-setup/index.md"
    - from: "book.toml"
      to: "src/"
      via: "[book] src = \"src\""
      pattern: "src\\s*=\\s*\"src\""
---

<objective>
Create the mdBook repository skeleton: project-specific book.toml, the SUMMARY.md navigation tree with all 13 chapters as placeholders, a stub .md file for every SUMMARY entry, the .NET/Fable version pins, and the admonish/mermaid preprocessor wiring. Verify the empty book builds.

Purpose: This is the root foundation (Wave 1). Every other Phase 1 plan (highlighting, include-proof, CI, template doc) depends on a buildable scaffold existing first. (INFRA-01)
Output: A repo that `mdbook build`s successfully into `book/` with zero content but the full chapter structure in place.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

Repo root is the git repository root (`/Users/ohama/projs/FableTutorial`). All paths in this plan are relative to that root.

The `/mdbook` skill (.claude/commands/mdbook.md) is available for local `mdbook build` / `mdbook serve` convenience. The `/pages` skill defaults to `src = "."` and a commit-to-docs workflow — DO NOT use its book.toml or workflow output. Author book.toml from the research template below.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create book.toml + version pins + .gitignore</name>
  <files>book.toml, global.json, .config/dotnet-tools.json, .gitignore</files>
  <action>
Create `book.toml` at repo root EXACTLY per RESEARCH.md Pattern 1 (do not use the /pages skill default):

```toml
[book]
title = "F# 개발자를 위한 Fable 튜토리얼"
authors = ["ohama"]
language = "ko"
src = "src"

[build]
build-dir = "book"
create-missing = false
extra-watch-dirs = ["examples"]

[output.html]
default-theme = "light"
preferred-dark-theme = "navy"
git-repository-url = "https://github.com/OWNER/REPO"
edit-url-template = "https://github.com/OWNER/REPO/edit/main/src/{path}"

[output.html.search]
enable = true
limit-results = 30
boost-title = 2
```

Leave `OWNER/REPO` as literal placeholders — the real GitHub repo URL is unknown (RESEARCH Open Question 2); it is filled by the author in plan 01-04's setup checkpoint. Do NOT invent a URL.

CRITICAL settings (RESEARCH Pitfalls 5, 6 + Anti-Patterns): `src = "src"` (NOT "."), `create-missing = false`, `extra-watch-dirs = ["examples"]`. These three are the whole reason book.toml is hand-authored instead of skill-generated.

Do NOT add `[preprocessor.admonish]` / `[preprocessor.mermaid]` blocks by hand — Task 3 runs the install commands that append them with the correct auto-populated `assets_version`.

Create `global.json` at repo root (RESEARCH Code Examples):
```json
{
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestMinor"
  }
}
```

Create `.config/dotnet-tools.json` at repo root (RESEARCH Code Examples):
```json
{
  "version": 1,
  "isRoot": true,
  "tools": {
    "fable": {
      "version": "5.3.0",
      "commands": ["fable"]
    }
  }
}
```

Create `.gitignore` at repo root with at minimum: `book/` (built HTML must never be committed — RESEARCH Anti-Pattern "Committing book/ output"), `bin/`, `obj/`, `node_modules/`, `.DS_Store`.
  </action>
  <verify>
`test -f book.toml && test -f global.json && test -f .config/dotnet-tools.json && test -f .gitignore`. Confirm `grep 'src = "src"' book.toml`, `grep 'create-missing = false' book.toml`, `grep 'extra-watch-dirs' book.toml` all match. Confirm `grep '"5.3.0"' .config/dotnet-tools.json` matches.
  </verify>
  <done>book.toml exists with src="src", create-missing=false, extra-watch-dirs=["examples"], placeholder repo URL; global.json pins .NET 10.x; dotnet-tools.json pins Fable 5.3.0; .gitignore excludes book/ and build artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create SUMMARY.md + introduction + 13 chapter stubs</name>
  <files>src/SUMMARY.md, src/introduction.md, src/ch01-setup/index.md, src/ch02-compile-model/index.md, src/ch03-fable-core/index.md, src/ch04-basic-interop/index.md, src/ch05-advanced-interop/index.md, src/ch06-pojo-patterns/index.md, src/ch07-npm-binding/index.md, src/ch08-elmish/index.md, src/ch09-feliz/index.md, src/ch10-routing/index.md, src/ch11-json-http/index.md, src/ch12-testing/index.md, src/ch13-build/index.md</files>
  <action>
Create `src/SUMMARY.md` EXACTLY per RESEARCH.md Pattern 2 (the full tree with section headers 기초 / 핵심 개념 / JavaScript 상호 운용 / Elmish와 UI / 에코시스템, and one link per chapter from ch01 through ch13).

Create `src/introduction.md` as a short Korean landing page stub: a one-paragraph description of what the tutorial is (pull from PROJECT.md core value) and who it is for (F# 숙련자). Keep it minimal — the chapter-authoring template section is added later by plan 01-05; do NOT add it here.

Create a stub `index.md` for EVERY chapter listed in SUMMARY.md (all 13: ch01-setup through ch13-build, exact directory names from RESEARCH Pattern 2). Each stub contains only its H1 title matching the SUMMARY label (e.g. `# 1장. 프로젝트 설정`) plus one line `> 준비 중입니다.`. Because `create-missing = false`, a missing file for any SUMMARY entry is a BUILD ERROR — every listed entry MUST have a real file (RESEARCH Pitfall 5).

Do NOT write any tutorial content. This phase writes zero chapter content (key constraint). Stubs only.
  </action>
  <verify>
`ls src/ch*/index.md | wc -l` returns 13. `test -f src/SUMMARY.md && test -f src/introduction.md`. Every path referenced in SUMMARY.md resolves to an existing file: `grep -oE '\(([a-z0-9./-]+\.md)\)' src/SUMMARY.md | tr -d '()' | while read f; do test -f "src/$f" || echo "MISSING: $f"; done` prints nothing.
  </verify>
  <done>SUMMARY.md lists introduction + 13 chapters; introduction.md is a minimal Korean stub; all 13 chapter index.md stub files exist with matching H1 titles; no SUMMARY entry lacks a file.</done>
</task>

<task type="auto">
  <name>Task 3: Install preprocessors, patch book.toml, verify empty build</name>
  <files>book.toml</files>
  <action>
Ensure local tooling is present (RESEARCH Installation). If `mdbook`, `mdbook-admonish`, `mdbook-mermaid` are not on PATH, install pinned versions: `brew install mdbook` or `cargo install mdbook --version 0.5.3 --locked`; `cargo install mdbook-admonish --version 1.20.0 --locked`; `cargo install mdbook-mermaid --version 0.17.0 --locked`. (Local toolchain only — CI uses binary download in plan 01-04.)

From repo root run BOTH installers so they append their preprocessor blocks + auto-populated `assets_version` to book.toml:
```bash
mdbook-admonish install .
mdbook-mermaid install .
```
These also copy required asset files (admonish CSS, mermaid JS) into the project. Let them; do not hand-edit the keys they add.

Run `mdbook build` from repo root. It must exit 0. With `create-missing = false` and all 13 stubs present, there should be no missing-file errors.
  </action>
  <verify>
`grep -q 'preprocessor.admonish' book.toml && grep -q 'preprocessor.mermaid' book.toml`. `mdbook build` exits 0. `test -f book/index.html`. `test -f book/ch01-setup/index.html` (proves a SUMMARY chapter rendered).
  </verify>
  <done>book.toml contains [preprocessor.admonish] and [preprocessor.mermaid] with assets_version; `mdbook build` exits 0; book/index.html and per-chapter HTML files are generated.</done>
</task>

</tasks>

<verification>
- `mdbook build` exits 0 from repo root (Success Criterion foundation — gates all later plans).
- `book/index.html` exists; `book/ch01-setup/index.html` exists.
- book.toml has `src = "src"`, `create-missing = false`, `extra-watch-dirs = ["examples"]`, and both preprocessor blocks.
- All 13 SUMMARY entries map to existing stub files.
- `book/` is gitignored (not staged).
</verification>

<success_criteria>
The empty mdBook scaffold builds cleanly with the full 13-chapter structure, pinned .NET/Fable versions, and admonish+mermaid preprocessors configured. INFRA-01 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md` recording: files created, exact mdBook/plugin versions installed locally, the assets_version values admonish/mermaid wrote into book.toml, and confirmation that the OWNER/REPO placeholder still needs filling in plan 01-04.
</output>
