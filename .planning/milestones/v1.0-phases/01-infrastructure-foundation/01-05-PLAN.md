---
phase: 01-infrastructure-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/introduction.md
autonomous: true

must_haves:
  truths:
    - "A chapter authoring template is documented in the book itself (in-situ reference for future authors)"
    - "The template specifies concept→example flow, Korean prose with English-term annotation rule, and the independent runnable example structure"
    - "The documented template matches the structure the ch01-setup page already follows (include-by-anchor, no raw code in markdown)"
  artifacts:
    - path: "src/introduction.md"
      provides: "Landing page that includes the chapter-authoring template ('챕터 구조' section)"
      contains: "{{#include"
  key_links:
    - from: "src/introduction.md template doc"
      to: "the convention used by src/ch01-setup/index.md"
      via: "documents concept→example + {{#include}}-anchor + Korean/English rule that ch01 already follows"
      pattern: "개념|예제|#include"
---

<objective>
Document the chapter authoring template inside the book (a "챕터 구조" section in src/introduction.md) so every future chapter has an in-situ reference: the concept→example flow, Korean prose with first-occurrence English-term annotation, the include-by-anchor rule (zero raw code in markdown), and the independent runnable example structure.

Purpose: Phase 1's last deliverable. It encodes the conventions (CONT-01/02/03) that ch01-setup already demonstrates, so authors of Ch.1+ have a copy-pasteable standard. Depends on 01-03 (the template references the real include pattern proven there) and 01-02 (introduction.md must be clean of its test block first). (INFRA-03 template half, CONT-01, CONT-02, CONT-03, Success Criterion 4)
Output: A documented authoring template in the published book.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

Depends on 01-01 (introduction.md exists), 01-02 (introduction.md test block removed — avoids editing the same lines twice), 01-03 (the include-by-anchor pattern this template documents is proven). Runs in Wave 3 parallel with 01-04 (no file conflict: 01-04 owns .github/ + book.toml, this plan owns src/introduction.md only).

The canonical template is RESEARCH Pattern 6, with enforcement rules CONT-01/02/03.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the chapter authoring template to introduction.md</name>
  <files>src/introduction.md</files>
  <action>
Append a "챕터 구조 (Chapter Structure)" section to `src/introduction.md` documenting the standard chapter template from RESEARCH Pattern 6. The section must include:

1. The chapter .md skeleton (Korean): H1 title `# N장. 제목 (English Title)`, a NOTE admonition pointing to `examples/chNN-name/` with the run command, then sections in this fixed order: `## 개념 (Concept)` → `## 예제 (Example)` → `## 실행하기 (Running the Example)` → `## 핵심 포인트 (Key Points)`.

2. The example code rule: code is ALWAYS pulled via `{{#include ../../examples/chNN-name/src/FileName.fs:anchor-name}}` inside a ```fsharp fence — NEVER copy-pasted into markdown (RESEARCH Anti-Pattern: copy-pasting rots silently). Show the fenced include form as a literal example.

3. The enforcement rules, stated explicitly:
   - CONT-01: 개념 section ALWAYS precedes 예제 section.
   - CONT-02: every chapter has a corresponding independently-runnable `examples/chNN-name/` project; the 실행하기 commands (`dotnet tool restore && npm install && npm run dev`) must work from a cold clone.
   - CONT-03: Korean prose throughout; English term in parentheses on FIRST occurrence per chapter, e.g. "모듈 (module)", "컴파일러 (compiler)" — not in every sentence.
   - All code via {{#include}} anchors; zero raw code blocks.
   - No verbatim terminal output pasted — describe expected outcomes in prose (RESEARCH / PITFALLS P17).

Write the section in Korean (it lives in the published book). To show the literal `{{#include ...}}` directive WITHOUT mdBook trying to resolve it, escape it per mdBook convention (e.g. `\{{#include ...}}` or wrap in a fenced code block that documents the syntax) so the build does not fail or render an unintended include.

Confirm the documented template matches what `src/ch01-setup/index.md` (from plan 01-03) already does — concept→example, fenced include-by-anchor. They must agree.
  </action>
  <verify>
`grep -q '개념' src/introduction.md && grep -q '예제' src/introduction.md && grep -q '#include' src/introduction.md`. `grep -q 'CONT-0\|병기\|module' src/introduction.md` (English-annotation rule documented). `mdbook build` exits 0 (the literal include example was escaped and did not break the build, and did not accidentally resolve a real include). The rendered introduction page shows the template section: `grep -q '챕터 구조' book/introduction.html` (or equivalent rendered heading).
  </verify>
  <done>introduction.md contains a Korean "챕터 구조" section documenting concept→example flow, the {{#include}}-anchor rule, and CONT-01/02/03 enforcement; the build passes and the section renders.</done>
</task>

</tasks>

<verification>
- Success Criterion 4: the chapter authoring template (concept→example, Korean + English-term rule, independent runnable example structure) is documented and renders in the book.
- The template agrees with the convention ch01-setup already follows.
- `mdbook build` exits 0 (escaped literal include did not break the build).
</verification>

<success_criteria>
The chapter authoring standard is documented in-situ in the published book. INFRA-03 (template) / CONT-01 / CONT-02 / CONT-03 / Success Criterion 4 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-05-SUMMARY.md` recording: where the template lives, the escaping method used for the literal include example, and confirmation that the template matches the ch01-setup page.
</output>
