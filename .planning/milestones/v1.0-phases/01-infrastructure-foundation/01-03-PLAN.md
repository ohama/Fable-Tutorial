---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - examples/ch01-setup/src/App.fs
  - examples/ch01-setup/App.fsproj
  - examples/ch01-setup/package.json
  - examples/ch01-setup/vite.config.js
  - examples/ch01-setup/index.html
  - src/ch01-setup/index.md
autonomous: true

must_haves:
  truths:
    - "Adding a {{#include ../../examples/ch01-setup/src/App.fs:anchor}} line to a chapter .md renders the example code in the prose"
    - "Built HTML shows the anchored F# content but NOT the ANCHOR / ANCHOR_END comment lines"
    - "An independent example project structure (.fsproj + package.json + vite.config.js + index.html) exists for ch01-setup"
  artifacts:
    - path: "examples/ch01-setup/src/App.fs"
      provides: "F# source with ANCHOR/ANCHOR_END comment delimiters"
      contains: "ANCHOR: hello-world"
    - path: "examples/ch01-setup/App.fsproj"
      provides: "Independent buildable Fable project file pinning Fable.Core 5.0.0"
      contains: "Fable.Core"
    - path: "examples/ch01-setup/package.json"
      provides: "npm scripts using dotnet fable watch --verbose"
      contains: "--verbose"
    - path: "src/ch01-setup/index.md"
      provides: "Chapter page that includes the example via anchor"
      contains: "{{#include"
  key_links:
    - from: "src/ch01-setup/index.md"
      to: "examples/ch01-setup/src/App.fs"
      via: "{{#include ../../examples/ch01-setup/src/App.fs:hello-world}} resolved by mdBook links preprocessor"
      pattern: "#include \\.\\./\\.\\./examples/ch01-setup/src/App\\.fs:hello-world"
    - from: "App.fs ANCHOR comments"
      to: "rendered HTML"
      via: "mdBook strips anchor comment lines, renders only anchored content"
      pattern: "ANCHOR: hello-world"
---

<objective>
Build the minimal proof that the `{{#include}}` + ANCHOR pipeline works end-to-end: create the first independent example project (`examples/ch01-setup/`) with anchored F# source, then wire `src/ch01-setup/index.md` to include it by anchor name and verify the rendered HTML contains the anchored code without the anchor comment noise.

Purpose: This proves the core authoring mechanism every future chapter relies on — code is quoted from real .fs files via anchors, never copy-pasted (silent-rot prevention, STATE.md decision). It also establishes the independent-example directory structure. This is a THROWAWAY proof example, not tutorial content. (INFRA-03, CONT-02, Success Criterion 3)
Output: A working include-by-anchor wiring on the ch01 page and a runnable example project skeleton.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

Depends on plan 01-01 (scaffold; src/ch01-setup/index.md already exists as a stub). Runs in parallel with plan 01-02 (no shared files: this plan owns examples/ + src/ch01-setup/index.md; 01-02 owns theme/ + src/introduction.md).

Path mechanics (RESEARCH Pattern 4 + Pitfall 2): the include path is relative to the MARKDOWN file. From `src/ch01-setup/index.md`, two `../` escape `ch01-setup/` then `src/` to reach repo root, then into `examples/`. The correct path is `../../examples/ch01-setup/src/App.fs`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the ch01-setup independent example project</name>
  <files>examples/ch01-setup/src/App.fs, examples/ch01-setup/App.fsproj, examples/ch01-setup/package.json, examples/ch01-setup/vite.config.js, examples/ch01-setup/index.html</files>
  <action>
Create `examples/ch01-setup/src/App.fs` with ANCHOR/ANCHOR_END comment delimiters (RESEARCH Pattern 4):

```fsharp
module App

open Fable.Core

// ANCHOR: hello-world
[<EntryPoint>]
let main _ =
    printfn "Hello from Fable"
    0
// ANCHOR_END: hello-world
```

Use ANCHOR comments, NOT line-number includes (RESEARCH Anti-Pattern: line numbers rot when lines shift above).

Create `examples/ch01-setup/App.fsproj` per RESEARCH Code Examples (TargetFramework net10.0, Compile Include App.fs, PackageReference Fable.Core 5.0.0 pinned).

Create `examples/ch01-setup/package.json` per RESEARCH Code Examples. The `dev` script MUST use `dotnet fable watch --verbose --run npx vite` — the `--verbose` flag is MANDATORY (Fable issue #3631 / STATE.md decision: without it Vite deadlocks after 1-3 recompiles). The `build` script is `dotnet fable --run npx vite build`. devDependency: vite ^6.0.0.

Create `examples/ch01-setup/vite.config.js` per RESEARCH Code Examples (server.watch.ignored excludes **/*.fs, **/*.fsproj, **/obj/** so Vite does not fight Fable's watcher).

Create `examples/ch01-setup/index.html` — a minimal HTML entry that loads the Fable-compiled output as a module script (e.g. a `<div id="app">` plus `<script type="module" src="./src/App.fs.js"></script>` consistent with the bare Fable+Vite setup). Keep it minimal; this is proof-of-wiring, not a real app.

Do NOT run the example or install npm packages here — this plan only proves the include/anchor wiring at mdBook build time. Actually compiling/running the example is Phase 2 content.
  </action>
  <verify>
`test -f examples/ch01-setup/src/App.fs && test -f examples/ch01-setup/App.fsproj && test -f examples/ch01-setup/package.json && test -f examples/ch01-setup/vite.config.js && test -f examples/ch01-setup/index.html`. `grep -q 'ANCHOR: hello-world' examples/ch01-setup/src/App.fs && grep -q 'ANCHOR_END: hello-world' examples/ch01-setup/src/App.fs`. `grep -q -- '--verbose' examples/ch01-setup/package.json`. `grep -q 'Fable.Core' examples/ch01-setup/App.fsproj`.
  </verify>
  <done>examples/ch01-setup/ contains src/App.fs (with hello-world anchor), App.fsproj (Fable.Core 5.0.0), package.json (--verbose dev script), vite.config.js (ignores .fs), and index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the include into ch01 page and verify rendered output</name>
  <files>src/ch01-setup/index.md</files>
  <action>
Replace the stub `src/ch01-setup/index.md` (from plan 01-01) with a page that demonstrates the include-by-anchor wiring (RESEARCH Pattern 4):

````markdown
# 1장. 프로젝트 설정

## 예제

첫 번째 Fable 프로그램입니다.

```fsharp
{{#include ../../examples/ch01-setup/src/App.fs:hello-world}}
```
````

The include path MUST be `../../examples/ch01-setup/src/App.fs:hello-world` (relative to this .md file; two `../` to reach repo root — RESEARCH Pitfall 2). Keep this page minimal — it is the wiring proof, not real Ch.1 content (Phase 2 writes the actual chapter).

Run `mdbook build` from repo root. Verify the rendered page contains the anchored content and that the ANCHOR comment lines were stripped (RESEARCH Pattern 4 verification):
```bash
mdbook build
grep -r "printfn" book/ch01-setup/         # MUST find the rendered content
grep -r "ANCHOR" book/ch01-setup/          # MUST find nothing — anchors stripped
grep -r "ANCHOR" book/ch01-setup/index.html
```
If `ANCHOR` appears in the output, the anchor was not recognized; if `printfn` is missing, the path is wrong (re-check the `../../` count).
  </action>
  <verify>
`mdbook build` exits 0. `grep -rq "printfn" book/ch01-setup/index.html` (anchored content rendered). `grep -rL "ANCHOR" book/ch01-setup/index.html` confirms NO `ANCHOR`/`ANCHOR_END` comment text leaked into the HTML. `grep -q '{{#include ../../examples/ch01-setup/src/App.fs:hello-world}}' src/ch01-setup/index.md`.
  </verify>
  <done>The ch01 page includes the example by anchor; built HTML shows the F# snippet (printfn) WITHOUT anchor comment lines, proving Success Criterion 3.</done>
</task>

</tasks>

<verification>
- Success Criterion 3: a single `{{#include ...:anchor}}` line renders the example code in the prose.
- Built HTML contains the anchored content; ANCHOR/ANCHOR_END comment lines are stripped.
- CONT-02 foundation: an independent example project structure exists (.fsproj + package.json + vite.config.js + index.html).
- `mdbook build` exits 0.
</verification>

<success_criteria>
The {{#include}}-with-anchor wiring is proven end-to-end and the independent-example directory structure is established. INFRA-03 / CONT-02 / Success Criterion 3 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md` recording: the example project layout created, the exact include path used, confirmation that anchors are stripped in output, and the --verbose dev-script convention for future example projects.
</output>
