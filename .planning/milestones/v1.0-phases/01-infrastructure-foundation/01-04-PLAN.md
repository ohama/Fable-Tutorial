---
phase: 01-infrastructure-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .github/workflows/book.yml
  - book.toml
autonomous: false

must_haves:
  truths:
    - "Pushing to main triggers GitHub Actions, which builds mdBook and deploys to GitHub Pages"
    - "The book is readable at the GitHub Pages URL"
    - "F# code blocks render with color highlighting on the live deployed site"
    - "The CI workflow installs mdBook + plugin BINARIES (not cargo install) and uses the artifact-based deploy (upload-pages-artifact + deploy-pages)"
  artifacts:
    - path: ".github/workflows/book.yml"
      provides: "Build + deploy-pages workflow with pinned binary downloads"
      contains: "actions/deploy-pages"
  key_links:
    - from: ".github/workflows/book.yml build job"
      to: "GitHub Pages environment"
      via: "actions/upload-pages-artifact@v3 (path: book/) then actions/deploy-pages@v4"
      pattern: "upload-pages-artifact"
    - from: "push to main"
      to: "workflow trigger"
      via: "on.push.branches: [main]"
      pattern: "branches:\\s*\\[main\\]"
---

<objective>
Author the GitHub Actions workflow that builds mdBook (via pinned binary downloads) and deploys it to GitHub Pages using the artifact-based pattern, verify the plugin binary URLs are live, then drive the author through the unavoidable manual setup (enable Pages, add remote, push) and confirm the deployed site is live with working F# highlighting.

Purpose: Closes the loop — readers can visit a live site. This depends on all prior Wave 1/2 plans being green (it deploys the full built book). (INFRA-04, Success Criterion 1)
Output: `.github/workflows/book.yml` and a verified live GitHub Pages deployment.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

Depends on 01-01 (scaffold), 01-02 (highlighting — must be on the live site), 01-03 (include proof). This plan deploys whatever those produced.

DO NOT use the /pages skill workflow — it generates a commit-to-docs pattern (RESEARCH Pitfall 3). Author book.yml from RESEARCH Pattern 5 (upload-pages-artifact + deploy-pages).

Unknowns handled here: the real GitHub repo URL (placeholder in book.toml from 01-01) and the plugin binary tarball filenames (RESEARCH Open Questions 1 & 2). Plugin URLs are verified live before relying on them; repo URL is filled by the author at the setup checkpoint.

No remote is configured yet. Enabling Pages, adding the remote, and pushing are author-only actions (no CLI substitute that the planner can assume) — modeled as checkpoints, not fake automation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify plugin binary URLs, then author book.yml</name>
  <files>.github/workflows/book.yml</files>
  <action>
FIRST verify the plugin binary download URLs are live (RESEARCH Pitfall 4 / Open Question 1) before committing the workflow:
```bash
curl -I https://github.com/rust-lang/mdBook/releases/download/v0.5.3/mdbook-v0.5.3-x86_64-unknown-linux-gnu.tar.gz
curl -I https://github.com/tommilligan/mdbook-admonish/releases/download/v1.20.0/mdbook-admonish-v1.20.0-x86_64-unknown-linux-gnu.tar.gz
curl -I https://github.com/badboy/mdbook-mermaid/releases/download/v0.17.0/mdbook-mermaid-v0.17.0-x86_64-unknown-linux-gnu.tar.gz
```
Expect HTTP 302 redirect to the asset for each. If any returns 404, the filename convention differs — inspect that repo's releases page for the correct `x86_64-unknown-linux-gnu` (or `-musl`) tarball name and use the corrected URL. RESEARCH-approved fallback: if the mdBook binary URL is problematic, use `peaceiris/actions-mdbook@v2` (pinned) for mdBook itself while still binary-downloading the plugins; record any substitution in the SUMMARY.

Create `.github/workflows/book.yml` per RESEARCH Pattern 5: triggers on push to main + workflow_dispatch; permissions contents:read, pages:write, id-token:write; concurrency group "pages"; a `build` job that checks out, installs mdBook + mdbook-admonish + mdbook-mermaid via the verified binary URLs into /usr/local/bin, runs actions/configure-pages@v5, runs `mdbook build`, then actions/upload-pages-artifact@v3 with path: book/; and a `deploy` job (needs: build) with environment github-pages running actions/deploy-pages@v4.

Use the corrected URLs from the curl check. Do NOT use cargo install in CI (RESEARCH Anti-Pattern: 10+ min compile).
  </action>
  <verify>
`test -f .github/workflows/book.yml`. `grep -q 'upload-pages-artifact' .github/workflows/book.yml && grep -q 'deploy-pages' .github/workflows/book.yml`. `grep -q 'branches: \[main\]' .github/workflows/book.yml`. `grep -q 'path: book/' .github/workflows/book.yml`. Confirm NO `cargo install` appears: `! grep -q 'cargo install' .github/workflows/book.yml`. The three curl -I checks returned 302 (or substitution documented).
  </verify>
  <done>book.yml uses verified binary downloads + upload-pages-artifact + deploy-pages, triggers on push to main, and contains no cargo install. Plugin URLs confirmed live (or fallback applied and noted).</done>
</task>

<task type="auto">
  <name>Task 2: Fill the real repo URL in book.toml once known</name>
  <files>book.toml</files>
  <action>
The author provides the real GitHub repo (OWNER/REPO) at the setup checkpoint (Task 3). Once known, replace the `OWNER/REPO` placeholders left by plan 01-01 in book.toml:
- `git-repository-url = "https://github.com/OWNER/REPO"`
- `edit-url-template = "https://github.com/OWNER/REPO/edit/main/src/{path}"`

with the actual owner and repo name. If the author has not yet decided the repo name when this task runs, defer the edit until immediately after the Task 3 checkpoint resolves, then apply it before the push.

Run `mdbook build` again to confirm book.toml is still valid after the edit.
  </action>
  <verify>
`! grep -q 'OWNER/REPO' book.toml` (placeholders replaced). `grep -q 'git-repository-url' book.toml`. `mdbook build` exits 0.
  </verify>
  <done>book.toml's git-repository-url and edit-url-template point at the real repo; build still passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
The CI workflow (.github/workflows/book.yml) is committed-ready and book.toml has the real repo URL. The remaining steps require the author's GitHub account and repo settings, which have no planner-side CLI substitute (no remote is configured and the Pages source toggle is account-scoped).
  </what-built>
  <how-to-verify>
Author, please complete these one-time steps:
1. Create the GitHub repo (if it does not exist yet) and note OWNER/REPO — give this to the executor so Task 2's book.toml edit uses the correct URL.
2. Add the remote and push main:
   `git remote add origin https://github.com/OWNER/REPO.git`
   `git push -u origin main`
3. In the repo: Settings → Pages → Source: select "GitHub Actions" (NOT "Deploy from a branch"). This enables the `github-pages` environment that deploy-pages requires (RESEARCH Pattern 5).
4. Go to the Actions tab and confirm the "Deploy mdBook to Pages" workflow runs and both build + deploy jobs succeed (green).
  </how-to-verify>
  <resume-signal>Type "deployed" once both workflow jobs are green, or paste the error if a job failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The site is deployed to GitHub Pages by the workflow. Final confirmation that the live site meets the phase's headline success criteria.
  </what-built>
  <how-to-verify>
1. Open the GitHub Pages URL (Settings → Pages shows it, typically `https://OWNER.github.io/REPO/`).
2. Confirm the book loads — introduction + the 13-chapter sidebar are visible.
3. Navigate to "1장. 프로젝트 설정" (ch01-setup) and confirm the F# example code block is shown (the included App.fs snippet) AND that it has COLOR syntax highlighting (keywords/types colored, not monochrome). This jointly confirms Success Criteria 1, 2, and 3 on the live site.
  </how-to-verify>
  <resume-signal>Type "approved" if the live site loads with colored F# on the ch01 page, or describe what is wrong (blank page / monochrome / missing snippet).</resume-signal>
</task>

</tasks>

<verification>
- Success Criterion 1: push to main → Actions builds mdBook → readable at the Pages URL (confirmed at the human checkpoints).
- Success Criterion 2 (live): F# blocks are colored on the deployed site.
- Success Criterion 3 (live): the ch01 page shows the included example snippet.
- Workflow uses binary downloads + artifact deploy; no cargo install; no commit-to-docs.
</verification>

<success_criteria>
A push to main produces a live, readable GitHub Pages site with colored F# code and the include-wired ch01 example. INFRA-04 / Success Criterion 1 satisfied (and 2, 3 confirmed live).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md` recording: the final plugin binary URLs used (and any fallback), the live Pages URL, the resolved OWNER/REPO, and confirmation of the live highlighting + include checks.
</output>
