---
phase: 05-ecosystem-and-real-world-axis
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ch13-build/App.fsproj
  - examples/ch13-build/package.json
  - examples/ch13-build/vite.config.js
  - examples/ch13-build/index.html
  - examples/ch13-build/src/App.fs
  - examples/ch13-build/deploy-app.yml
  - examples/ch13-build/package-lock.json
  - src/ch13-build/index.md
autonomous: true

must_haves:
  truths:
    - "Reader runs `cd examples/ch13-build && dotnet tool restore && npm install && npm run build` and it exits 0, producing examples/ch13-build/dist/ (SC-4)"
    - "The example ALSO builds successfully with the production flag: `dotnet fable --noReflection --run npx vite build` exits 0 (SC-3 — --noReflection is a valid Fable 5.3.0 flag), and dist/ is produced"
    - "examples/ch13-build/src/App.fs is a small self-contained Feliz/Elmish app that uses NO reflection-dependent code (no Decode.Auto, no F# reflection) so it compiles and runs correctly under --noReflection"
    - "examples/ch13-build/vite.config.js teaches the GitHub Pages subpath: it sets `base` (e.g. base: \"/REPO-NAME/\") with a comment explaining the project-subpath asset-path requirement (RESEARCH Pitfall 9), while keeping the dev watch-ignore config"
    - "examples/ch13-build/deploy-app.yml is a COMPLETE, copy-able GitHub Actions Pages workflow (checkout → setup-dotnet → setup-node → tool restore → npm ci → dotnet fable --noReflection --run npx vite build → upload-pages-artifact → deploy-pages) that the chapter shows as code; it is NOT installed into .github/workflows (no second live deployment — would conflict with the existing book.yml, RESEARCH Open Q #4)"
    - "The production build script form (--noReflection placement) is RESOLVED BY BUILD: the exact working command is confirmed by running it to exit 0 and recorded; if `dotnet fable --noReflection --run npx vite build` needs adjustment for Fable 5.3.0, the working form is found and documented"
    - "src/ch13-build/index.md follows the authoring template in Korean (개념→예제→실행하기→핵심 포인트), pulls F# / config / workflow code via {{#include}} anchors, uses PLAIN `>` blockquotes (no [!NOTE]), no mermaid fence, wraps `--noReflection` and F# attributes in backticks, and reaffirms the cross-chapter note that --noReflection breaks Decode.Auto (so Ch.11's manual decoders are why production builds work); mdbook build exits 0 with anchors rendered"
  artifacts:
    - path: "examples/ch13-build/src/App.fs"
      provides: "Small self-contained Feliz/Elmish app (reflection-free) used to demonstrate the production build; ANCHOR markers"
      contains: "Program."
    - path: "examples/ch13-build/App.fsproj"
      provides: "net10.0 fsproj, RootNamespace Ch13Build, Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0 + Fable.Elmish 5.0.2 + Fable.Elmish.React 5.0.1 + Feliz 3.3.3"
      contains: "Feliz"
    - path: "examples/ch13-build/vite.config.js"
      provides: "Vite config with `base` for GitHub Pages subpath (commented) + dev watch-ignore"
      contains: "base"
    - path: "examples/ch13-build/package.json"
      provides: "dev/build scripts (--verbose) + a build:prod script using --noReflection; react@^18.3 + react-dom@^18.3 in dependencies; vite ^6 in devDependencies"
      contains: "noReflection"
    - path: "examples/ch13-build/deploy-app.yml"
      provides: "Complete copy-able GitHub Actions Pages workflow building the Fable SPA with --noReflection and deploying dist/ via deploy-pages; ANCHOR markers for inclusion"
      contains: "deploy-pages"
    - path: "examples/ch13-build/index.html"
      provides: "<div id=\"root\"></div> mount node + <script type=module src=./src/App.fs.js>"
      contains: "id=\"root\""
    - path: "src/ch13-build/index.md"
      provides: "Full Ch.13 prose: production build + tree-shaking, --noReflection (and its Decode.Auto interaction), Vite base for Pages, GitHub Actions workflow taught as code"
      min_lines: 70
  key_links:
    - from: "src/ch13-build/index.md"
      to: "examples/ch13-build/deploy-app.yml"
      via: "{{#include ...:workflow}} (and vite base + build-script anchors)"
      pattern: "#include.*ch13-build/"
    - from: "examples/ch13-build/package.json build:prod"
      to: "reflection-free production bundle"
      via: "dotnet fable --noReflection --run npx vite build"
      pattern: "noReflection"
    - from: "examples/ch13-build/vite.config.js"
      to: "GitHub Pages project subpath asset resolution"
      via: "base: \"/REPO-NAME/\""
      pattern: "base"
    - from: "examples/ch13-build/deploy-app.yml"
      to: "GitHub Pages deployment of dist/"
      via: "upload-pages-artifact path: dist + deploy-pages"
      pattern: "deploy-pages"
---

<objective>
Create the standalone examples/ch13-build project and write Ch.13 (빌드 최적화와 배포), which teaches the production workflow: an optimized Fable production build with the `--noReflection` flag, automatic Vite tree-shaking, Vite `base` configuration for a GitHub Pages project subpath, and a complete GitHub Actions Pages deployment workflow taught AS COPY-ABLE CODE (not wired live — that would conflict with the repo's existing book.yml). The example is a small reflection-free Feliz/Elmish app that builds cleanly with `--noReflection`, and the chapter reaffirms the cross-chapter note that `--noReflection` breaks `Decode.Auto` (so Ch.11's manual decoders are the reason production builds work).

Purpose: Satisfies REAL-03 (production build optimization + deploy) and Phase 5 Success Criteria 3 (--noReflection production build + GitHub Pages deployment taught → app works at a live URL pattern) and 4 (npm run build exit 0). (REAL-03)
Output: A runnable examples/ch13-build that builds with both `npm run build` and `dotnet fable --noReflection --run npx vite build` (exit 0); a vite.config.js teaching the Pages `base` subpath; a complete deploy-app.yml workflow shown as code; and a complete Korean Ch.13 chapter with code via {{#include}} anchors only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ecosystem-and-real-world-axis/05-RESEARCH.md
@.planning/phases/04-elmish-and-ui-axis/04-01-SUMMARY.md

Repo root: `/Users/ohama/projs/FableTutorial`. Paths relative to that root.

This is a wave 1 plan (parallel with 05-01 Ch.11; disjoint files). It is INTENTIONALLY self-contained: Ch.13 builds its OWN small example so it does not depend on Ch.11's example existing on disk. (The chapter prose may REFER to Ch.11's manual-decoder choice conceptually as the cross-link, but the build does not require Ch.11's files.)

TASK ORDERING: Tasks execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 1 scaffolds (app + vite base + deploy-app.yml); Task 2 runs BOTH the standard build and the --noReflection production build, resolves the exact flag form, and inspects output; Task 3 writes prose quoting verified behavior, then runs mdbook build. Do not reorder or parallelize.

Copy the verified runnable example pattern from Ch.8 (examples/ch08-elmish/) — fsproj at example ROOT, `<Compile Include="src/App.fs" />`, Fable emits src/App.fs.js adjacent to source, index.html loads ./src/App.fs.js, NO @vitejs/plugin-react. The shared dotnet tool manifest lives at repo-root `.config/dotnet-tools.json` (Fable 5.3.0 pinned). Name = "ch13-build", RootNamespace Ch13Build.

Established stack (Ch.8, 04-01-SUMMARY — reuse exactly): react/react-dom in `dependencies` (NOT devDependencies); react@^18.3 → 18.3.1; Fable.Elmish 5.0.2 + Fable.Elmish.React 5.0.1 + Feliz 3.3.3 build cleanly; Program.withReactSynchronous "root" with matching index.html div#root.

NEW for Ch.13 (vs Ch.8):
- A `build:prod` package.json script using `--noReflection` (RESEARCH Pattern 2: `dotnet fable --noReflection --run npx vite build`; --noReflection is CONFIRMED in Fable 5.3.0 CLI source).
- vite.config.js adds a `base` setting for the GitHub Pages project subpath (RESEARCH Pitfall 9: default base "/" → assets 404 on a project subpath).
- A complete deploy-app.yml GitHub Actions workflow file in the EXAMPLE dir (examples/ch13-build/deploy-app.yml) shown as code — NOT placed in .github/workflows (RESEARCH Open Q #4: a second live Pages deployment conflicts with the existing book.yml; teach the structure, don't wire it).
- The example app MUST be reflection-free (no Decode.Auto, no F# reflection) so --noReflection is genuinely safe.

Authoring template (MANDATORY): @src/introduction.md "챕터 구조". Fixed order 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose, English term in parens on FIRST occurrence only.

CRITICAL constraints:
- 개념 section is PROSE-ONLY — no raw code blocks. All code via {{#include}} anchors in 예제.
- NO admonish syntax (`> [!NOTE]`) — PLAIN markdown `>` blockquotes only (STATE.md [01-01]).
- mermaid is UNAVAILABLE — no ```mermaid fence; use ASCII/prose inside a plain ```text fence if a diagram is wanted.
- Wrap F# attributes and CLI flags in backticks in prose (e.g. `--noReflection`, `Decode.Auto`, `[<ReactComponent>]`) so mdbook does not parse them as HTML (STATE.md [02-03]).
- ALL code via {{#include}} anchors. Include path from src/ch13-build/index.md is `../../examples/ch13-build/<file>:anchor` (App.fs, vite.config.js, deploy-app.yml all support {{#include ...:anchor}} via comment-based anchors — `// ANCHOR:` in JS, `# ANCHOR:` in YAML).
- `--verbose` required in dev script (Fable #3631 deadlock, STATE.md [Init]).
- Versions (pin EXACTLY): Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Feliz 3.3.3, net10.0, Vite ^6, react@^18.3, react-dom@^18.3.
- `.gitignore` already covers fable_modules/, **/*.fs.js, dist/, node_modules/ — do NOT commit generated files; DO commit package-lock.json AND deploy-app.yml AND vite.config.js (these are source/teaching artifacts, not generated).
- Do NOT add a file under `.github/workflows/` — deploy-app.yml lives in examples/ch13-build/ as a teaching artifact only.

Production build + Pages facts (RESEARCH Pattern 2 + Pitfalls 2/9 + Open Q #3/#4):
- `--noReflection` is a real Fable 5.3.0 flag (CONFIRMED in Entry.fs). It removes per-type reflection helper emission → smaller bundle. It DISABLES type reflection, so `Decode.Auto` (which uses `typeof<'T>`) breaks — manual decoders (Ch.11) are unaffected.
- Vite tree-shaking is AUTOMATIC in `vite build` (Rollup) — no extra config needed.
- Vite `base: "/REPO-NAME/"` makes asset URLs resolve under a GitHub Pages project subpath (https://OWNER.github.io/REPO-NAME/). Default "/" → 404 on a subpath.
- The Ch.11-style app uses `Program.withReactSynchronous` (not `[<ReactComponent>]`), so --noReflection + React is expected to be a non-issue (RESEARCH Open Q #3). Verify by building.
- GitHub Pages allows ONE active deployment per repo; a second deploy-pages workflow would conflict with book.yml. Teach the workflow as code readers copy to THEIR repo (RESEARCH Open Q #4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ch13-build example (reflection-free app) + vite base + deploy-app.yml workflow</name>
  <files>examples/ch13-build/App.fsproj, examples/ch13-build/package.json, examples/ch13-build/vite.config.js, examples/ch13-build/index.html, examples/ch13-build/src/App.fs, examples/ch13-build/deploy-app.yml</files>
  <action>
Create the example directory mirroring Ch.8's verified layout (examples/ch08-elmish/).

`App.fsproj`: net10.0, RootNamespace Ch13Build, `<Compile Include="src/App.fs" />`, PackageReferences: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, Fable.Elmish 5.0.2, Fable.Elmish.React 5.0.1, Feliz 3.3.3.

`package.json`: name "ch13-build", private, version 0.1.0, type module, scripts: dev = `dotnet fable watch --verbose --run npx vite`, build = `dotnet fable --run npx vite build`, build:prod = `dotnet fable --noReflection --run npx vite build`. `dependencies`: { "react": "^18.3", "react-dom": "^18.3" }. `devDependencies`: { "vite": "^6.0.0" }.

`vite.config.js`: defineConfig with BOTH (1) server.watch.ignored = ["**/*.fs", "**/*.fsproj", "**/obj/**"] (dev), and (2) a `base` setting for Pages with an explanatory comment + JS ANCHOR markers, e.g.:
```javascript
import { defineConfig } from "vite";

export default defineConfig({
  // ANCHOR: pages-base
  // GitHub Pages 프로젝트 서브경로(https://OWNER.github.io/REPO-NAME/)에 배포할 때
  // base를 "/REPO-NAME/"로 설정해야 에셋 경로가 404 나지 않습니다.
  base: "/REPO-NAME/",
  // ANCHOR_END: pages-base
  server: {
    watch: {
      ignored: ["**/*.fs", "**/*.fsproj", "**/obj/**"]
    }
  }
});
```
NOTE: `base: "/REPO-NAME/"` is a teaching placeholder; it does not affect a local `npm run build` exiting 0 (it only changes emitted asset path prefixes). Keep it as-is. NO @vitejs/plugin-react.

`index.html`: lang ko, charset UTF-8, viewport meta, title "Ch13 - 빌드와 배포", body has `<div id="root"></div>` and `<script type="module" src="./src/App.fs.js"></script>`.

`src/App.fs` — a SMALL self-contained, reflection-free Feliz/Elmish app (a minimal counter is sufficient; the point of this chapter is the BUILD, not new app features). NO Decode.Auto, NO F# reflection, NO `[<ReactComponent>]` (use withReactSynchronous to stay on the safe path per Open Q #3). ANCHORs: `app` (the whole program is fine as one anchor, or split model/update/view/program). No `[<EntryPoint>]`. Example skeleton:
```fsharp
module App

open Elmish
open Elmish.React
open Feliz

// ANCHOR: app
type Model = { Count: int }
type Msg = Increment | Decrement

let init () : Model * Cmd<Msg> = { Count = 0 }, Cmd.none

let update (msg: Msg) (model: Model) : Model * Cmd<Msg> =
    match msg with
    | Increment -> { model with Count = model.Count + 1 }, Cmd.none
    | Decrement -> { model with Count = model.Count - 1 }, Cmd.none

let view (model: Model) (dispatch: Msg -> unit) : ReactElement =
    Html.div [
        prop.children [
            Html.h1 [ prop.text (sprintf "카운트: %d" model.Count) ]
            Html.button [ prop.onClick (fun _ -> dispatch Increment); prop.text "+1" ]
            Html.button [ prop.onClick (fun _ -> dispatch Decrement); prop.text "-1" ]
        ]
    ]

Program.mkProgram init update view
|> Program.withReactSynchronous "root"
|> Program.run
// ANCHOR_END: app
```

`deploy-app.yml` — a COMPLETE GitHub Actions Pages workflow as a teaching artifact (this file lives in examples/ch13-build/, NOT in .github/workflows/). Add YAML ANCHOR markers (`# ANCHOR: workflow` ... `# ANCHOR_END: workflow`) around the whole workflow body so it can be {{#include}}-d. Use the RESEARCH Pattern 2 workflow: name, on push main + workflow_dispatch, permissions (contents read, pages write, id-token write), concurrency group pages-app, a deploy job (environment github-pages) with steps: actions/checkout@v4; actions/setup-dotnet@v4 (dotnet-version 10.0.x); actions/setup-node@v4 (node lts/*, cache npm, cache-dependency-path examples/ch13-build/package-lock.json); dotnet tool restore; npm ci (working-directory examples/ch13-build); production build `dotnet fable --noReflection --run npx vite build` (working-directory examples/ch13-build); actions/configure-pages@v5; actions/upload-pages-artifact@v3 (path examples/ch13-build/dist); actions/deploy-pages@v4 (id deployment). Keep it self-consistent and copy-able.
  </action>
  <verify>
`test -f examples/ch13-build/App.fsproj && test -f examples/ch13-build/src/App.fs && test -f examples/ch13-build/index.html && test -f examples/ch13-build/package.json && test -f examples/ch13-build/vite.config.js && test -f examples/ch13-build/deploy-app.yml`. App.fs anchor + reflection-free: `grep -q 'ANCHOR: app' examples/ch13-build/src/App.fs && ! grep -q 'Decode.Auto' examples/ch13-build/src/App.fs && ! grep -q 'ReactComponent' examples/ch13-build/src/App.fs && ! grep -q 'EntryPoint' examples/ch13-build/src/App.fs`. Mount + root: `grep -q 'withReactSynchronous' examples/ch13-build/src/App.fs && grep -q 'id="root"' examples/ch13-build/index.html`. build:prod with --noReflection: `grep -q 'noReflection' examples/ch13-build/package.json`. Vite base anchor: `grep -q 'ANCHOR: pages-base' examples/ch13-build/vite.config.js && grep -q 'base' examples/ch13-build/vite.config.js`. Workflow anchor + deploy-pages: `grep -q 'ANCHOR: workflow' examples/ch13-build/deploy-app.yml && grep -q 'deploy-pages' examples/ch13-build/deploy-app.yml && grep -q 'noReflection' examples/ch13-build/deploy-app.yml`. Workflow NOT installed live: `! test -f .github/workflows/deploy-app.yml`. react in dependencies + --verbose: `grep -q '"react"' examples/ch13-build/package.json && grep -q -- '--verbose' examples/ch13-build/package.json`.
  </verify>
  <done>examples/ch13-build scaffolded: a reflection-free Feliz/Elmish counter app (withReactSynchronous, no Decode.Auto/ReactComponent/EntryPoint); package.json has a build:prod script using --noReflection; vite.config.js has an anchored `base` Pages setting + dev watch-ignore; deploy-app.yml is a complete anchored GitHub Actions Pages workflow (in the example dir, NOT in .github/workflows); index.html has div#root.</done>
</task>

<task type="auto">
  <name>Task 2: Build standard AND --noReflection production build; RESOLVE flag form; inspect output</name>
  <files>examples/ch13-build/package.json (only if --noReflection flag form needs adjustment), examples/ch13-build/package-lock.json, examples/ch13-build/ (build + inspect)</files>
  <action>
From `examples/ch13-build/`:
1. `dotnet tool restore` (exit 0), `npm install` (exit 0 — installs react@^18.3, react-dom@^18.3, vite). Generates package-lock.json.
2. `npm run build` (standard `dotnet fable --run npx vite build`) — must exit 0; produces dist/.
3. PRODUCTION BUILD (RESOLVE the --noReflection flag form BY RUNNING IT, do NOT assume): run `npm run build:prod` (`dotnet fable --noReflection --run npx vite build`) — must exit 0; produces dist/. This confirms (a) --noReflection is accepted by Fable 5.3.0 and (b) the reflection-free app builds and bundles under it (Open Q #3: withReactSynchronous + React works under --noReflection). If the exact flag PLACEMENT fails (e.g. Fable wants `--noReflection` before/after the project arg differently), find the working form, update the build:prod script AND deploy-app.yml to match, and record the corrected command. If --noReflection were somehow rejected (not expected — confirmed in CLI source), document the error and the closest working production form.

INSPECT for Task 3 + SUMMARY:
- Confirm dist/ exists after both builds; confirm dist/ contains the bundled JS (Vite output) and index.html.
- OPTIONAL size note: if easy, compare the emitted bundle with and without --noReflection (e.g. byte size of dist assets) to give the chapter a concrete "smaller bundle" statement; if not easily measurable, state qualitatively that --noReflection removes per-type reflection helpers.
- Capture the resolved react/react-dom version from package-lock.json (expected 18.3.1).

Write findings for Task 3 + SUMMARY. Do NOT commit generated files (fable_modules/, *.fs.js, dist/, node_modules/); DO leave package-lock.json staged for commit.
  </action>
  <verify>
`cd examples/ch13-build && npm run build` exits 0 AND `npm run build:prod` exits 0. `test -d examples/ch13-build/dist`. `test -f examples/ch13-build/package-lock.json`. --noReflection accepted: build:prod exit 0 confirms; `grep -q 'noReflection' examples/ch13-build/package.json`. If the flag form was corrected, package.json and deploy-app.yml both reflect the working command (`grep -q 'noReflection' examples/ch13-build/deploy-app.yml`). react version captured: `grep -q 'react' examples/ch13-build/package-lock.json`. Findings recorded for Task 3.
  </verify>
  <done>Both the standard build (`npm run build`) and the production `--noReflection` build (`npm run build:prod`) exit 0 and produce dist/; the working --noReflection command form is confirmed (Open Q #3: withReactSynchronous + React builds fine under --noReflection) and recorded; if adjusted, package.json + deploy-app.yml match; package-lock.json committed; dist/ inspected.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.13 chapter prose (production build, --noReflection, Vite base, Pages workflow) and verify render</name>
  <files>src/ch13-build/index.md</files>
  <action>
Fully replace the stub `src/ch13-build/index.md` following the authoring template. H1 `# 13장. 빌드 최적화와 배포 (Build Optimization and Deployment)`, then a PLAIN `>` blockquote (NOT [!NOTE]) pointing to examples/ch13-build + run command (`cd examples/ch13-build && dotnet tool restore && npm install && npm run build`). Korean prose, English term in parens on FIRST occurrence. Fixed section order 개념 → 예제 → 실행하기 → 핵심 포인트.

PRECONDITION: Use the actual build outcomes recorded in Task 2 (the working --noReflection command form, dist/ output, any size note). Do not claim behavior that did not build.

- 개념 (Concept) — PROSE ONLY, NO code blocks: (1) Explain the production build: `npm run dev` runs an unminified watch build, while a production build (`vite build`) minifies and TREE-SHAKES (tree-shaking) automatically via Rollup — unused code is dropped, no extra config needed. (2) Explain the Fable `--noReflection` flag: by default Fable emits reflection (reflection) helper metadata for each type; `--noReflection` omits it, producing a smaller bundle. CRITICAL interaction — reaffirm the cross-chapter note: `--noReflection` disables type reflection, which BREAKS `Decode.Auto` from Thoth.Json (it uses `typeof<'T>` at runtime); this is exactly why 11장 used MANUAL decoders — manual decoders do not rely on reflection, so the production build keeps working. (Wrap `--noReflection`, `Decode.Auto` in backticks.) (3) Explain GitHub Pages deployment of a SPA: the build output is the static `dist/` folder; deploying to a project page URL (https://OWNER.github.io/REPO-NAME/) means assets live under a subpath, so Vite's `base` must be set to `"/REPO-NAME/"` or assets 404 (Pitfall 9). (4) Explain the GitHub Actions workflow: a workflow checks out, restores .NET tools + npm deps, runs the production build, and uploads `dist/` as a Pages artifact, then deploys. Note plainly (Open Q #4): this tutorial's repo already deploys the BOOK via book.yml, and a repo can have only one active Pages deployment — so this chapter teaches the app-deploy workflow as code you copy to YOUR OWN app repository, rather than wiring a second live deployment here.
- 예제 (Example): Intro sentence, then include anchors in fenced blocks with 1–2 sentence Korean explanations:
  - The app (fsharp fence): `{{#include ../../examples/ch13-build/src/App.fs:app}}` — a small reflection-free app; note it uses no `Decode.Auto`, so `--noReflection` is safe.
  - The Vite base setting (javascript fence): `{{#include ../../examples/ch13-build/vite.config.js:pages-base}}` — explain the subpath/asset-404 reasoning.
  - The deploy workflow (yaml fence): `{{#include ../../examples/ch13-build/deploy-app.yml:workflow}}` — explain each stage briefly (build with `--noReflection`, upload dist/, deploy-pages) and that it goes in `.github/workflows/` of the READER's app repo.
- 실행하기 (Running): Prose for the local production build: `dotnet tool restore` → `npm install` → `npm run build` (standard) and `npm run build:prod` (the `--noReflection` form verified in Task 2) — both produce the `dist/` folder, the deployable static bundle. Explain that to deploy, the reader sets `base` to their repo name, commits the workflow to their app repo, enables Pages (Settings → Pages → Source: GitHub Actions), and on push the app appears at https://OWNER.github.io/REPO-NAME/. Do NOT paste raw terminal output. (If Task 2 measured a size difference, state it as a concrete sentence; otherwise describe qualitatively.)
- 핵심 포인트 (Key Points): 6–7 bullets: `vite build`는 자동 tree-shaking + minify; `--noReflection`은 타입 리플렉션 메타데이터 제거 → 번들 축소; `--noReflection`은 `Decode.Auto`를 깨뜨림 → 11장의 수동 디코더 덕분에 프로덕션 빌드가 동작 (cross-link reaffirmed); 배포 산출물은 정적 `dist/` 폴더; GitHub Pages 프로젝트 페이지는 Vite `base: "/REPO-NAME/"` 필요 (안 그러면 에셋 404); GitHub Actions 워크플로로 빌드→`dist/` 업로드→배포; 한 저장소는 하나의 Pages 배포만 — 이 워크플로는 당신의 앱 저장소에 복사해서 사용 (이 튜토리얼 저장소는 책을 배포 중).

Then run `mdbook build` and confirm Ch.13 renders with all anchors resolved.
  </action>
  <verify>
`mdbook build` exits 0. `test -f book/ch13-build/index.html`. Anchors resolved in rendered HTML: `grep -q 'withReactSynchronous' book/ch13-build/index.html && grep -q 'base' book/ch13-build/index.html && grep -q 'deploy-pages' book/ch13-build/index.html`. Template sections present: `for s in 개념 예제 실행하기 '핵심 포인트'; do grep -q "$s" src/ch13-build/index.md || exit 1; done`. No admonish: `! grep -q '\[!NOTE\]' src/ch13-build/index.md`. No mermaid fence: `! grep -q '```mermaid' src/ch13-build/index.md`. --noReflection + Decode.Auto cross-link present: `grep -q 'noReflection' src/ch13-build/index.md && grep -q 'Decode.Auto' src/ch13-build/index.md`. Code via include (app + vite + workflow): `grep -c '#include' src/ch13-build/index.md` returns >= 3. 개념 prose-only + flags backtick-wrapped: manual review.
  </verify>
  <done>src/ch13-build/index.md follows the template in Korean, 개념 prose-only, includes the three anchors (app / vite base / deploy workflow), explains production build + tree-shaking + `--noReflection` (and reaffirms it breaks `Decode.Auto` so Ch.11's manual decoders matter) + Vite `base` for Pages + the workflow-as-code (no second live deploy), wraps flags/attributes in backticks, uses plain blockquotes, no mermaid; mdbook build renders the chapter with anchors resolved.</done>
</task>

</tasks>

<verification>
- `cd examples/ch13-build && dotnet tool restore && npm install && npm run build` exits 0, and `npm run build:prod` (`dotnet fable --noReflection --run npx vite build`) exits 0, both producing dist/ (SC-3 + SC-4 + phase-wide build gate).
- The example app is reflection-free (no Decode.Auto/ReactComponent), mounts via withReactSynchronous "root", and builds correctly under --noReflection (Open Q #3 resolved).
- vite.config.js teaches the Pages `base` subpath (anchored); deploy-app.yml is a complete copy-able GitHub Actions Pages workflow (anchored) building with --noReflection and deploying dist/ — and is NOT installed under .github/workflows (Open Q #4: no second live deployment).
- src/ch13-build/index.md follows the authoring template, Korean, 개념 prose-only, code via {{#include}} only (App.fs + vite.config.js + deploy-app.yml anchors), plain blockquotes, no mermaid, flags/attributes backtick-wrapped, reaffirms the --noReflection ↔ Decode.Auto cross-link.
- `mdbook build` exits 0 and book/ch13-build/index.html contains the anchored code.
</verification>

<success_criteria>
REAL-03 satisfied and Phase 5 Success Criteria 3 + 4 met: a reader runs the Ch.13 example and produces an optimized production bundle with `--noReflection` (exit 0), understands Vite tree-shaking and the `base` subpath requirement, and has a complete, copy-able GitHub Actions workflow to deploy a Fable SPA to GitHub Pages (taught as code, not wired). The example builds with `npm run build` (exit 0) and the chapter reaffirms that Ch.11's manual decoders are why the --noReflection production build works.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ecosystem-and-real-world-axis/05-03-SUMMARY.md` recording: the example files created, the verified build outputs (both `npm run build` and `npm run build:prod` exit 0), the confirmed working `--noReflection` command form (Open Q #3 resolved: withReactSynchronous + React builds under --noReflection), any bundle-size observation, the resolved react version, and confirmation that the deploy-app.yml workflow is a teaching artifact (NOT installed under .github/workflows — Open Q #4). Reaffirm the cross-link: --noReflection breaks Decode.Auto, so Ch.11's manual decoders are required for production.
</output>
