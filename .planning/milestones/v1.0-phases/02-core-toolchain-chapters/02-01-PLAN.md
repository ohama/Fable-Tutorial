---
phase: 02-core-toolchain-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ch01-setup/App.fsproj
  - examples/ch01-setup/src/App.fs
  - examples/ch01-setup/index.html
  - src/ch01-setup/index.md
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Reader following Ch.1 runs `dotnet tool restore && npm install && npm run build` in examples/ch01-setup and it exits 0 with no errors"
    - "Fable emits examples/ch01-setup/src/App.fs.js (compiled output) and Vite build produces examples/ch01-setup/dist/"
    - "The example writes visible text to the browser page (DOM #app element), not just the DevTools console"
    - "src/ch01-setup/index.md follows the authoring template (개념 → 예제 → 실행하기 → 핵심 포인트) in Korean and pulls all code via {{#include}} anchors"
    - "mdbook build renders Ch.1 with the anchored App.fs code visible in the output HTML"
    - "Generated artifacts (fable_modules/, *.fs.js, dist/) are gitignored and not committed"
    - "02-01-SUMMARY.md exists and documents the verified runnable example pattern for plans 02-02 and 02-03 to copy"
  artifacts:
    - path: "examples/ch01-setup/src/App.fs"
      provides: "Browser Hello World: writes to DOM via Fable.Browser.Dom; no [<EntryPoint>]; ANCHOR: hello-world"
      contains: "document.getElementById"
    - path: "examples/ch01-setup/App.fsproj"
      provides: "net10.0 fsproj with Fable.Core 5.0.0 + Fable.Browser.Dom 2.20.0"
      contains: "Fable.Browser.Dom"
    - path: "src/ch01-setup/index.md"
      provides: "Full Ch.1 prose: setup steps, --verbose rationale, include-by-anchor"
      min_lines: 40
    - path: ".gitignore"
      provides: "Ignores Fable/Vite generated artifacts"
      contains: "fable_modules"
    - path: ".planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md"
      provides: "Canonical runnable-example-pattern reference for 02-02 and 02-03"
      contains: "App.fs"
  key_links:
    - from: "examples/ch01-setup/index.html"
      to: "examples/ch01-setup/src/App.fs.js"
      via: "<script type=module src=./src/App.fs.js>"
      pattern: "src/App\\.fs\\.js"
    - from: "src/ch01-setup/index.md"
      to: "examples/ch01-setup/src/App.fs"
      via: "{{#include ...:hello-world}} anchor"
      pattern: "#include.*ch01-setup/src/App.fs:hello-world"
    - from: "examples/ch01-setup/src/App.fs"
      to: "DOM #app"
      via: "Fable.Browser.Dom document.getElementById"
      pattern: "document\\.getElementById"
---

<objective>
Evolve the existing Phase-1 ch01-setup skeleton into a real, runnable browser Hello World and write the full Ch.1 (프로젝트 설정) chapter prose. This is the FIRST phase where Fable actually compiles and runs — this plan proves the entire compile pipeline (dotnet tool restore → Fable compile → Vite build) end-to-end ONCE, establishing the verified pattern that Ch.2 and Ch.3 copy.

Purpose: Satisfies SETUP-01 and Phase 2 Success Criterion 1 (reader sets up .NET SDK + dotnet tool restore + Fable + Vite and sees Hello World in the browser) and contributes to Criterion 4 (npm run build exits 0, {{#include}} anchors render). (SETUP-01)
Output: A working examples/ch01-setup that builds with `npm run build`, visible browser DOM output, an updated .gitignore, and a complete Korean Ch.1 chapter that follows the authoring template.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-toolchain-chapters/02-RESEARCH.md

Repo root: `/Users/ohama/projs/FableTutorial`. All paths relative to that root.

TASK ORDERING: Tasks in this plan execute SEQUENTIALLY (Task 1 → Task 2 → Task 3). Task 2's `npm run build` depends on Task 1's fsproj/App.fs edits, and Task 3's `mdbook build` + 실행하기 prose depend on Task 2 having actually built the example. Do not reorder or parallelize.

Authoring template (MANDATORY — read before writing prose): @src/introduction.md "챕터 구조" section. Section order is fixed: 개념 → 예제 → 실행하기 → 핵심 포인트. Korean prose; English term in parens on FIRST occurrence only.

Existing skeleton facts (verified on disk):
- `examples/ch01-setup/App.fsproj` lives at the example ROOT (NOT in src/). It uses `<Compile Include="src/App.fs" />`. KEEP this layout — do not move the fsproj. Fable run against this fsproj emits `src/App.fs.js`.
- `examples/ch01-setup/src/App.fs` currently uses `[<EntryPoint>]` + `printfn` (console-only). This must change.
- `index.html`, `package.json` (with `--verbose`), `vite.config.js` are already CORRECT — do not rewrite them except index.html title if desired.
- Current `src/ch01-setup/index.md` is a bare skeleton that does NOT follow the authoring template — it will be fully rewritten.

CRITICAL constraints:
- admonish preprocessor is COMMENTED OUT (STATE.md [01-01]). Do NOT use `> [!NOTE]` / `> [!WARNING]` / admonish fence syntax anywhere. Use PLAIN markdown blockquotes: `> **주의:** ...`. (The template snippet in introduction.md shows `> [!NOTE]` as illustrative ONLY — do not copy that syntax into real chapter prose.)
- ALL code via `{{#include}}` anchors — never paste raw F# into the markdown. Include path from `src/ch01-setup/index.md` is `../../examples/ch01-setup/src/App.fs:anchor` (STATE.md [01-03]).
- `--verbose` is mandatory in the dev watch script (Fable #3631 deadlock) — prose must EXPLAIN why, not present it as magic.
- Versions: Fable.Core 5.0.0, Fable.Browser.Dom 2.20.0, net10.0, Vite ^6.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fable.Browser.Dom, fix App.fs for browser DOM output, update .gitignore</name>
  <files>examples/ch01-setup/App.fsproj, examples/ch01-setup/src/App.fs, examples/ch01-setup/index.html, .gitignore</files>
  <action>
Update `examples/ch01-setup/App.fsproj`: add `<PackageReference Include="Fable.Browser.Dom" Version="2.20.0" />` to the existing ItemGroup that has Fable.Core 5.0.0. Keep `<Compile Include="src/App.fs" />` and net10.0 / RootNamespace="Ch01Setup" unchanged. (Use `dotnet add examples/ch01-setup/App.fsproj package Fable.Browser.Dom --version 2.20.0` OR hand-edit the fsproj — either is fine; hand-edit is more deterministic.)

Rewrite `examples/ch01-setup/src/App.fs` to produce VISIBLE browser output (RESEARCH Pitfall 1, Code Examples Ch.1). Remove `[<EntryPoint>]` and `printfn` (browser apps run module top-level; EntryPoint's main is never called — RESEARCH State of the Art / Open Question 1). New content:

```fsharp
module App

open Browser

// ANCHOR: hello-world
let app = document.getElementById "app"
app.innerHTML <- "<h1>Hello from Fable!</h1>"
// ANCHOR_END: hello-world
```

Keep the `// ANCHOR: hello-world` / `// ANCHOR_END: hello-world` markers so the existing {{#include ...:hello-world}} in the chapter keeps resolving.

`index.html` already has `<div id="app"></div>` and `<script type="module" src="./src/App.fs.js">` — verify these are present (they are). Optionally update the `<title>` to "Ch01 - Fable 셋업". Do NOT change the script src path.

Update repo-root `.gitignore` to add the Fable/Vite generated artifacts (RESEARCH Open Question 3 — currently MISSING) BEFORE Task 2 builds and commits anything: append `fable_modules/`, `**/*.fs.js`, and `dist/`. The existing entries (book/, bin/, obj/, node_modules/, .DS_Store) stay. Result must ignore: built Fable JS (`**/*.fs.js`), the `fable_modules/` dep cache, and Vite `dist/` output, so compiled artifacts are never committed. (These .gitignore additions MUST land in Task 1 — i.e. before the Task 2 build produces any generated artifacts — so nothing compiled is ever staged.)
  </action>
  <verify>
`grep -q 'Fable.Browser.Dom' examples/ch01-setup/App.fsproj`. `grep -q 'document.getElementById' examples/ch01-setup/src/App.fs && ! grep -q 'EntryPoint' examples/ch01-setup/src/App.fs`. Use fixed-string greps for the .gitignore checks (the patterns contain `*` and `.` which break regex grep): `grep -qF 'fable_modules/' .gitignore && grep -qF '**/*.fs.js' .gitignore && grep -qF 'dist/' .gitignore`. `grep -q 'id="app"' examples/ch01-setup/index.html`.
  </verify>
  <done>App.fsproj references Fable.Browser.Dom 2.20.0; App.fs writes to DOM #app (no EntryPoint, no printfn) with hello-world anchor intact; .gitignore excludes fable_modules/, **/*.fs.js, dist/ (verified with grep -qF fixed-string checks) and these entries are in place before any compiled output is produced.</done>
</task>

<task type="auto">
  <name>Task 2: Compile and build the example end-to-end (prove the pipeline)</name>
  <files>examples/ch01-setup/ (build only — no source files modified)</files>
  <action>
This is the load-bearing verification for the whole phase. Run the real toolchain (confirmed available: dotnet 10.0.203, node v25.9.0, npm 11.12.1, Fable 5.3.0 pinned at repo root).

From `examples/ch01-setup/`:
1. `dotnet tool restore` — installs Fable 5.3.0 from the repo-root `.config/dotnet-tools.json` (tool restore walks up the tree). Must exit 0.
2. `npm install` — installs vite@^6. Must exit 0.
3. `npm run build` — runs `dotnet fable --run npx vite build`. Fable compiles `src/App.fs` → `src/App.fs.js`; Vite bundles into `dist/`. Must exit 0.

Confirm the outputs exist after build: `examples/ch01-setup/src/App.fs.js` (Fable output) and `examples/ch01-setup/dist/index.html` (Vite output).

If the build fails: read the error, fix it (common causes: wrong package version, missing `open Browser`, fsproj Compile path). Do NOT mark done until `npm run build` exits 0. Do not commit any generated files (they are gitignored from Task 1).

Note for prose accuracy (Task 3): observe the actual command output and the location of App.fs.js so the chapter's 실행하기 section is accurate.
  </action>
  <verify>
`cd examples/ch01-setup && dotnet tool restore && npm install && npm run build` exits 0. `test -f examples/ch01-setup/src/App.fs.js`. `test -f examples/ch01-setup/dist/index.html`. `git status --porcelain examples/ch01-setup` shows no staged *.fs.js or dist/ (gitignored).
  </verify>
  <done>dotnet tool restore + npm install + npm run build all exit 0; App.fs.js and dist/index.html generated; no generated artifacts tracked by git.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Ch.1 chapter prose, verify mdbook render, and write the SUMMARY</name>
  <files>src/ch01-setup/index.md, .planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md</files>
  <action>
Fully rewrite `src/ch01-setup/index.md` following the authoring template (introduction.md "챕터 구조"). Korean prose; English term in parens on first occurrence. Section order FIXED: 개념 → 예제 → 실행하기 → 핵심 포인트. Start with H1 `# 1장. 프로젝트 설정 (Project Setup)` then a plain blockquote pointing to the example dir + run command (NOT `[!NOTE]` — plain `>`).

Content requirements per section:
- 개념 (Concept): Explain the Fable toolchain for an F# dev new to JS tooling — what `.NET SDK`, `dotnet tool restore` (reads .config/dotnet-tools.json, pins Fable 5.3.0), Fable (compiler F#→JS), and `Vite` (dev server + bundler) each do and how they fit together (Fable compiles .fs → .fs.js; Vite serves/bundles). Explain that Fable emits `App.fs.js` ADJACENT to the source (RESEARCH Pitfall 2) and index.html loads `./src/App.fs.js`. Explain WHY `--verbose` is required in the watch script (without it, Vite freezes after a few saves — Fable #3631; RESEARCH Pitfall 3). No code in this section.
- 예제 (Example): One sentence describing the example (브라우저 페이지에 보이는 Hello World). Then the F# code via include anchor (the ONLY code in the chapter):
  ```fsharp
  {{#include ../../examples/ch01-setup/src/App.fs:hello-world}}
  ```
  Followed by 1–3 sentences explaining: `open Browser` (from Fable.Browser.Dom), `document.getElementById "app"`, and that there is NO `[<EntryPoint>]` because browser Fable runs module top-level statements (contrast with .NET console convention).
- 실행하기 (Running): Describe the three-command cold-start sequence in prose: `dotnet tool restore` → `npm install` → `npm run dev` (watch + Vite at http://localhost:5173) and `npm run build` (production → dist/). Describe the EXPECTED behavior in prose (page shows "Hello from Fable!" heading; the value is written to the DOM, not the console). Do NOT paste raw terminal output.
- 핵심 포인트 (Key Points): 3–5 bullets, e.g.: Fable는 .fs를 인접한 .fs.js로 컴파일한다; 브라우저 출력은 printfn(콘솔)이 아니라 DOM에 써야 보인다; --verbose는 watch deadlock 방지에 필수; dotnet tool restore가 Fable 버전을 고정한다.

Then run `mdbook build` from repo root and confirm Ch.1 renders with the anchored code.

FINALLY, write `.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md` (see <output>). This SUMMARY is a HARD deliverable of this plan — plans 02-02 and 02-03 are wave-2 and depend on it as their canonical "runnable example pattern" reference. The plan is NOT done until the SUMMARY file exists on disk.
  </action>
  <verify>
`mdbook build` exits 0 from repo root. `test -f book/ch01-setup/index.html`. The rendered HTML contains the anchored code: `grep -q 'document.getElementById' book/ch01-setup/index.html` (proves {{#include}} resolved). `grep -q '개념' src/ch01-setup/index.md && grep -q '실행하기' src/ch01-setup/index.md && grep -q '핵심 포인트' src/ch01-setup/index.md` (template sections present). Confirm NO admonish syntax: `! grep -q '\[!NOTE\]' src/ch01-setup/index.md`. SUMMARY exists: `test -f .planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md`.
  </verify>
  <done>src/ch01-setup/index.md follows the 개념→예제→실행하기→핵심 포인트 template in Korean, pulls code only via {{#include}} anchor, uses plain blockquotes (no admonish), and mdbook build renders the chapter with the App.fs code visible in the HTML. 02-01-SUMMARY.md exists on disk documenting the verified runnable example pattern for 02-02/02-03.</done>
</task>

</tasks>

<verification>
- `cd examples/ch01-setup && dotnet tool restore && npm install && npm run build` exits 0 (Phase Success Criterion 1 + 4).
- examples/ch01-setup/src/App.fs.js and dist/index.html are generated; neither is tracked by git (.gitignore updated).
- App.fs writes to the DOM (visible browser output), no [<EntryPoint>], no printfn.
- src/ch01-setup/index.md follows the authoring template, Korean prose, code via {{#include}} anchor only, plain blockquotes (no admonish).
- `mdbook build` exits 0 and book/ch01-setup/index.html contains the anchored App.fs code.
- `.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md` exists (gate for wave-2 plans).
</verification>

<success_criteria>
SETUP-01 satisfied: a reader following Ch.1 can set up .NET SDK + dotnet tool restore + Fable + Vite and see a Hello World rendered in the browser. The example builds with `npm run build` (exit 0) and the chapter renders with end-to-end {{#include}} anchor wiring. The verified runnable example pattern is established (and recorded in 02-01-SUMMARY.md) for Ch.2 and Ch.3 to copy.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-toolchain-chapters/02-01-SUMMARY.md` recording: the exact App.fs final content, that Fable.Browser.Dom 2.20.0 was added, the verified build command sequence and its outputs (App.fs.js location, dist/), the .gitignore additions, and any deviation between RESEARCH Pattern 2 (fsproj in src/) and the actual on-disk layout (fsproj at example root with Compile Include="src/App.fs"). This SUMMARY is the canonical "runnable example pattern" reference for plans 02-02 and 02-03 (which are wave-2 and depend on this file).
</output>
